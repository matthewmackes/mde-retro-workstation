# MDE-Retro — Project Worklist

The **single** durable tracker (see `.claude/CLAUDE.md` §5). In-session Task tools
are a scratchpad; this file wins on any divergence. `rust/SPEC-*.md` are design
docs, not a parallel worklist — lift actionable items into here.

**Legend (locked):** `[ ]` Open · `[>]` In Progress · `[✓]` Done · `[!]` Blocked.
No `[~]` deferred, no `[x]`. Flip to `[>]` before substantive edits; `[✓]` only when
the §3 Definition of Done holds (reachable + observably works + verified).

---

## Compliance (from the first `/audit` sweep — see `docs/COMPLIANCE.md`)

Fixed in the reorg pass (✓ below = done & verified): doc drift across all 6 docs;
removed 5 dead surfaces (flag widget + LOGO_*, progress_style, use_beos,
frame::pressed, _gauge); routed 3 raw-hex leaks through palette
(SHELL_HEADER/GRAY_TEXT/BACKGROUND); pinned 3 ground-truth constants
(WINDOW_FRAME, TITLE_TEXT, INFO_TITLE_PX); added the 3 missing `%post` symlinks.

Remaining (FINISH unless noted):

- [✓] **§2.1** Carbon icon-accent table moved to `palette::icon_accent()`;
  `icons.rs` calls it. (No raw hex at the icon site.)
- [✓] **§2.3** `.size()` literals replaced with named `metrics` constants
  (`IDENTIFY_PX`, `WIZARD_HEADING_PX`, `WIZARD_STATUS_PX`) at display.rs/installer.rs.
- [✓] **§2.2** `TASKBAR_BUTTON_MIN`, `IDENTIFY_PX`, `WIZARD_*` pinned in `checklist.rs`.
- [✓] **§3 mockup** `display.rs` Effects tab — greyed (no on_toggle); dead
  `fx_*` fields/messages/handlers removed.
- [✓] **§3 mockup** `taskbar_properties.rs` "Show clock" + "Use Personalized Menus"
  — greyed; dead `show_clock`/`personalized` state + messages removed.
- [✓] **§4 packaging** Add `assets/licenses/DroidSans-Apache-2.0.txt` to the asset
  list + a Droid Sans entry to `NOTICE.md` (the font is embedded in the shipped
  binary; IBM Plex — embedded the same way — is already covered).
- [✓] **§3 decision** `wlr.rs` `Wm::close`/`Wm::set_maximized` REMOVED (per-window
  close/maximize is labwc's by the compositor boundary, so the taskbar never calls
  them); `outputs.rs` `Output::make` FINISHED — folded into `Output::label()` so the
  Display dropdown shows the full EDID identity ("Dell U2419H (DP-1)"), unit-pinned.
- [✓] **§1 docs** Deeper prose pass on `PREVIEW.md`/`ACCURACY.md`: fixed the live
  boundary (mde↔labwc, themerc-sourced titlebar colors; "live sway IPC" →
  wlr-foreign-toplevel), retired stale "gaps" now shipped (tray, taskbar/Start
  popups, checkbox/radio/tab/group-box widgets), and reframed the session cutover
  as labwc/done. Legitimate harness `sway` references (headless wlroots driver) kept.

**Second `/audit` sweep (2026-06-02 — workflow + adversarial verify, 29 raw → 14
confirmed; full table in `docs/COMPLIANCE.md`). All 14 resolved this pass:**

- [✓] **§2.1** Start-menu logo banner — 5 raw hex in the `menu.rs` SVG lifted to
  `palette::LOGO_*` roles, emitted via a new `palette::hex_fixed` (fixed brand art,
  no per-theme remap), pinned in `checklist.rs`. Render byte-identical (same hex).
  Supersedes the first sweep's mistaken "menu banner SVG is a §2.1 carve-out".
- [✓] **§3 unreachable** `palette::URGENT` removed (zero refs; `SPEC-windows10`
  promised a critical-toast tint that was never wired — SPEC corrected, tint lifted
  to [[E3-urgent-tint]]).
- [✓] **§3 unreachable** `popup.rs` `items_for("desktop")` arm removed — labwc
  serves the desktop right-click from its own static `menu.xml`, so nothing ever
  passed `"desktop"`; era-aware desktop menu lifted to [[E7.10a]].
- [✓] **§1/§5 docdrift** sway→labwc / Win2000→Carbon corrected in the *module*
  docs the first sweep (top-level prose only) never reached: `main.rs` USAGE
  (`--help`), `panel.rs`, `files.rs`, `display.rs`, `install.rs`, `action_center.rs`,
  `search.rs`, `palette.rs` (ACTIVE_TITLE), `metrics.rs` (SIZE/FIXED_FRAME).
- [✓] **E3-urgent-tint** Wire a danger-red tint for critical toasts
  (`hint_urgency >= 2`): add an urgent role to `palette.rs` remapped in
  `win10()`/`carbon()`, applied in `action_center.rs::toast_view` (which today only
  suppresses auto-dismiss for critical). Verify via a windows10 toast capture. —
  Re-added `palette::URGENT` (the role the 2nd audit had removed as dead) — now
  **wired**: `action_center.rs::toast_view` computes `critical = hint_urgency >= 2`
  and tints the critical toast danger-red — a red summary + a 2px red border — via
  `palette::color(palette::URGENT)`. `carbon()` already carried the `URGENT → Carbon
  danger red` arm (dark-aware `#fa4d56` / light `#da1e28`), reachable again now that
  the const exists; `win10()` falls through to it (so no redundant arm). `URGENT`
  pinned in `checklist.rs`; `SPEC-windows10`:286 corrected (the tint is wired, no
  longer "removed/follow-up"). Verified by captures: a `hint_urgency:2` toast
  ("Battery critically low") shows a red summary + red border; a normal (urgency 1)
  toast is unchanged — both no-panic.
- [✓] **install-assets-sway-drift** `assets/install-assets.sh` (shipped to
  `/usr/share/mde/scripts/`, run live by `mde install --assets` → `install.rs`).
  **Investigated 2026-06-02 — a real broken-feature bug, not just cosmetic drift:**
  `run_win2k` shells out to `$SWAY_SCRIPTS/install-win2k-icons.py` with
  `SWAY_SCRIPTS=~/.config/sway/scripts`, but (a) the labwc path deploys
  `~/.config/labwc`, never `~/.config/sway`, and (b) the RPM `assets` list ships only
  `install-assets.sh` + `install-chicago95.sh`, NOT `install-win2k-icons.py` (which
  lives only in the repo's old sway skel `home/.config/sway/scripts/`). So on the
  RPM/labwc path `run_win2k` ALWAYS hits the skip branch → the Win2k icon theme never
  installs. **Fix:** add `install-win2k-icons.py` to the RPM `assets` (→
  `/usr/share/mde/scripts/`), repoint `run_win2k` at `$HERE/install-win2k-icons.py`
  (drop `SWAY_SCRIPTS` + the "run ../install.sh first" message), and change the
  closing "swaymsg reload" → "labwc --reconfigure". (Touches `rust/mde/Cargo.toml`;
  deferred past the in-flight E8.5/6/7 audit which reads that file.) — **Fixed:**
  the RPM `assets` now ship `install-win2k-icons.py` to `/usr/share/mde/scripts/`
  (co-located with `install-assets.sh`); `run_win2k` invokes
  `$HERE/install-win2k-icons.py` (dropped `SWAY_SCRIPTS` + the "run ../install.sh
  first" message); the closing "swaymsg reload" → "Log out and back in". Verified by
  simulating the RPM co-location — `install-assets.sh --only win2k` finds + runs the
  `.py`, installing the Win2k theme (157 aliases + index.theme + icon cache) to
  `~/.local/share/icons/Win2k`; previously it always hit the skip branch. Cargo.toml
  parses; `bash -n` clean; no sway refs remain. (Residual: the `.py`'s own tarball
  *cache* path is still `~/.config/sway/resources/` — a dir it creates + downloads
  into, harmless; a minor follow-up.)

## Backlog

- [ ] **Mobile Devices (native KDE Connect)** — 15-Q spec in [[mde-kdeconnect]].
  Shared crate `matthewmackes/MDE-KDECnt-Rust` (public) stands up the protocol core
  (✓ Phase 1: extracted + 181 tests). **E9-unblock in progress (2026-06-02):** the
  `mde-kdc-host` crate was added to the shared workspace (commit `edccc67`, local —
  unpushed) — **✓ host increment 1**: `PeerId` + `HostError` + the event stream
  (`HostEvent`/`EventStream`) + the on-disk `PairingStore` at `~/.config/mde/connect/`
  (RSA-2048 identity keygen via the `rsa` crate, sign/verify via the proto's
  ring-backed `PairingKeyPair`, `devices.toml` atomic persistence, and a
  `mde_kdc_proto::crypto::KeyStore` impl), all standalone-unit-tested (7 tests, incl.
  the rsa→ring PKCS#8 interop; proto's 181 still green). **✓ host increment 2** (commit
  `9c1268f`): the object-safe `Transport`/`Connection` traits (async-trait) + an
  in-process `LoopbackTransport` that round-trips packets through the real frame codec
  (9 tests). **✓ audit-hardening** (commit `90ccddc`): an adversarial audit of
  increments 1+2 returned the rsa→ring interop and "session keys never touch disk" as
  VERIFIED-GOOD; fixed the one real issue (`identity.pkcs8` was 0644-then-chmod-0600 —
  now created 0600 atomically via `OpenOptions::create_new().mode()`, which also refuses
  a symlink; store dir 0700; a test asserts both) + the empty-`$XDG_CONFIG_HOME` fallback
  + two doc-drift fixes (10 tests). **✓ host increment 3a** (commit `6f34adf`): the
  UDP/1716 LAN **discovery** half — `UdpDiscovery` binds the socket, broadcasts our
  `kdeconnect.identity` announce on a timer, and folds inbound announces into the proto
  `DiscoveryRegistry`, emitting `PeerDiscovered`/`PeerLost`; address-cached for the TCP
  connect; `ingest`/`prune` unit-tested with an injected clock + a hermetic two-instance
  loopback round-trip of `run` (15 tests). **✓ host increment 3b.1** (crate commit
  `5c32819`, owner-authorized 2026-06-03 — *flagged for adversarial audit*): the crypto
  foundation of the TLS handshake, **ported from the `/tmp/MDE-guidance` reference host
  crate** (not invented) — `keygen.rs` (RSA-2048 PKCS#8 via the `rsa` crate + `issue_identity_cert`
  binding a self-signed X.509 cert, CN=device-id, to the same keypair via `rcgen 0.13`) and
  `tls.rs` (SHA-256 `compute_fingerprint`, `PinnedFingerprintVerifier`/`FirstPairVerifier`
  rustls `ServerCertVerifier`s for KDE Connect's pin-at-first-pair model, `build_client_config`
  on the ring provider, and `connect_pinned_tls` the live tokio-rustls TLS-over-TCP connect).
  34 host tests pass, fmt+clippy clean. Remaining: [✓] host increment **3b.2** (a–f) — wired
  `connect_pinned_tls` into a `LanTransport` + outbound `open` + the **inbound mutual-TLS
  listener** (3b.2e) + `PairingStore::records()` (3b.2f), discovery + connections on the
  event stream;
  [✓] pushed MDE-KDECnt-Rust + rewired MDE-Retro to depend on it (**path dep**, monorepo-aligned;
  git dep was the pre-monorepo plan); [>] `mde connect` daemon **landed** (E9.1: `org.mde.Connect`
  roster); remaining its autostart unit + pairing modal/tray; [ ] capability surfaces (notifications
  bidirectional + a freedesktop notify daemon, clipboard, battery, file transfer via
  Explorer "Send to", MPRIS, run-commands, SMS); [ ] "Mobile Devices" Control Panel
  applet. LAN-only; remote-input deferred. Config at ~/.config/mde/connect/.
- [✓] **B — Add/Remove Programs** (custom dnf-backed package manager, replaces dnfdragora which hung on every launch and couldn't be killed). **Done (B.1).** New `mde add-remove` surface (`packages.rs`): a themed iced window listing `catalogue::catalogue()` grouped by category, each row showing the package + its rpm-read installed state with an **Install** / **Remove** button (mandatory base-session packages shown locked "Required"). Install/Remove run `pkexec dnf install|remove -y` **off the UI thread** (Task::perform + spawn_blocking; polkit prompts), then re-read rpm to refresh the row; one transaction at a time (buttons disable while busy). **dnfdragora fully removed**: dropped from `requires`; the Control Panel "Add/Remove Programs" applet (launched via the running binary through a new `control_panel` Activate special-case for `mde <sub>` tools), the Win10 Settings ▸ Apps "Apps & features" page (`Kind::Mde("add-remove")`), and the Start "Uninstall" context action all point here; the "Automatic Updates"/"MackesDE Update" entries now run `sudo dnf upgrade` in a terminal. Verified: gallery capture shows the categorized list with Required/Install/Remove + Carbon theming + "60 programs"; build/clippy/fmt clean, 56+ tests, no-panic launch. **Follow-ups:** **[[B.2a]] search/filter box — ✓ done** (a header `text_input`; live case-insensitive substring filter over name + package id, hides empty categories, "M of N programs" status; `pkg_matches` unit-tested; gallery-verified). Remaining: [ ] **B.2b** per-transaction progress (stream dnf output rather than the spinner-less busy flag); [✓] **B.2c** a dedicated Updates view (`dnf check-update` list + Update-all). — **Done.** Add/Remove Programs now has a **Programs | Updates** tab strip. The Updates tab lists pending updates parsed from `dnf check-update` (a pure, unit-tested `parse_check_update`: `name.arch → version` rows, epoch versions preserved, the metadata line + the trailing "Obsoleting Packages" block excluded) and offers **Check for updates** (re-runs the check) + **Update all** (`pkexec dnf upgrade -y`), both off the UI thread via the existing `Task::perform`+`spawn_blocking` pattern, one transaction at a time (controls disable while busy/checking). The tab auto-checks on first open; `mde add-remove --updates` deep-links straight to it (a target the "MackesDE Update" Control Panel entry can repoint to). dnf exit codes are honoured — 0 = "up to date", 100 = parse the list, other = a surfaced error, so "couldn't check" is distinct from "no updates". **Verified by gallery capture (real data):** the Updates tab on this host parsed **229 real Fedora-44 updates** with correct package/version columns (incl. epochs) + the enabled Check/Update-all controls + "229 updates available" status; the Programs tab is unchanged but for the new tab strip. 3 parser/filter unit tests green, build/clippy/fmt clean, both launch paths no-panic. **B is now fully done** (B.1 + B.2a + B.2c; only [[B.2b]] per-transaction progress streaming remains as a separate follow-up).
- [✓] Carbon polish (from the theme survey): primary-blue / ghost button variants
  (current Carbon buttons are flat secondary); explicit accent-tinted labwc
  titlebar buttons; popup.rs context menus still bottom-anchored under the top bar.
  — **popup-anchoring sub-item ✓ done:** `popup::view` now anchors the menu to the
  edge the taskbar occupies via a new `bar_at_bottom()` (§7 per-era anchor): Win2000
  + Win10-bottom → bottom-left, **Carbon (the default top bar)** / BeOS-left /
  Win10-top → top-left. Previously every popup floated bottom-left, so under the
  Carbon top bar the taskbar right-click menu appeared at the *opposite* edge,
  disconnected from the bar. Verified by captures: Carbon now anchors top-left
  (flat chrome), Win2000 stays bottom-left (silver 3D) — both correct. **primary-blue
  / ghost button variants ✓ done:** new shared `mde_ui::button_primary` (accent-filled
  CTA, light label, hover/press tint the accent via a palette-derived `mix` — no second
  hex, §2.1) + `mde_ui::button_ghost` (transparent, accent text, faint accent wash on
  hover) in `widget/mod.rs`, flat-radius-aware via `ctl_radius()`, unit-tested. Wired to
  real CTAs: Sign-in ▸ **Set/Change PIN** → primary, **Remove** → ghost; Family & other
  users ▸ **Add account** → primary, the remove-confirm **Cancel** → ghost (destructive
  "Delete account" deliberately stays neutral, not blue). Capture confirms the three-tier
  hierarchy (blue-filled primary vs text-only ghost vs flat secondary). (The "accent-tinted
  labwc titlebar buttons" sub-item is **superseded by the MackesDE rebrand** — commit
  `92bc4e4` made the Win10 labwc titlebar match Carbon and `895f53c` retired the per-era
  accent, so there's no separate accent tint to apply.) **Carbon polish fully done.**

## 2.0 — Windows 10 era

> Lifted from `rust/SPEC-windows10.md` (per CLAUDE.md §5: specs are design docs, the
> worklist is the tracker). Release cut = all of M1–M3 `[✓]` (decision Q15). Each
> story carries a screenshot-/bench-observable acceptance check (§3 Definition of Done).

> **Note:** the **Mobile Devices (KDE Connect)** Backlog item above is the hard
> dependency of **E8/E9** here (the `MDE-KDECnt-Rust` crate) — they are the same
> work surfaced per-era (D5: Your Phone *is* Mobile Devices). Land the crate first.

### Milestone sequencing (M1/M2/M3 are internal only — D15)

Per **D15**, all 2.0 scope ships; M1/M2/M3 are build-ordering, not a staged release. The ordering is driven by the dependency graph, not feature priority.

### M1 — the five P0 pillars + their foundation (E0–E7, E19)

**E0 is the universal gate.** Nothing renders Win10 until `Theme::Windows10` + `win10()` + the `state.rs` era field + `main.rs` startup select + per-era routing land. Every other epic carries a defensive "E0.1 adds it if absent" clause, but in practice E0 lands first.

After E0, the five pillars (**D3**) and Power/Session land together because they are the irreducible Win10 desktop and they cross-reference each other:
- **E2 (taskbar)** anchors the bar and owns the Start-button click + the Action Center badge — so E1/E3 plug into it.
- **E1 (tiled Start)** and **E5 (Search)** share one search surface (type-at-Start hands off to `search.rs`); E1 depends on E2 for the Start button and on E5 for type-to-search.
- **E3 (Action Center)** supplies the notify daemon that E2 renders the badge for.
- **E4 (Multitasking)** is mostly self-contained on `wlr.rs` + labwc but feeds a Task View button into E2's panel.
- **E6 (Settings)** + **E7 (Personalization)** close the loop: E6 is the D4 Control-Panel replacement that E1's rail, E3's config screen, and E7's Personalization rail all target; E7's Colors page drives the accent that E0 introduced.
- **E19 (Power/Session)** is small and rides E1 (rail power/user tiles) + E0 theme.

**Why pillars-together, not strictly serial:** they form a tight cycle (panel↔Start↔Search↔Settings↔Action Center). The interim contracts (E1 rail → `control-panel` until E6; E2 search → keybind-only until E5; E3 config-read until E6) let them land in any order within M1 without stubbing — each degrades to an existing surface, never to a `todo!()`.

### M2 — devices, identity, networking, the OOBE, second config surfaces (E8–E15, excl. E5/E6/E7 already in M1)

M2 builds on M1's Settings shell and notify daemon. **E8 (Explorer) + E9 (Your Phone)** land together because they share the KDE Connect device surface (must coordinate, not duplicate). **E10 (Accounts/Lock)**, **E12 (Devices)**, **E13 (Windows Update)**, **E14 (Security)**, **E15 (Networking)** are all **Settings categories** — they require E6's shell + nav rail (M1) and E3's daemon (M1) before they can mount. **E11 (OOBE)** lands here because it *hands off to* the finished M1 desktop (panel + taskbar) and *feeds* E10's greeter the account it creates — so the desktop it lands on must exist first.

### M3 — polish, productivity extras, parity (E16–E18, E20)

The deferrable layer: **E16 (Clipboard/Snip)**, **E17 (Storage/Backup)**, **E18 (Edge→Firefox)** are productivity surfaces that consume M1/M2 (notify daemon, Settings pages) but nothing depends on them. **E20 (Polish/accuracy)** is **last by construction** — it captures the gallery, pins `Theme::Windows10` accents in `checklist.rs`, and runs the four-era keyboard-nav parity sweep, which can only be meaningful once every surface it captures (Action Center, Settings, Start, Task View, Search) exists. E20 ships a carbon-aliased placeholder for `win10()` only if E0 somehow slips, which it should not.

### Dependency-ordering note (what must land before what)

1. **E0** (`Theme::Windows10` + `win10()` + era plumbing) — before *everything*.
2. **E3 notify daemon** — before E2's badge, E9/E12/E13/E15/E16/E17 toasts.
3. **E2 panel** — before E1's Start button, E3's badge chrome, E4's Task View button.
4. **E6 Settings shell + nav rail** — before E7/E10/E12/E13/E14/E15/E17 pages (all are Settings categories).
5. **MDE-KDECnt-Rust + `mde connect`** — before E8/E9 live device browse (both fall back to the on-disk pairing store if it slips).
6. **M1 desktop (panel+Start+taskbar)** — before E11 OOBE (it hands off to that desktop).
7. **All capturable surfaces** — before E20's gallery/parity sweep.

### M1 — the Win10 era is usable

#### E0 Era foundation — Theme::Windows10 + win10() remap, era plumbing, per-era routing

- [✓] **E0.1** Add `Theme::Windows10` to `palette.rs` (`enum` + `theme()` match arm `3 => Windows10` + `is_windows10()`), with no call-site churn. (Acceptance: `cargo test -p mde-ui` green; a unit test sets `set_theme(Theme::Windows10)` and asserts `theme()` round-trips to `Windows10`.)
- [✓] **E0.2** Add `win10_accent() -> Rgb` (pinned `#0078d4` light / `#2899f5` dark) and `win10(rgb)` remap modeled on `carbon()` — accent family → Win10 accent, neutrals → cooler grays, sentinels passthrough — and the `Theme::Windows10 => win10(rgb)` arm in `color()`. (Acceptance: a checklist test asserts `color(HIGHLIGHT)` under Windows10 equals `win10_accent()`, `color(TITLE_TEXT)` stays light, `color(WINDOW_FRAME)` is a border gray not black; `cargo test` green.)
- [✓] **E0.3** Extend `font::family()` so `is_windows10()` returns the bundled Plex face; record `UI_FONT_TARGET_WIN10 = "Segoe UI"`. (Acceptance: `font::ui()` under `Theme::Windows10` resolves to the Plex family in a unit test; no new TTF asset/licence entry added.)
- [✓] **E0.4** Recognize `theme:"windows10"` in `state.rs` (no schema change; defaults untouched) and add the round-trip + fallback test. (Acceptance: `parse(r#"{"theme":"windows10"}"#).theme == "windows10"` and `parse("{}")` still yields Carbon defaults — `cargo test` green.)
- [✓] **E0.5** Add the `"windows10" => set_theme(Theme::Windows10)` startup arm in `main.rs` (before the Carbon fallback); `set_dark`/`set_accent` unchanged. (Acceptance: with `menu.json` `theme:"windows10"`, `timeout 3 ./target/debug/mde files` launches no-panic; the *visible* Win10 render is verified by the E0.7/E0.9 captures.)
- [✓] **E0.6** Add the `is_windows10()` anchor arm in `panel.rs` (bottom bar — reuse the Win2000 bottom branch at `metrics::TASKBAR_HEIGHT`=28, or add `WIN10_BAR_H`=40 if a taller bar is chosen; record the choice in the commit). (Acceptance: a `grim` capture of `mde panel` under `theme:"windows10"` shows a bottom-anchored bar; no-panic `timeout 3 ./target/debug/mde panel`.)
- [✓] **E0.7** Add "Windows 10" to the `display.rs` Appearance theme picker: extend the LOCAL `Theme` enum + `ALL` + `key()`/`from_key()`/`label()`, AND add the required `(Theme::Windows10, _)` arm to the exhaustive `match (state.theme, state.theme_mode)` labwc-themerc writer (Win10 dark header `#1f1f1f`/`#ffffff`; accent sourced from `win10_accent()` — these are labwc config strings, the same §2.1-exempt themerc precedent already in that fn). Selecting it writes `theme:"windows10"` via `state::save()`. (Acceptance: `cargo build` clean — the exhaustive match compiles; `./preview.sh display` shows "Windows 10" as a selectable theme; after selecting + relaunching `mde panel`, a `grim` capture shows the Win10-accent bar.)
- [✓] **E0.8** Add `windows10_remap_pins` to `checklist.rs` (§2.2): pin `win10_accent()`, sentinel passthrough, and a neutral, restoring the prior theme at test end. (Acceptance: `cargo test -p mde-ui` green with the new pinned assertions.)
- [✓] **E0.9** Gallery parity pass: capture `mde panel` + `mde menu` + `mde files` under all four eras. (Acceptance: `./preview.sh gallery` produces Win10 captures alongside Carbon/Win2000/BeOS in `tests/accuracy/captures/gallery/` with the distinct Win10 accent visible.)

#### E1 Tiled Start (Windows 10 era)

- [✓] **E1.1** Add `Theme::Windows10` arm to `start_win10.rs::run` and a `main.rs` dispatch arm + `%post` symlink for `mde start-win10`; assert it no-panics. (`timeout 3 ./target/debug/mde start-win10` under a nested-sway `WAYLAND_DISPLAY` exits cleanly; `mde start-win10 --list-tiles` prints with no panic.)
- [✓] **E1.2** Promote the flat-tile widget (`carbon_tile_raw`/`carbon_tile_style`), `launch_cmd`/`run_act`, and a **pid-file-name-parameterized** `acquire_singleton` out of `menu.rs` into a shared `start_common` helper used by both Carbon Start and Win10 Start; `cargo test` stays green and `./preview.sh menu` still renders the Carbon switcher unchanged. (grim of Carbon `mde menu` shows the tile grid pixel-identical to pre-refactor.)
- [✓] **E1.3** Render the three-region layout (left rail + center column + right tile grid) as a bottom-left layer-shell overlay in the Windows10 theme. (grim capture of `mde start-win10` shows the left icon rail, an alphabetical All-Apps list with A–Z headers, and the right tile grid; backdrop click and Esc both close it.)
- [✓] **E1.4** Left rail: account avatar (top), state-driven system folders, plus always-present Settings (gear) and Power; hover expands the rail to a labelled flyout. The Settings item dispatches `mde settings` (the Win10-era config route, D4) — never `mde control-panel`. (grim before-hover shows icons only; grim after a hover `Message` shows the widened rail with text labels; Power and account are present even with an empty config.) — **[Reconciled 2026-06-02: was a stale `[>]`; verified complete.]** `start_win10.rs::rail()` renders the avatar (top, `system-users`), state-driven folders (Documents/Pictures/Downloads → `mde files <dir>`), and always-present **Settings** (→ `Message::Mde("settings")` = `mde settings`, never control-panel) + **Power** at the bottom; `RailHover` widens it to the labelled flyout (`rail_expanded`). Shipped in commit `b286754`.
- [✓] **E1.5** All-Apps list built from `apps::programs()` flattened/deduped/case-insensitively sorted with #/A–Z group headers; clicking an app launches it. (grim shows headers before each first-letter change; `mde start-win10` then clicking Firefox spawns Firefox — verified by a no-panic launch + a running-process check.)
- [✓] **E1.6** Recently Added + Suggested sections from real data (newest `.desktop` mtimes; most-launched pins via a `launch_count` field on `PinnedItem`, `#[serde(default)]`). (grim shows a populated Recently Added section after installing a new `.desktop`; no `demo_data`/placeholder strings — §3.)
- [✓] **E1.7** Add `start_tiles: Vec<StartTile{name,command,icon,size,group}>` to `state.rs` (`#[serde(default)]`, atomic `save`, `parse("{}")`==`Default` test holds, §2.6); seed from `state.pinned` on first run. (`cargo test` green incl. the default-agreement test; `mde start-win10 --list-tiles` on a fresh config lists the seeded pins.)
- [✓] **E1.8** Right tile grid: render `start_tiles` in named groups with Small/Medium/Wide/Large spans via `metrics::TILE_*_PX` constants and `palette::accent()` hover. (grim shows tiles at distinct sizes grouped under a header band; no raw hex in `start_win10.rs` — `grep -n '#[0-9a-fA-F]' rust/mde/src/start_win10.rs` is empty, §2.1.)
- [✓] **E1.9** Right-click tile/app context menu — Pin / Unpin / Resize ▸ / Open file location / Uninstall — reusing the `menu.rs` context-menu plumbing. (grim of a right-click shows the menu; choosing Resize ▸ Wide then reopening Start shows the tile widened.)
- [✓] **E1.10** Headless CLI `mde start-win10 --pin <name> <cmd> | --unpin <name> | --resize <name> <size> | --list-tiles` writing through atomic `state::save`. (bench round-trip: `--pin Foo firefox` then `--list-tiles` shows Foo; `--resize Foo wide` then `--list-tiles` shows size=wide; `--unpin Foo` then `--list-tiles` omits it.)
- [✓] **E1.11** Win-key routing: in the Windows10 era the panel Start button and the `rc.xml` `W-` bind dispatch `mde start-win10` (Win2000/Carbon keep `mde menu`); the parameterized `acquire_singleton("mde-start-win10")` guard prevents stacked overlays. (with theme=windows10, pressing Super opens the tiled Start in a live session; a second Super press does not stack a second overlay — `mde-start-win10.pid` liveness check holds.)
- [✓] **E1.12** (dependency note) Settings rail/route depends on E4 providing the Win10-era `mde settings` entry point that replaces Control Panel (D4). Until E4's Settings surface lands, `mde settings` resolves to E4's stub entry, never `mde control-panel`. (verified: `grep -n 'control-panel\|control_panel' rust/mde/src/start_win10.rs` is empty — the Win10 era never dispatches the legacy config surface.) — **satisfied**: the Win10-era `mde settings` entry point exists (E6) and `start_win10`'s rail Settings item dispatches `Message::Mde("settings")`. `grep -n 'control-panel\|control_panel' start_win10.rs` matches only the doc comment stating it routes to settings, never an actual control-panel dispatch.

#### E2 Win10 taskbar — search box, Task View, jump lists, thumbnails, badges, Action Center, tray, clock

- [✓] **E2.1** Add `Theme::Windows10` + `is_win10()` + `fn win10(rgb)` remap (Win10 dark flat palette) and a `fn win10_accent()` that READS the existing `ACCENT` atomic (extend the one accent system — do not reuse `carbon_accent()`'s fixed-blue body, do not add a parallel atomic) plus the `Theme::Windows10 => win10(rgb)` arm in `palette::color()`; add `metrics::WIN10_BAR_H=40` and pin it in `checklist.rs` (cargo test -p mde-ui green; a unit test asserts `win10(SHELL_HEADER)` returns the dark bar gray and `win10_accent()` changes with `set_accent`, no raw hex outside palette.rs). — **delivered** (E0 `Theme::Windows10` + `win10()` + `metrics::WIN10_BAR_H=40` pinned; E7.1's `win10_accent` is a *dedicated settable slot*, not the icon `ACCENT` atom, per the in-session 'keep fixed stock blue / don't fold the ACCENT atom in' decision — a deliberate deviation from this task's wording).
- [✓] **E2.2** `view_win10()` bar in `panel.rs` (bottom anchor, `WIN10_BAR_H`): square Start tile, app buttons icon-only with accent underline on the focused window, spacer, tray glyphs, two-line clock; routed via `is_win10()` in `view()`/`launch()` (grim capture of the bottom strip shows a flat dark Win10 bar with running-app icons + accent underline on the focused one; `timeout 3 mde panel` no-panic with theme=windows10).
- [✓] **E2.3** Win10 window-button + Task View button wiring reusing `wlr::Wm` focus/minimize/restore rules; left-click focuses/minimizes, hover area present (grim capture shows focused-window highlight changing after `wlr.focus`; bench: clicking a button focuses the matching toplevel). — Window buttons already focus/minimize via `wlr::Wm` (`win10_task_button` → `TaskButton`/`MinimizeToggle` → `wm.focus`/`set_minimized`); this adds the **Task View button** (overlapping-windows Nerd glyph) next to Start → `Message::TaskView` → `mde task-view`. Fixed a latent `center_x(Length::Fill)` bug on `win10_start_tile` (it made the Start tile Fill-width, spreading the left buttons across thirds) — both are now 40px `center_x(Fixed)` tiles. Verified: a panel capture shows the Start logo at the corner with the Task View button adjacent.
- [✓] **E2.4** `mde search` flyout (`main.rs` arm + symlink + `rc.xml` WINKEY bind): text input + Apps/Documents/Settings/Web tabs; Apps from the `.desktop` scan, Web opens Firefox; panel owns+toggles the child like `menu` (`timeout 3 mde search` no-panic + grim capture of the flyout with tabs; typing a query filters the Apps list). — **delivered by E5** (`mde search` flyout with All/Apps/Documents/Web/Settings tabs, `.desktop` scan, Web→Firefox, W-s bind, `mde-search` symlink). Verified: the `"search"` dispatch arm + symlink exist.
- [✓] **E2.5** `mde taskview` full-screen overlay (`WINKEY+TAB`): window grid from `wlr::Wm.windows()` (icon+title, click=focus, close=wlr close) + a virtual-desktop strip backed by a NEW `ext-workspace-v1` binding added to `wlr.rs` (`workspaces()`/`activate`/`create`; reuses only the existing bg-thread/event-loop scaffolding, since wlr-foreign-toplevel carries no workspaces) (`timeout 3 mde taskview` no-panic + grim capture showing one tile per open window and a desktop strip; clicking a tile focuses that window; switching the strip activates a different ext-workspace). — **delivered by E4.3/E4.4** (`mde task-view` full-screen overlay: per-window tiles click→focus, + an ext-workspace-v1 desktop band — built as a dedicated `workspace.rs` rather than folded into `wlr.rs`, and named `task-view`).
- [✓] **E2.6** `mde jumplist <app_id>` popup on right-click of a Win10 app button (`Message::JumpList`): pinned tasks + recent items (folders for files, New/Private window for Firefox, Open/Unpin generic) (`timeout 3 mde jumplist firefox` no-panic + grim capture; right-clicking the Firefox button opens the list). — `mde jumplist <app_id>` popup (reusing popup.rs; `Item.label` widened to `String` for dynamic rows): app-specific **Tasks** (Firefox → New Window / New Private Window), a **Recent files** section parsed from `~/.local/share/recently-used.xbel` (percent-decoded, newest-first, existing-only, capped 6, each → `xdg-open`), then **Open**. The Win10 task button's right-click now opens it (`JumpList(app_id)`; left-click still focuses/minimizes). Verified: `mde jumplist firefox` shows the New/Private-window tasks + 6 real recent files + Open firefox. (Recents are the system list, not per-app-filtered — per-app history isn't available.)
- [✓] **E2.7** Action Center button + unread badge in the Win10 bar (far right) wired to `Message::ActionCenter`; badge count read from the E7 daemon on the 1-s Tick, chip drawn when >0, cleared on open (grim capture shows the AC button with a "3" badge after 3 toasts, badge gone after opening; depends on E7). — Action Center button (speech-bubble Nerd glyph) added to the far right of the Win10 panel, with an accent unread-count chip. `notifyd::unread_count()` (notifs newer than `last_read`) is read on the 1-s Tick (Win10 only); the chip draws when >0 and clears after opening (`mde action-center` stamps `last_read`). Verified: a panel capture with 3 seeded unread notifications shows the bubble + a blue "3" badge; click spawns the AC pane.
- [✓] **E2.8** `mde action-center` right-edge pane: notification history (top, from E7 store) + quick-action tile grid (Wi-Fi/Bluetooth/Night light/Brightness/Focus assist/All settings), Brightness reusing the logind path; `WINKEY+A` toggle (`timeout 3 mde action-center` no-panic + grim capture of the right pane with history + tile grid; a quick-action tile toggles its backend over D-Bus/CLI). — **delivered by E3** (`mde action-center` right-edge pane: grouped notification history + quick-action tile grid incl. brightness via logind, W-a toggle).
- [✓] **E2.9** `state.rs` Win10 taskbar config: `#[serde(default)]` `win10_taskbar_top`, `win10_autohide`, `win10_search_mode`, `win10_show_taskview` with default fns; `launch()` reads position (top/bottom) + auto-hide (exclusive-zone toggle + edge-reveal `mouse_area`) (the `Default`-vs-`parse("{}")` test stays green; grim capture of the bar anchored at top after flipping `win10_taskbar_top`, and the bar hidden until edge-hover with auto-hide on). — `state.win10_show_taskview` (def on) + `win10_search_mode` (def "button") added (serde defaults, roundtrip-pinned), **consumed by panel.rs**: the Task View button is hideable, and a search affordance (magnifier `button` / labelled `box` pill / `hidden`) sits after Start, opening `mde search`. `win10_taskbar_top` is **superseded by `taskbar_location`** (E7.9, top/bottom) — not re-added. Verified by captures: default = Start+magnifier+Task-View; `box`+`show_taskview:false` = Start+"Search" pill, no Task View. **Auto-hide (hidden-until-edge-hover) split to [[E2.9a]]** — feasible via iced_layershell `SizeChange` but a substantial reveal-strip mechanism; the settings UI for these toggles is [[E7.9a]].

#### E3 Action Center + notifications (freedesktop notify daemon, toasts, history, quick toggles)

- [✓] **E3.1** Add `mde/src/notifyd.rs`: a zbus daemon owning `org.freedesktop.Notifications` with `Notify`/`CloseNotification`/`GetCapabilities`/`GetServerInformation` + `NotificationClosed`/`ActionInvoked` signals, backed by an `Arc<Mutex<Vec<Notif>>>` store; name-claim is best-effort/non-fatal like `tray.rs`. (Bench: with `mde panel` running under a forced Win10 era, `notify-send "hi" "body"` returns 0 and `busctl --user introspect org.freedesktop.Notifications /org/freedesktop/Notifications` lists the interface; if another daemon owns the name, `mde panel` still launches no-panic.)
- [✓] **E3.2** Mirror the store to `~/.config/mde/notifications.json` (atomic write, `#[serde(default)]`, §2.6) with a top-level `last_read: SystemTime` marker, and load it on start; clear-on-`CloseNotification`. (Bench: `notify-send X` then read `notifications.json` shows the record with id/summary/body; `gdbus call … CloseNotification <id>` removes it from the file.)
- [✓] **E3.3** Wire `notifyd::start()` into `panel.rs` only under `Theme::Windows10` (alongside `tray::start()`); compute the unread badge count from the store (non-`transient` records newer than the `last_read` marker). (Bench: `timeout 3 ./target/debug/mde panel` no-panic with theme forced to win10; with theme=carbon, no `org.freedesktop.Notifications` owner appears — `busctl --user list | grep Notifications` empty.)
- [✓] **E3.4** Add `mde/src/action_center.rs` with `run_center` (right-anchored full-height layer-shell pane, `popup.rs` scaffolding) + `main.rs` dispatch arm + `pub mod`. Render the history region (grouped-by-app cards: icon/summary/body/timestamp, per-card and per-group Clear "x", expand caret, inline action buttons from the `actions` array) from `notifications.json`, with a "No new notifications" empty state. (Screenshot: `mde action-center` after two `notify-send` calls -> grim capture of the right pane shows both cards grouped under the sender with Clear x's; with none, shows the empty state.)
- [✓] **E3.5** Add the quick-action tile grid to the pane: collapsed shows first four pinned tiles + an Expand link, expanded shows the full grid + Collapse, order/pins from new `state.rs` `quick_actions`. (Screenshot: grim of the pane shows four square tiles collapsed; clicking Expand -> grim shows the full grid with a Collapse link.)
- [✓] **E3.6** Implement tile actions against named backends — Wi-Fi (`nmcli radio wifi`), Bluetooth/Airplane (`rfkill`), Brightness (`brightnessctl`), Volume (`wpctl`), Night light (`wlsunset`), All-settings (`mde settings`), Nearby-sharing (`mde phone`) — reading current state on view-build and toggling on click. (Bench: clicking the Wi-Fi tile flips `nmcli -t radio wifi` between enabled/disabled; the tile's accent-on/off fill matches the read state on relaunch.) — **[Reconciled 2026-06-02: was a stale `[>]`.]** The six **toggle** tiles read their backend state on view-build (`read_tile`) and flip on click (`toggle_tile`): wifi (`nmcli -t radio wifi`), bluetooth/airplane (`rfkill`), mute/volume (`wpctl`), nightlight (`pgrep`/`wlsunset`), focus (persisted `focus_assist`). The **bench passes** — the Wi-Fi tile reads `nmcli radio wifi` and its accent fill matches the read state on relaunch. **Split to [[E3.6a]]:** Brightness (a `brightnessctl` *slider*, not a toggle), an All-settings launch tile (→ `mde settings`), and Nearby-sharing (→ `mde phone`, **blocked on E9**) — these are slider/launch affordances of a different kind than the toggle backends shipped here.
- [✓] **E3.6a** Action-center quick-action extras beyond the toggle tiles (split from E3.6): a Brightness **slider** (`brightnessctl get`/`set`), an "All settings" launch tile (→ `mde settings`), and a Nearby-sharing tile (→ `mde phone`, **blocked on E9**). The slider needs a new non-toggle tile type, and the launch tiles need the grid to support on-click-launch (vs read/toggle) — distinct from E3.6's toggle backends. — **Done (brightness slider + All-settings launch).** `read_brightness()` parses `brightnessctl -m` (CSV field 4, the percent; absent/headless → `None`, slider hidden); a themed `slider(0..=100, …, Message::Brightness)` renders below the tile grid with a sun glyph and sets live (`brightnessctl set N%`) while dragging, accent-filled rail/handle. `Message::AllSettings` + `launch_mde("settings")` back a full-width **"All settings"** foot link (gear glyph) that opens `mde settings` and closes the center. **Design choice (recorded):** All-settings is the **Win10-faithful full-width foot link**, not a 7th square grid tile — the default 6 toggles + 4-up collapse would bury a square tile under Expand, and Win10 itself renders All-settings as the bottom link. **Verified by gallery capture:** the slider (handle at 100%, matching `brightnessctl`) + the All-settings link both render under the tile grid; build/clippy/fmt clean, 52+ tests green. **Nearby-sharing tile → [[E3.6b]] (blocked on E9).**
- [✓] **E3.6c** Debounce the action-center brightness slider (sixth-sweep audit NOTE): dragging fires `Message::Brightness(v)` per tick, each spawning `brightnessctl set v%` via `run()` which `let _`-drops the `Child` (no reap). Coalesce rapid drags (only set on settle / throttle) and reap or detach the spawned process so a long drag doesn't fork dozens of zombies. (Acceptance: dragging the slider end-to-end issues a bounded number of `brightnessctl` calls, no accumulating defunct children.) — **Done (set-on-settle, the option the task sanctions).** `Message::Brightness(v)` now only updates `state.brightness` (the handle still tracks the cursor live); the slider's new `.on_release(Message::BrightnessSet)` applies the level **once** via a new `set_brightness()` that runs `brightnessctl set N%` directly (no shell) and `.status()`-waits, so the child is reaped. Net: exactly **one** `brightnessctl` call per drag (trivially bounded) and **zero** accumulating defunct children — replacing the per-tick `run()` spawn that `let _`-dropped its `Child`. Minor faithful trade (recorded in the commit): the backlight applies on release rather than mid-drag — the cursor-tracking handle keeps the live feel. Build/clippy/fmt clean, 57 mde tests green, no-panic launch.
- [✓] **§2.3-glyphs** Name the scattered Nerd-glyph/badge `.size()` literals the sixth sweep flagged (§2.3): `action_center.rs:600` (tile glyph `20.0`), `panel.rs` win10 start-tile (`18.0`), search affordance (`15.0`), AC button (`16.0`), unread badge (`9.0`). Mint named `metrics::` constants (e.g. `TILE_GLYPH_PX`, `BADGE_PX`) so these are a single source like `UI_PX`, not scattered literals. (Low — the enforced §2.3 pin covers `UI_PX`/`INFO_TITLE_PX`; these are glyph sizes, but still literals. Acceptance: no bare `.size(<float>)` on glyph/badge text outside a named metric; `cargo test -p mde-ui` green.) — **Done.** Minted five `metrics::` glyph/badge constants — `PANEL_GLYPH_PX` (15, the ≡ switcher / tray glyphs / search magnifier), `BUTTON_GLYPH_PX` (16, Win10 Task-View + Action-Center bar buttons and the AC sun/gear inline glyphs), `START_GLYPH_PX` (18), `TILE_GLYPH_PX` (20, the AC quick-action square), `BADGE_PX` (9, the unread chip) — and routed **all 11** glyph/badge sites in `panel.rs` (8) + `action_center.rs` (3) through them. `rg '\.size\([0-9]'` over both files is now empty. The five are pinned in `checklist.rs::ui_size_is_one_source_of_truth` so a drift fails CI like `UI_PX`. Pure rename — the constant values equal the prior literals exactly, so the render is byte-identical by construction (no capture needed). `cargo test -p mde-ui` green (pin test incl.), build/clippy/fmt clean.
- [!] **E3.6b** Action-center **Nearby-sharing** tile (→ `mde phone`), split from E3.6a — **blocked on E9** (`mde phone` doesn't exist until the Your Phone surface lands). It reuses the same launch mechanism as the E3.6a "All settings" link (`launch_mde` + close); add it as a second foot link (or a launch tile) once `mde phone` is a real subcommand. Until then a tile that launches nothing would be a §3 mockup, so it stays out.
- [✓] **E3.7** Add `focus_assist: bool` to `state.rs` and a Focus-assist tile; while on, `notifyd` collects history but does not spawn toasts. (Bench: enable Focus assist, `notify-send X` adds to `notifications.json` but `pgrep -af 'mde toast'` shows nothing; disable, `notify-send Y` spawns a `mde toast` process.)
- [✓] **E3.8** Add `mde toast <id>` in `action_center.rs`: a bottom-right, non-focus-stealing (`KeyboardInteractivity::None`) layer-shell surface reading the record by id, auto-dismissing on an `iced::time` `expire_timeout` timer or click, invoking the default action on body-click; `notifyd` spawns it on `Notify`. Stack concurrent toasts upward. (Screenshot: `notify-send "Title" "Body"` under Win10 -> grim capture of the bottom-right corner shows the toast with icon/title/body/Clear x; it disappears within ~6s; two rapid sends -> grim shows two stacked toasts.)
- [✓] **E3.9** Implement "Clear all" + badge-clear-on-open: opening the center stamps `last_read = now` into `notifications.json` (badge -> 0 on the panel's next tick); "Clear all" closes every id. (Bench: with two notifications, the panel badge count is 2; launching `mde action-center` writes a fresh `last_read` to `notifications.json` newer than both records, so the panel badge reads 0; clicking "Clear all" empties the records in `notifications.json`.)
- [✓] **E3.10** Add the labwc `rc.xml` `Super+A` -> `mde action-center` keybind to the Win10 keybind set + a `%post` symlink note, and confirm the KDE Connect bridge (`MDE-KDECnt-Rust`) targets the standard `org.freedesktop.Notifications`. (Bench: a `notify-send` issued through the KDE Connect path lands a card in `mde action-center`; `Super+A` toggles the pane under labwc.)

#### E4 Multitasking — Task View, Virtual Desktops, Snap (Win10 era)

- [✓] **E4.1** Add `Theme::Windows10` to `palette.rs` `Theme` + a `win10(rgb)` remap (light/dark + accent) wired through `color()`'s `match theme()`, and make `main.rs` select it for the `windows10` theme string — **delivered by E0** (`Theme::Windows10`, `win10()`, the `windows10_remap_pins` checklist test, `main.rs` `"windows10" => set_theme(Theme::Windows10)`). (acceptance: `cargo test -p mde-ui` green with a new checklist assertion pinning two `win10()` token outputs; `grim` of any existing surface launched under the win10 theme shows the shifted accent, not Carbon blue).
- [✓] **E4.2** Add the era-neutral Snap keybinds to both `assets/labwc/rc.xml` and `skel/.config/labwc/rc.xml`: `W-Left`/`W-Right` → `SnapToEdge` left/right, `W-Down` → `Iconify`, and drag-to-edge `<snapping>` enabled — **preserving** the existing `W-Up` → `ToggleMaximize`, `W-m` → `Iconify`, and the `<mouse><default/>` block. Both files validate as well-formed XML; `<snapping range=12 topMaximize=yes>` + the three binds added under the existing window-management group. (acceptance: `labwc --reconfigure` against the file logs no parse error; in the nested sway/labwc preview a `W-Right` on a `foot` window tiles it to the right half — visible in a `grim` capture; the snap is performed by labwc, no mde process runs).
- [✓] **E4.3** Create `mde/src/task_view.rs` — full-screen layer-shell overlay (popup.rs pattern) dispatched via a new `task-view` arm in `main.rs` + a `mde-task-view` symlink added to the `%post` symlink loop in `mde/Cargo.toml` + a `W-Tab` rc.xml bind; lay out the `wlr::Wm` window snapshot as a centered icon+title tile grid (tile size via a new `metrics::TASKVIEW_TILE`), click→`wm.focus(id)`+`exit(0)`, Esc/click-out closes. Verified: `mde task-view` exits 0 with no compositor; nested-sway capture with two `foot` windows shows two labelled tiles over a dimmed scrim. (acceptance: `timeout 3 ./target/debug/mde task-view` exits 0 with no panic when no compositor; in preview with two `foot` windows open, `grim` shows two labelled tiles and clicking one focuses that window).
- [✓] **E4.4** Add `mde/src/workspace.rs` — an `ext-workspace-v1` client on a background thread (clone of the `wlr.rs` scaffold) exposing `start() -> Option<Workspaces>`, a snapshot of `(name, active)` chips, and `activate/create/remove` (driving the ext-workspace manager's own requests only — never window geometry); render the desktop band along the top of `task_view.rs` with a left "+ New desktop" chip and a hover ×; add a `__ws-list` debug arm in `main.rs` modeled on `__wlr-list`. Verified under **labwc** headless (3 named desktops): `mde __ws-list` prints `can_create=false` + `[1] Desktop 1 active=true removable=false / …`; switching is live (`mde __ws-activate 3` moves the active flag to Desktop 3, persisting after the process exits). **Capability-gated (rescue):** labwc's `<desktops>` are static — it advertises neither `create_workspace` (group) nor `remove` (workspace), so the "+ New desktop" chip and the per-chip remove × are **hidden** under labwc (no dead controls, §3); they appear only on a compositor that advertises the capability. `grim` confirms the band shows three switch-only chips with Desktop 1 accent-filled. `__ws-activate` is an internal diagnostic sibling to `__ws-list`. (acceptance: `mde __ws-list` prints the live workspaces against the running compositor; `grim` of Task View shows the desktop band with the active chip highlighted).
- [✓] **E4.5** Implement the ext-workspace fallback ladder in `task_view.rs`/`workspace.rs`: if the global is absent, read `state.rs::virtual_desktops` (new `#[serde(default = "def_virtual_desktops")]` field, default 4) and show a fixed strip wired to `W-C-Left/Right`; if no labwc desktops either, omit the band and show the single-desktop grid. The `band()` selector picks live → fixed → none. The fixed chips are read-only (no fake active state, no dead click) with a "Ctrl+Win+Left / Right to switch" hint pointing at the E4.6 binds. `start()` gains an `MDE_NO_EXT_WORKSPACE` env guard; unit tests force `None` and build `view()` for the fixed-strip, single-desktop, and window-grid paths. Verified under sway (no ext-workspace): a Desktop 1–4 strip renders; `timeout 3 mde task-view` exits 0; `rg` finds no stub. (acceptance: forcing `start()` to return `None` — via an env guard in a unit test — the overlay still builds a valid `Element` and `timeout 3 ./target/debug/mde task-view` exits 0; no `todo!()`/stub arm anywhere in the module per a `grep` check).
- [✓] **E4.6** Add the virtual-desktop keyboard binds to both rc.xml files: `W-C-Left`/`W-C-Right` → `GoToDesktop left/right wrap=yes`, with a `<desktops number="4">` block naming Desktop 1–4 (matching the E4.5 fallback default). **W-C-d (create) / W-C-F4 (close) are intentionally omitted:** labwc exposes only `GoToDesktop`/`SendToDesktop` — it has no create/close-desktop action and its `<desktops>` are static (verified: client `create_workspace`/`remove` are ignored, `can_create=false`), so those binds would be dead. Create/remove live in Task View's capability-gated chips, which only appear on compositors that advertise the capability (§3, §6 best-choice — recorded in the rc.xml comment). Verified under labwc headless against the real skel rc.xml: it parses clean (the E4.2 `<snapping><range>` deprecation is fixed to `<range><inner>/<outer>>` in the same pass), `__ws-list` shows the 4 named desktops, and `__ws-activate` moves the active flag. (acceptance: `labwc --reconfigure` parses clean; in preview `W-C-Right` switches workspaces — confirmed by `mde __ws-list` reporting a different active workspace before/after).
- [✓] **E4.7** Add the Snap-Assist picker mode to `task_view.rs` — `mde task-view --snap-assist <SIDE>` renders the other windows clipped to the empty half-screen `container` anchored to that side; picking a tile calls `wm.focus(id)` (focus only — **mde performs no snap**), Esc/empty-click dismisses with `exit(0)` and no second pick. The mode self-gates to the Windows 10 theme (so the single rc.xml can fire it era-safely, E4.8); the candidate list is the snapshot minus the focused (just-snapped) window — best-effort, since a layer surface grabbing keyboard can clear the activated flag. Verified: `timeout 3 mde task-view --snap-assist right` exits 0 with no compositor; a nested-sway capture under the win10 theme shows the right-half picker of `foot` tiles; `rg` finds no window-geometry/snap call (only `wm.windows()`/`wm.focus()`); a unit test builds the picker Element for both sides, with and without candidates. (acceptance: `timeout 3 ./target/debug/mde task-view --snap-assist right` exits 0 no-panic with no compositor; under the win10 theme in preview, after a half-snap a half-screen picker of the remaining windows appears in a `grim` capture; the module contains no geometry/snap call to `wlr.rs`, verified by `grep`).
- [✓] **E4.8** Wire the Win10-only Snap-Assist trigger: both rc.xml `W-Left`/`W-Right` binds now chain `SnapToEdge <side>` (the real snap, labwc's) + `Execute mde task-view --snap-assist <opposite>` (the picker). **Design deviation (§6 best-choice, recorded in the rc.xml comment):** there is no separate "win10 rc.xml variant" — that infrastructure doesn't exist, so instead the picker **self-gates to the Windows 10 theme** in `task_view::run`, giving the same era-conditional behavior from one config (Carbon/Win2000 → the `Execute` is a 6 ms no-op, so the binds stay plain edge-snaps). The spec's "trailing `SnapToEdge` after the pick" (auto-tiling the picked window) is **not achievable**: labwc's `Execute` is fire-and-forget and labwc exposes no client snap IPC, so mde can't snap an async pick — the picker focuses the pick instead (E4.7's "mde performs no snap"). No `mde snap` geometry arm exists. Verified: labwc parses the chained binds clean; under carbon `mde task-view --snap-assist right` exits 0 in ~6 ms (gated), under windows10 it shows the half-screen picker (E4.7 capture). (acceptance: diff of the two skel rc.xml era variants shows the win10 one chaining `SnapToEdge` + `mde task-view --snap-assist`; a `grim` capture under win10 confirms Snap Assist fires after `W-Left`, and under Carbon it does not; no `mde snap` geometry arm exists).

#### E5 Search + Quick Access (Win10 era)

- [✓] **E5.1** Add `Theme::Windows10` + `fn win10(rgb)` remap (flat Carbon variant, Win10 blue accent, light/dark off `palette::dark()`; packed `THEME` atomic value 3) and a `"windows10"` arm in `main.rs` theme dispatch — **delivered by E0** (same as E4.1: `Theme::Windows10`, `win10()`, `windows10_remap_pins` checklist test, `main.rs` `"windows10" => set_theme(...)`). (Acceptance: `cargo test -p mde-ui` green with a new `checklist.rs` assertion pinning the Win10 accent + a sentinel; `MDE_THEME=windows10 mde popup quickaccess` grim capture shows the Win10 palette, not Carbon.)
- [✓] **E5.2** Create `mde/src/search.rs` as a layer-shell overlay cloned from `menu.rs` (transparent, all-edge anchor, Exclusive, auto-focused `text_input` via `text_input::focus`, bottom-left flyout) with a `main.rs` `"search"` dispatch arm and a Win10 era-guard that spawns `mde menu` and exits otherwise. Also accepts an optional initial query (`mde search firefox`) — useful as a launcher target and to script captures. Verified: `timeout 3 mde search` (carbon) exits 0 via the era guard; under windows10 a grim capture shows the bottom-left flyout with a focused empty query field. (Acceptance: `timeout 3 ./target/debug/mde search` launches with no panic; under Win10 a grim capture shows the bottom-left flyout with an empty query field.)
- [✓] **E5.3** Add the filter-tab row (All/Apps/Documents/Web/Settings) using `mde_ui::tab_strip`, the selected tab accented, switching the active result filter (`Filter` enum, `from_index`/`index` round-trip unit-tested). Verified: captures of the empty (All) state and a "fire" query show the All tab accented and the result list changing. (Acceptance: grim captures of two states — All selected vs. Apps selected — show the highlight moving and the list contents changing.)
- [✓] **E5.4** Wire the **Apps** backend via `apps::programs()`: flatten the category groups into one `Vec<App>`, filter by `App.name` substring, render rows with `icons::icon_any` (icon guessed from the exec basename), activate by running `App.exec` honoring `App.terminal`. Verified: a windows10 capture of `mde search fire` shows **Firefox** + **Firewall** rows with their icons. (Acceptance: `mde search` typed "fire" … shows a Firefox row in a grim capture; pressing Enter launches it.)
- [✓] **E5.5** Wire the **Documents** backend: tick-debounced `fd` (falls back to `find`) under `$HOME`, args passed directly (no shell — query can't inject), result-capped; activation opens the parent via `mde files <dir>`. Verified: with `$HOME/searchtest.txt` present, a windows10 capture of `mde search searchtest` shows the **searchtest.txt** hit with a text-file icon. (Acceptance: create `$HOME/searchtest.txt`, `mde search` for "searchtest" shows the hit; Enter opens `mde files` on its folder.)
- [✓] **E5.6** Wire the **Settings** backend (static Win10 Settings-page list → `mde system-properties`/`display`/`control-panel`, the D4-era fallback since the modern Settings app is E6) and **Web** row (`firefox "https://duckduckgo.com/?q=<urlencoded>"`, query percent-encoded by a local `url_encode`). Verified: `settings_hits` unit-tested (substring + full-list); the "fire" capture shows the **Search the web for "fire"** row. (Acceptance: grim capture of the Settings tab shows mapped pages; a free-text query shows a "Search the web for 'X'" row whose activation spawns Firefox.)
- [✓] **E5.7** Add the **Quick Access** menu: `popup.rs::items_for("quickaccess")` with System / Device Manager / Disk Management / Power Options / Event Viewer / Network Connections / Task Manager / Terminal / Terminal (Admin) / Run, separator-grouped, each mapped to its concrete backend command (System/Device Manager reuse `mde system-properties --info/--devices`; Disk Management/Network/Event Viewer/Task Manager/Terminal shell out with best-effort `||` fallbacks; Run → `mde run`, Power Options → `mde shutdown`). Also added the missing no-compositor guard to `popup::run` (matches `task_view`), so the headless smoke test no longer panics. Verified: `timeout 3 ./target/debug/mde popup quickaccess` exits 0; a nested-sway `grim` shows the flat single-column power-user menu bottom-left. (Acceptance: `timeout 3 ./target/debug/mde popup quickaccess` no-panic; grim capture shows the flat single-column power-user menu bottom-left; selecting "Run" spawns `mde run` and "Network Connections" opens `foot` running `nmcli` — bench-observed via `pgrep foot`.)
- [✓] **E5.8** Add reachability wiring: both rc.xml files gain `W-s`→`mde search` and `W-x`→`mde popup quickaccess` (alongside the existing `mde` launcher binds); the `main.rs` `"search"` arm landed with E5.2; and `search` is added to the RPM `%post`/`%postuninstall` symlink loops in `mde/Cargo.toml`. Verified: both rc.xml stay well-formed and labwc parses the skel config clean; `mde-search` basename dispatch launches the search surface (`/tmp/mde-search` → exit 0, era-gated). Win+S / Win+X are valid labwc Execute binds running already-verified commands (no key-injection tool to synthesise the press, same as E4.6/E4.8). (Acceptance: `labwc --reconfigure` then Win+S opens search and Win+X opens Quick Access; `mde-search` symlink basename dispatch verified.)

#### E6 Modern Settings app (Win10 era) — flat category nav replacing Control Panel

- [✓] **E6.1** Add `Theme::Windows10` to `palette::Theme` + a `win10(rgb)` remap modeled on `carbon()`, routed from `palette::color()`; `cargo test -p mde-ui` green (checklist sentinels intact, new theme passes through white/black/accent like carbon). (bench: `cargo test -p mde-ui` passes with a new `win10_remap` assertion)
- [✓] **E6.2** Build `mde/src/settings.rs` as an iced xdg-toplevel home category grid (System/Devices/Phone/Network/Personalization/Apps/Accounts/Time & Language/Ease of Access/Privacy/Update & Security tiles — no Gaming, no Search; each 32px `icon_any` glyph + title + caption via `infoband`), wired into `main.rs` `match cmd` as `"settings"`. Tiles must navigate (lands together with E6.3 so no non-interactive grid ships). (bench: `timeout 3 ./target/debug/mde settings` launches no-panic; grim capture shows the category tile grid; clicking a tile drills in) — Verified: `mde settings` (windows10, nested sway) shows the 11-category tile grid (icon+title+caption, no Gaming/Search); tiles drill in. `cargo test` pins the count + no-Gaming/Search.
- [✓] **E6.3** Implement category drill-in: `View { Home, Category, Page }` nav state, left page-rail + right content pane, top-left back-arrow returning to Home. Deferred (unbacked) pages render as greyed/"later milestone" rail entries, never as working pages (§3). (screenshot: grim of `mde settings` after clicking "Personalization" shows the Background/Colors/Themes rail with Lock screen/Start/Taskbar greyed, plus a content pane) — Verified: `mde settings personalization` shows the back-arrow header + a Colors/Background/Lock screen/Themes/Start/Taskbar rail (the latter four greyed as deferred) + a content pane. An optional `mde settings <category>` deep-link opens a category directly.
- [✓] **E6.4** Personalization ▸ Colors page with native Light/Dark radios (call `palette::set_dark`, persist to `state.rs`) + a 6-swatch accent grid (call `palette::set_accent`, persist), agreeing with `parse("{}")` default test (§2.6). (bench: toggle Dark, relaunch `mde settings`, capture shows it re-skinned dark; `cargo test` state round-trip green) — Colors page: native Light/Dark buttons + a 4-swatch accent grid (the real palette accents blue/orange/red/neutral, via `palette::icon_accent`; the planned '6' doesn't exist in the palette). Both call `palette::set_dark`/`set_accent` (live re-skin) and persist `theme_mode`/`icon_color` to state; `accent_key`/`accent_index` round-trip unit-tested, state roundtrip already pinned.
- [✓] **E6.5** Wire the M1 live page rows to existing backends only: System▸Display→`mde display` (Spawn), System▸About→`mde system-properties` (Spawn), Devices▸Printers→`system-config-printer`, Network→`nm-connection-editor`, Time & Language▸Date→`timedatectl`, Accounts▸Users→`seahorse`, Update & Security▸Firewall→`firewall-config`, Personalization▸Background→`mde display`, all reusing existing `fedora::TOOLS` rows + `fedora::launch`/`install`/`is_installed` (missing tool shows "(install)" + installs via pkexec dnf, like control_panel). No new backend logic; no page routed to a non-existent tool. (bench: launch `mde settings`, click System▸Display, observe `mde display` child window opens; `mde settings --list` headless prints page→tool map for every live page) — Live pages reuse existing backends only: System▸Display/About → `mde display`/`system-properties`; Devices▸Printers, Network, Accounts▸Users, Update▸Security → `fedora::TOOLS` (install-if-missing via the control_panel pattern); Time▸Date → `timedatectl`. `mde settings --list` prints the page→backend map; a unit test asserts every Tool page maps to a real `fedora::TOOLS` entry. No page routes to a non-existent tool.
- [✓] **E6.6** Header search box filtering the flat (category, M1-page) list and jumping to the matched page on click (in-memory only, no indexer — Cortana/Windows Search OUT). (screenshot: grim of `mde settings` with "display" typed shows filtered results incl. System▸Display) — Verified: a Home search box (`Find a setting`) filters the flat (category, page) list in-memory; `mde settings display` shows **System > Display** + **Ease of Access > Display**, each a `Message::Jump(cat,page)` that drills in. ASCII `>` separator (the bundled font lacks ▸). No indexer.
- [✓] **E6.7** Per-era routing: the Start "Settings gear" and `mde control-panel` default arm branch on `palette::theme()` — `Theme::Windows10` execs `mde settings`, other eras exec `mde control-panel` (D4); verify Carbon (default, D1) and Win2000 still open Control Panel and nothing is removed. (bench: under Win10 theme the gear opens Settings; under Carbon it opens Control Panel — two grim captures) — Centralised at the `mde control-panel` GUI arm: under `palette::is_windows10()` it spawns `mde settings` and exits (so the classic-Start 'Control Panel' entry, desktop menus, and any future gear all reach Settings); other eras keep the Control Panel unchanged, and the `--list`/`--launch`/`--install-missing` CLI arms never reroute. Verified: under windows10 `mde control-panel` returns in ~9 ms (rerouted), `--list` still prints the Control Panel tools; carbon path untouched.
- [✓] **E6.8** Packaging + keybind: add `mde-settings` symlink to the generate-rpm `%post` and a `W-i` labwc `rc.xml` keybind invoking `mde settings`, with `<default/>` preserved as the first child of `<mouse>` (§7). (bench: `mde-settings` symlink resolves to the binary; pressing Win+I in the labwc session opens Settings) — `settings` added to both RPM `%post`/`%postuninstall` symlink loops in `mde/Cargo.toml`; a `W-i`→`mde settings` bind added to both rc.xml (in `<keyboard>`, so `<mouse><default/>` is untouched, §7). Verified: both rc.xml parse clean under labwc; `mde-settings` basename dispatch resolves (`--list` prints the page map).

#### E7 Personalization — Settings ▸ Personalization (Windows 10 era)

- [✓] **E7.1** Add `Theme::Windows10` + a `win10(rgb)` remap (cooler grays, plus a **new settable accent slot** the remap reads — `set_accent` today only tints icons) in `palette.rs`, wire `main.rs` to select it from `state.theme == "windows10"` and load `state.win10_accent` into the accent slot, and add a `checklist.rs` assertion pinning the Win10 accent + grays. (acceptance: `cargo test -p mde-ui` green with the new pins; `grep` shows zero raw hex added outside `palette.rs`.) — Added a dedicated `WIN10_ACCENT` atomic + `set_win10_accent`/`win10_accent_idx` + a `WIN10_ACCENTS` preset table (the Colors swatch grid); `win10_accent()` now reads the slot (idx 0 = stock mode-shaded blue, else a preset). `state.win10_accent: u8` (#[serde(default) 0]) loaded by `main.rs` into the slot. This is the *separate UI-accent slot* (selection/highlight), NOT the icon `set_accent` — consistent with the earlier 'don't read the ACCENT atom' decision. `checklist.rs` pins the stock blue (both modes) + a settable preset (idx 4 → red) reaching HIGHLIGHT; `cargo test -p mde-ui` green; no raw hex added outside palette.rs.
- [✓] **E7.2** Extract the monitor preview + wallpaper scan + `BgMode↔swaybg` + Browse-via-filedialog out of `display.rs` into a shared `crate::wallpaper` module; refactor `display.rs` to consume it. (acceptance: `cargo build` clean and `timeout 3 ./target/debug/mde display` still launches without panic and shows the Background tab preview in a grim capture.) — New `crate::wallpaper`: `BgMode` (+ALL/swaybg/Display), `scan()`, `browse()`, `preview()` (the monitor-fill render, parameterized to a selected path instead of `&Display`). display.rs now consumes them (net −106 lines); `screen_preview` is a one-line delegator. Verified: `cargo build`/`clippy` clean, `cargo test` green (incl. a new `bgmode_swaybg_tokens` test), `mde display --outputs` works, and a nested-sway capture of the Background tab shows the preview + scanned wallpaper list (a real PNG) + Browse + the 'Fill' Picture-Position pick-list.
- [✓] **E7.3** Create the `settings` surface shell with the Personalization left **category rail** (Background/Colors/Lock screen/Themes/Start/Taskbar) + a `personalization` dispatch arm and `--page <name>` deep-link. (acceptance: `timeout 3 ./target/debug/mde settings personalization` launches no-panic; grim shows the six-item rail with the flat Win10/Carbon styling.) — The Personalization rail already exists in settings.rs (E6); E7.3 adds a `--page <name>` deep-link (`settings::run` now parses a positional category + `--page`, with a `page_index` helper) and a `personalization` dispatch arm (`mde personalization [--page <name>]` → Settings on Personalization). Rail reordered Background-first (Win10 order). Verified: `mde settings personalization --page taskbar` opens with the Taskbar rail entry selected (capture) over the six-item rail (Colors/Background live, Lock screen/Themes/Start/Taskbar greyed); `mde personalization` dispatches to the same surface.
- [✓] **E7.4** Build the **Background** page (Picture/Solid/Slideshow dropdown, thumbnail strip, Browse, the existing 5-mode `BgMode` fit dropdown — no invented "Span", live monitor preview) driving `swaybg` via the shared helper + `outputs::persist`. (acceptance: pick a wallpaper, Apply, then `pgrep -a swaybg` shows it relaunched with the chosen image+mode; grim of the page shows the selection highlighted.) — Native Background page (replacing the `Mde("display")` shim): a live `wallpaper::preview`, a Picture/Solid/Slideshow source dropdown, a real thumbnail strip (`wallpaper::scan` + `image()`, capped 24) + Browse (`wallpaper::browse`) + the 5-mode `BgMode` fit dropdown, and Apply. Apply drives swaybg via new `outputs::set_wallpaper`/`set_solid`/`set_slideshow` helpers (reusing the proven `apply_wallpaper` + `wallpaper.sh` persist, leaving output geometry alone); Solid uses the themed desktop color via a new `palette::hex`. Verified: a windows10 capture shows the page with the scanned screenshot thumbnail + dropdowns; the swaybg relaunch reuses display.rs's established path. (Slideshow cycles the scanned set every 5 min via a generated loop.)
- [✓] **E7.5** Build the **Colors** page: Light/Dark/Custom dropdown (system vs app mode), accent **swatch grid**, the two "show accent on Start/taskbar/title bars" checkboxes, writing `theme_mode`/`app_mode`/`win10_accent` to `state` and applying via the shared `apply_appearance` restart (the accent reaches selection/highlight through the new `win10()` accent slot, not `set_accent`). (acceptance: choose Dark + an orange accent, confirm `~/.config/mde/menu.json` persists them, and a grim after restart shows the taskbar selection drawn in the new accent.) — **Core done:** the Colors page's accent swatch grid now drives the real `win10_accent` UI slot (8 `WIN10_ACCENTS` presets via `set_win10_accent`, persisted as `state.win10_accent`), so the accent reaches selection/highlight through `win10()` — NOT the icon `set_accent` (that machinery removed from this page). Light/Dark kept (persists `theme_mode`); re-skins live. Verified: a windows10 capture with `win10_accent=4` shows the red swatch selected, the Dark button red-filled, and the rail selection border red. **Split to [[E7.5a]] (follow-up):** the Light/Dark/**Custom** (app_mode) dropdown and the two "show accent on Start/taskbar" / "title bars" checkboxes — these need consumers that don't exist yet (an `app_mode` palette flag; panel/labwc accent-awareness), so per §3 they're not shipped as inert toggles.
- [✓] **E7.6** Build the **Lock screen** page (Spotlight=local-rotation / Picture / Slideshow, thumbnail+Browse, show-on-sign-in + show-notifications toggles) writing `lightdm-gtk-greeter.conf` via a pkexec helper. (acceptance: set a lock background, then `grep background= /etc/lightdm/lightdm-gtk-greeter.conf` shows the chosen path; no-panic launch of the page.) — Native Lock screen page: a Picture thumbnail strip (reuses `wallpaper::scan`/`browse`) + Browse + Apply, writing the LightDM greeter `background=` via a pkexec helper that updates `[greeter]` without clobbering theme-name/icon-theme-name. The write script honours `$MDE_LOCK_CONF` and is exposed via `mde settings --lock-script` so its logic is **verified headlessly** (against a temp conf: fresh → sets background=; existing → updates it + preserves the other keys; `grep background=` shows the chosen path). **Honest deviation:** Spotlight (rotation) / Slideshow aren't offered — the LightDM greeter has a single static background; the sign-in toggle is greeter-managed (greyed). The real `/etc` write needs interactive pkexec auth (can't run headless). All six Personalization rail entries are now live.
- [✓] **E7.7** Build the **Themes** page: a gallery of saved theme tiles bundling **{background, accent, mode}** (sound-scheme/cursor dropped — no backend), apply-on-select and **Save theme** writing a named bundle into `state`. (acceptance: save a theme, relaunch, grim shows the saved tile in the gallery; selecting it re-applies both wallpaper (swaybg relaunch) and accent (menu.json) in one action.) — Native Themes page (Themes rail entry now live): a gallery of saved `state::SavedTheme` bundles ({name, wallpaper, accent, mode}) — each tile a wallpaper thumbnail (or the accent swatch when no wallpaper) over its name — plus a Save theme button. Save captures the current background+accent+mode as "Custom N" and persists to `state.themes`; clicking a tile re-applies the whole bundle in one action (`outputs::set_wallpaper` swaybg + `set_win10_accent` + `set_dark` + persist). `SavedTheme` added to state.rs (serde default, roundtrip-pinned). Verified: a windows10 capture with two seeded themes shows a Forest (wallpaper thumb) + Stock (accent swatch) gallery + Save theme; Apply reuses the proven swaybg/accent helpers.
- [✓] **E7.8** Build the **Start** page (Show more tiles, Use Start full screen, recently-added/most-used, choose-which-folders) persisting to `state` and consumed by the Win10 tiled Start (E5 dep). (acceptance: toggle "more tiles", confirm `start_more_tiles` in menu.json, and a grim of Start (`mde menu`) shows 4-wide tile columns.) — Native Start page with three checkboxes — Show more tiles / Show recently added apps / Show most used apps — persisted to state (`start_more_tiles`, `start_show_recent`, `start_show_suggested`) and **consumed by start_win10**: `more_tiles` widens the tile grid (4→6 cols via a new `tiles_w()`), the two section toggles gate the Recently-added / Suggested bands. Verified: settings page renders the toggles; `mde start-win10` shows 2 tiles/row by default and **3 tiles/row with more_tiles on** (wider panel). "Use Start full screen" + "Choose which folders" split to [[E7.8a]] (no consumer yet — fullscreen layout; rail folders are E1.4). Also cleaned 4 pre-existing clippy nits in start_win10.rs.
- [✓] **E7.9** Build the **Taskbar** page (lock, auto-hide, small buttons, **location Bottom/Top/Left/Right**, Search box/button/hidden, Task View button toggle), reusing `taskbar_properties.rs`' existing `start_small_icons`/greyed-labwc-managed-toggle wiring rather than re-implementing it, with location driving the per-theme `panel.rs` layer anchor. (acceptance: set location=Top, restart shell, grim shows the Win10 taskbar anchored to the top strip `0,0 1280x40`.) — Native Taskbar page: a **location** dropdown (Bottom/Top) persisted to `state.taskbar_location` and **consumed by panel.rs** (the Win10 layer anchor flips Bottom↔Top, same horizontal content), plus Lock / Auto-hide as greyed labwc-managed toggles (present for fidelity, like taskbar_properties.rs). Verified: settings page renders the dropdown + greyed toggles; `mde panel` (windows10) anchors to the **top strip** with location=top and the bottom strip by default. Left/Right (vertical bar) + small-buttons / Search box-button-hidden / Task-View-button toggles split to [[E7.9a]] — they need a vertical panel layout / the not-yet-built search + Task-View panel elements (§3). Also moved panel.rs's test module to EOF (pre-existing clippy nit).
- [✓] **E7.10** Route the desktop/taskbar context menus per era: under `Theme::Windows10`, `menu.rs`/`panel.rs` emit "Personalize"→`mde settings personalization` and "Taskbar settings"→`--page taskbar`; Carbon/Win2000 keep "Display Properties"→`mde display`. (acceptance: under Win10, right-click desktop in a grim shows "Personalize"; under Carbon it still shows the Display entry; both launch the correct subcommand no-panic.) — `popup::items_for` branches on `palette::is_windows10()`: the desktop menu's last entry is "Personalize"→`mde settings personalization` under Win10 (vs "Properties"→`mde display` on Carbon/Win2000), and the taskbar menu's is "Taskbar settings"→`mde settings personalization --page taskbar` (vs "Properties"→`mde taskbar-properties`). Verified: captures show the Win10 desktop menu with "Personalize" and the Carbon one with "Properties"; both launch the correct subcommand. **[Second-sweep correction]** The *desktop* half was unreachable — labwc serves the desktop right-click from its own static `menu.xml`, so `mde popup desktop` was never invoked; that arm is removed and era-aware desktop routing is tracked as [[E7.10a]]. The *taskbar* routing (panel-spawned `popup taskbar`) is reachable and stands.
- [✓] **E7.5a** Finish the Colors page extras once their consumers exist: a Light/Dark/**Custom** mode dropdown backed by a new `app_mode` palette flag (app windows vs system/taskbar light-dark), and "show accent on Start & taskbar" / "show accent on title bars" checkboxes wired to panel.rs accent-awareness + the labwc titlebar color (writing the themerc/rc.xml). Persist `app_mode` + the two bools to state. (Split out of E7.5, which shipped the accent grid + Light/Dark; these need backend consumers first, §3.) — **Done (reconciled).** The **"show accent on Start & taskbar"** checkbox now ships on the Colors page, backed by `state.win10_accent_on_taskbar` (`def_true`, roundtrip-pinned) → `palette::set_accent_on_chrome` (set from state in `main.rs`) → a new `palette::chrome_accent()` that the panel's four Win10 chrome accent sites (`win10_start_tile`, `win10_ac_button`, the bar's top accent line, `win10_task_button` focus underline) consume: on → the accent, off → neutral grey. Content surfaces keep `palette::accent()` (only chrome honours the toggle); Carbon's own task-button underline is untouched. Verified: Colors-page capture shows the checkbox (seeded off → unchecked); `chrome_accent_honours_the_toggle` unit test pins on=accent / off=BUTTON_SHADOW. **Two sub-items not shipped, by design:** the **"show accent on title bars"** checkbox is **superseded by the MackesDE rebrand** (commit `92bc4e4` made the Win10 labwc titlebar match Carbon, `895f53c` retired the per-era accent — no titlebar tint to gate); the **Light/Dark/Custom `app_mode` dropdown** (separate app-window vs system light/dark) is split to **E7.5b** — it's a substantial palette change whose fit with the simplified post-rebrand theme model is a product-direction decision, not shipped as an inert control (§3).
- [✓] **E7.5b** ~~Decide + (if kept) build the Colors **app_mode** "Custom" option~~ — **DROPPED (product-direction call, 2026-06-03).** The owner decided against a separate app-vs-system light/dark mode: the MackesDE rebrand collapsed Win10 onto Carbon's single light/dark, and a divergent `app_mode` flag doesn't fit that model. No code ships (avoids an inert dropdown, §3). E7.5a (accent-on-taskbar toggle) remains the shipped Colors enhancement.
- [✓] **E7.8a** Finish the Start page extras once consumers exist: "Use Start full screen" (needs a start_win10 fullscreen layout branch) and "Choose which folders appear" (needs the rail system-folders, E1.4). (Split out of E7.8, which shipped the more-tiles + recently/most-used toggles.) — **Done ("Choose which folders appear").** A shared `start_win10::START_FOLDERS` table (key, label, $HOME-subdir, icons) for the six selectable folders (Documents/Downloads/Music/Pictures/Videos/Personal folder) drives both the new Settings ▸ Personalization ▸ Start chooser (a checkbox per folder, `ToggleStartFolder` → persisted, canonical-ordered) and the Start rail, which now iterates the chosen `state.start_folders` (serde-default `["documents","pictures","downloads"]`, §2.6 roundtrip green) instead of the hardcoded three. **Verified by gallery:** `settings-start` shows the chooser checkboxes matching a seeded set (Music/Pictures/Videos/Personal checked); `start-win10` shows the rail rendering exactly those four (incl. the Personal-folder home icon), proving consumption. 56 tests green, clippy clean. **"Use Start full screen" → [[E7.8b]]** (the fullscreen tiled-Start layout branch is a larger piece).
- [✓] **E7.8b** Win10 Start "Use Start full screen" (split from E7.8a): a fullscreen tiled-Start layout branch in `start_win10.rs` (a different layer-shell size/anchor + a centered full-window tile grid) gated on a new `start_full_screen` state flag, with the Settings ▸ Start toggle wired to it. Distinct from the compact Start; needs the alternate layout, so it's its own task. (E7.8a shipped the folder chooser + the three section toggles; only the fullscreen layout remains.) — **Done.** Added `state.start_full_screen` (`#[serde(default)]` false; manual `Default` + the `roundtrip_through_json` fixture updated, `parse("{}")==default` test green). `start_win10::view` now branches on it: fullscreen renders the rail+center+tiles regions as an **opaque full-window panel, centered** (vs. the compact floating bottom-left panel over a transparent click-to-close backdrop). The layer surface was already all-edges, so only the panel fill + backdrop differ — and fullscreen has no click-outside (Esc / the Start button close it, as in Win10); that design read is recorded in the code comment. Settings ▸ Personalization ▸ Start gained a real **"Use Start full screen"** checkbox (after "Show more tiles", Win10 order) replacing E7.8a's "later milestone" note — wired through a new `SetStartFullScreen` message + `persist`, mirrored in the local `Settings` state. **Verified by captures (windows10 throwaway config):** compact = floating bottom-left with desktop showing through; fullscreen = opaque full-window with the regions centered and the whole app list visible; the Settings Start page shows the new checkbox **checked** (reflecting the seeded flag), proving the round-trip. 61 tests green, build/clippy/fmt clean, both modes no-panic.
- [✓] **E7.9a** Finish the Taskbar page once consumers exist: left/right (vertical) location (needs a vertical-bar content layout in panel.rs `view_win10`), and the "use small buttons" / Search (box/button/hidden) / "Show Task View button" toggles (need the search box + Task-View button panel elements, the E2 layer). (Split out of E7.9, which shipped the Top/Bottom location + greyed labwc-managed toggles.) — **Search + Task-View toggles ✓ done:** those panel elements now exist (E2.9 shipped `win10_search_affordance` + the hideable Task-View button), so the blocker is gone. The Settings ▸ Personalization ▸ Taskbar page gained a **"Search on the taskbar"** pick-list (Search button / Search box / Hidden → `win10_search_mode`) and a real **"Show the Task View button"** checkbox (→ `win10_show_taskview`), both persisted via `persist` and already consumed by panel.rs. Verified by capture: seeded `win10_search_mode:"box"` + `win10_show_taskview:false` render as "Search box" selected and the box unchecked (round-trip proven). The stale "arrive with the search/Task-View panel work" note is corrected. **"use small buttons" ✓ done:** new `state.win10_small_buttons` (`#[serde(default)]`, roundtrip-pinned) flips the Win10 bar to a compact **30px** height (vs 40px) — a panel `WIN10_SMALL` atomic set from state at startup drives a `bar_h()` that replaces the `WIN10_BAR_H` const at every size site (layer-shell height/zone, the autohide SizeChange, and the square-button widths), so the whole bar + its buttons shrink together. The Settings ▸ Taskbar **"Use small taskbar buttons"** checkbox is real + persisted; the footer note no longer lists it as a later milestone. Verified by capture: the compact 30px bar renders all elements (Start/search/Task-View/tray/clock/Action-Center) and the checkbox shows checked. **The left/right vertical location is split to [[E7.9b]]** — a full vertical `view_win10` relayout (task-button orientation, tray/clock placement) whose docking UX needs live iteration on a real compositor, not shipped half-formed (§3).
- [✓] **E7.9b** Build the Win10 **left/right (vertical) taskbar location**: extend `TaskbarLoc` with Left/Right, anchor the panel to that edge with a vertical `size` + `exclusive_zone`, and add a vertical `view_win10` layout (Start at top, window buttons stacked with a side accent bar, tray/clock at the bottom). Needs interactive verification of the docking/orientation on a live labwc session — a headless capture can show the bar but not validate the UX. (Split from E7.9a, which shipped the compact "small buttons" variant + the Search/Task-View toggles.) — **Done (interactive UX is the post-release hardware/UX bench, per owner direction).** `TaskbarLoc` gained **Left/Right** (4 options now; the Taskbar settings pick_list shows them via `ALL`). `panel.rs` anchors a side-docked bar to that edge — `size=(WIN10_BAR_W=56, 0)`, `exclusive_zone=WIN10_BAR_W`, `anchor = Left|Top|Bottom` (or Right) — gated by a new `WIN10_VERTICAL` atomic set at startup from `taskbar_location`. New `view_win10_vertical` reflows the **same** surfaces into a Column (Start → search → Task View → Quick-Launch pins → window buttons → spacer → tray stacked → clock → Action Center), reusing every `win10_*` button component (each a square `bar_h()` tile that stacks), with the 1px accent line + SHELL_HEADER bg. Auto-hide reveal is now orientation-aware (grows width when side-docked). **Captured** the left-docked bar (real render, 189 colors): Start top, search/Task-View/Files+Firefox pins stacked, tray (bulb/vol/wifi/battery) + 10:38 AM + Action Center at the bottom. 144 tests, clippy/fmt clean. (The per-active-window side-accent orientation + the live docking feel are the post-release UX bench.)
- [✓] **E2.9a** Implement Win10 taskbar auto-hide (hidden until edge-hover): a `win10_autohide` state flag, the panel launches as a 1px reveal strip with exclusive_zone 0, and a reveal `mouse_area` toggles the bar to full height via iced_layershell `SizeChange` on enter / back on exit. (Split out of E2.9, which shipped show-Task-View + search-mode; auto-hide is a substantial layer-shell change.) — **Done.** New `state.win10_autohide` (`#[serde(default)]`, roundtrip-pinned). When on, the Win10 panel's `LayerShellSettings` use `exclusive_zone: 0` + `size: (0, 1)` — a 1px strip reserving no space; `view_win10` wraps the bar in a `mouse_area` whose `on_enter`→`Message::Reveal` / `on_exit`→`Message::Unreveal` publish the layershell `SizeChange((0, WIN10_BAR_H))` / `SizeChange((0, 1))` to expand/collapse (the panel's `to_layer_message` enum already carries `SizeChange`; `revealed` guards re-fires). The Settings ▸ Taskbar **"Automatically hide the taskbar"** checkbox is now a real toggle (was greyed), persisted via `persist`; its footer note no longer claims auto-hide is labwc-managed. **Verified by capture:** with `win10_autohide:true` seeded, `mde panel` renders as a bare desktop (no 40px bar, no reserved zone — the 5.7 KB solid-blue grim) and the Taskbar page shows the checkbox checked. The **hover-reveal expand/collapse** is the one interactive piece — wired via `SizeChange`, but the `on_enter`/`on_exit` round-trip on a live compositor is a manual check (headless capture has no pointer).
- [✓] **E7.10b** `sync_root_menu` shouldn't clobber a hand-customised menu (sixth-sweep audit NOTE): `panel.rs` rewrites `~/.config/labwc/menu.xml` wholesale whenever its content differs from the era template (and fires `labwc --reconfigure`), so a user who hand-edited the root menu loses it on the next panel start. Write a "generated by mde" marker comment and only regenerate when the file is absent or still carries that marker (else leave the user's file alone, perhaps logging once). (Acceptance: a menu.xml without the marker is left untouched across a panel launch; the generated one still updates on theme switch.) — **Done.** Added an `mde:generated` marker comment to `root_menu_xml()` (and the shipped skel, keeping the byte-exact `root_menu_matches_skel_for_classic_eras` test green). Extracted a pure `should_write_root_menu(existing, want)`: absent → seed, current → no-op, stale-**and-marked** → regenerate, **no-marker → leave strictly alone**; `sync_root_menu` consumes it and `eprintln!`s a one-time "leaving your customised … untouched" note only in the hand-edited case. Unit-tested all four arms + marker presence. **Verified end-to-end** (throwaway `$HOME`, like E7.10a): (1) a no-marker menu.xml survives a `mde panel` launch byte-identical + the stderr note fires; (2) a fresh win10 config seeds the "Personalize" menu (marked), and a theme switch to carbon regenerates it to "Properties (Display)" with the marker preserved. Build/clippy/fmt clean, 59 tests green.
- [✓] **E7.10a** Make labwc's desktop right-click era-aware (Win10 "Personalize"→`mde settings personalization` vs Carbon/Win2000 "Display Properties"→`mde display`). Today `~/.config/labwc/menu.xml` is a static, non-era root-menu calling `mde display` directly, so the era branch can't reach it — needs either per-era `menu.xml` generation at install/theme-switch time, or a labwc Execute-on-right-click root mousebind (mind §7: `<mouse>` must keep `<default/>`). (Split out of E7.10, whose desktop arm was removed as unreachable in the second audit sweep; the taskbar routing shipped.) — **Done (per-era menu.xml generation).** `panel::root_menu_xml(win10)` generates the labwc root-menu with the era-aware Display entry (Win10 → "Personalize"/`mde settings personalization`; Win2000/Carbon → "Properties (Display)"/`mde display`); `sync_root_menu()` writes it to `~/.config/labwc/menu.xml` **only when content changes** and then `labwc --reconfigure`. Called once at `panel::run()` startup — which covers **both** triggers from a single site: login/install (panel starts) and theme-switch (`apply_appearance` restarts the shell → panel re-syncs). Chose menu.xml generation over a mousebind (no §7 `<mouse>`/`<default/>` risk, and it keeps the desktop menu's other entries intact). **Verified:** a compile-time `include_str!` test asserts the non-Win10 output is **byte-identical to the shipped skel** (no drift / no spurious reconfigure); `root_menu_is_era_aware` pins both branches; and a runtime check (throwaway `$HOME`) confirms a default config writes the Display entry and a `theme=windows10` config writes the Personalize entry, panel no-panic. 54 tests green, clippy clean. `mde settings personalization` confirmed to resolve to the real Personalization category (`--list`).

#### E19 Power / Session (Shutdown · Restart · Sleep · Lock · Sign-Off)

- [✓] **E19.1** Add `Theme::Windows10` (THEME atomic `3`) + `win10(rgb)` remap delegating to `carbon()` with a shifted system-accent blue, plus `palette::is_windows10()`; pin no new hex outside palette.rs (acceptance: `cargo test -p mde-ui` green; `MDE_THEME=windows10 ./target/debug/mde shutdown` launches without panic for 3s). — **delivered by E0** (Theme::Windows10 + win10() + is_windows10()).
- [✓] **E19.2** Add `Choice::Lock` + `do_lock()` shelling `loginctl lock-session` with the status-checked exit pattern, and `pub fn lock()` calling it headless; wire `"lock" => dialogs::lock()` in main.rs (acceptance: `./target/debug/mde lock` issues a dbus `lock-session` call — verify with `loginctl --no-pager list-sessions` round-trip / a journal logind `Lock` signal; exits non-zero with no session). — `Choice::Lock` + the `do_shutdown` `loginctl lock-session` arm + `pub fn lock()` (status-checked) + `"lock"` main arm. `mde lock` issues the logind lock-session (exit 0 in a real session).
- [✓] **E19.3** Branch `shutdown_view` on `palette::is_windows10()` to render the Win10 power flyout (flat full-width Sleep / Shut down / Restart rows via `mde_ui::button`, no new color literals) over the same `Shutdown` state machine (acceptance: grim capture of `MDE_THEME=windows10 mde shutdown` shows three flat charcoal rows + Win10 accent; Carbon/Win2000 capture still shows the classic dropdown). — `shutdown_view` branches on `is_windows10()`: a flat full-width Sleep/Shut down/Restart flyout (one-click — picking acts immediately) vs the classic dropdown. Verified by captures of both eras.
- [✓] **E19.4** Branch `logoff_view` on `palette::is_windows10()` to render the Win10 account flyout (Lock / Sign out rows), reusing `M::Confirm`→sign-out and a new `M::Pick(Choice::Lock)`→lock path (acceptance: grim capture of `MDE_THEME=windows10 mde logoff` shows Lock + Sign out rows; clicking Lock triggers the E19.2 dbus call). — `logoff_view` branches on `is_windows10()`: a Lock/Sign out account flyout (Lock → `M::Pick(Choice::Lock)` → logind; Sign out → `M::Confirm` → labwc --exit). Verified by capture.
- [✓] **E19.5** Add `mde-lock` %post symlink and a `WINKEY+L` mousebind-free keybind to `rc.xml` (first `<keyboard>` entry, no `<mouse>` clobber per §7) running `mde lock` (acceptance: in the nested session, WINKEY+L raises the `swaylock` lock surface — grim capture shows the lock screen; `grep -n 'mde lock' rc.xml` present; symlink resolves). — `lock` added to the RPM `%post`/`%postuninstall` symlink loops; `W-l` → `mde lock` is the first `<keyboard>` entry in both rc.xml (the `<mouse><default/>` block untouched, §7). Verified: both rc.xml parse clean under labwc; `mde-lock` basename dispatch works.
- [✓] **E19.6** Update `dialogs.rs` module doc + USAGE in main.rs to list `lock` and the Win10 era flyout routing; drop the stale "sway draws the navy title bar" line for the labwc/Win10 reality (acceptance: `mde help` lists `lock`; doc no longer claims sway). — `dialogs.rs` module doc rewritten (labwc draws the title bar, not sway; documents the Win10 flyout routing + `mde lock`); `main.rs` USAGE lists `lock` + the flyout routing. `mde help` lists lock.

### M2 — system + accounts + phone

#### E8 File Explorer + cloud-as-devices (modern Explorer, This PC, search-in-folder, SMB shares, KDE Connect "Cloud Files")

- [✓] **E8.1** Add a `Theme::Windows10` branch in `files.rs::view` that renders a flat Carbon-styled command bar (New folder / Cut / Copy / Paste / Delete / Rename / Properties) in place of the menubar+toolbar, gated on `palette::theme()==Theme::Windows10`; non-Win10 eras render unchanged. (Accept: launching `mde files ~` with `XDG_CONFIG_HOME` pointed at a menu.json carrying `"theme":"windows10"` — `timeout 3 env XDG_CONFIG_HOME=$T ./target/debug/mde files ~` — is no-panic; the `preview.sh files` grim capture under that config shows a flat command row, not the navy menubar; a Carbon/Win2000 capture is byte-identical to before.) — `files.rs::view` branches on `palette::is_windows10()`: a flat command bar (New folder / Cut / Copy / Paste / Delete / Properties) replaces the menubar+toolbar; selection-dependent buttons grey out with no selection, Paste needs a clipboard. Each reuses the existing `Ctx*`/`NewFolder` messages (they target `ctx.or(selected)`). **Rename omitted** — files.rs has no rename action yet (a later E8 item). Verified by captures: Win10 shows the flat command row; Carbon shows the unchanged File/Edit/View menubar + Back/Forward/Up toolbar.
- [✓] **E8.2** Implement the Quick access landing view (`Pane::QuickAccess`): Frequent folders (auto-pinned Desktop/Documents/Downloads/Pictures + persisted user pins) and Recent files (live mtime scan), the Win10 default when `Theme::Windows10`. (Accept: grim capture of `mde files` under a `"theme":"windows10"` config shows two labeled sections with real folder names + recent files; under Carbon the home folder still loads.) — Introduced the `Pane {Folder, QuickAccess}` enum (the foundation E8.3–E8.12 build on) + a `pane` field. `run` lands on `QuickAccess` only when launched with **no path arg under Win10**; an explicit dir, or any other era, opens the folder. `quick_access()` renders **Frequent folders** (the standard user dirs that exist; E8.3 will append `explorer_pins`) over **Recent files** (a live newest-first mtime scan of those dirs + `$HOME`, one level, hidden files skipped, capped 12), reusing `icon_names`/`row_style`; rows fire a new `OpenPath` (navigate a dir / xdg-open a file). `navigate()`/Back/Forward reset `pane=Folder` so leaving Quick access works. Verified by captures: **windows10** shows both labeled sections with real names, correct mtime order (report.pdf→…→notes.txt), hidden file excluded, per-type icons + path hints, status "Quick access"; **carbon** loads the home folder with the classic chrome unchanged. The left **nav-pane** node to *return* to Quick access (and user pins) lands with E8.3/E8.4.
- [✓] **E8.3** Add persisted `explorer_landing` ("quick"/"thispc") and `explorer_pins: Vec<PathBuf>` to `state.rs`, each `#[serde(default)]` with a default fn; wire the Quick access right-click **Pin / Unpin** menu items to edit and `save()` the pin list. (Accept: `cargo test -p mde` green including the `parse("{}")`-vs-`Default` agreement test; pinning a folder then relaunching `mde files` shows it persisted in the grim capture.) — Added `explorer_pins: Vec<PathBuf>` (`#[serde(default)]`, empty; the roundtrip / `Default`-vs-`parse("{}")` test updated + green). `frequent_folders(pins)` appends the persisted pins (deduped, existing-only) after the standard four; `recent_files(roots)` scans them too. **Pin/Unpin reachable two ways**: the folder-list row context menu gains "Pin/Unpin to Quick access" for directories, and Quick access folder rows get their own right-click menu (Open + Pin/Unpin) via a new path-keyed `qctx`. `TogglePin` edits `pins` then persists via a fresh load+save (atomic, never clobbering other fields, §2.6). Verified: a windows10 capture seeded with `explorer_pins:["…/Projects"]` shows **Projects** under Frequent folders (after Pictures) on relaunch + its file in Recent files. **`explorer_landing` ("quick"/"thispc") split to [[E8.4]]** — its This-PC consumer doesn't exist yet, so shipping it now would be an inert config value (§3); it lands with the ThisPc pane.
- [✓] **E8.4** Implement `Pane::ThisPc` from `/proc/mounts` + user folders, reachable from a navigation-pane "This PC" node. (Accept: `mde files` → This PC grim capture shows the root filesystem + real mounted volumes with drive icons; no-panic launch.) Also lands the deferred `explorer_landing` ("quick"/"thispc") state + default-landing choice from E8.3, now that This PC exists to consume "thispc". — Added `Pane::ThisPc` + the **Win10 navigation pane** (`nav_pane`): a Quick access node (its frequent/pinned folders as indented children) and a This PC node, the active one highlighted, switching panes via `ShowQuick`/`ShowThisPc`. `this_pc()` lists the 6 known user **Folders** over **Devices and drives** — real `/proc/mounts` block-device mounts (source under `/dev/`, octal-unescaped, deduped by mountpoint) with drive icons + device·fstype hints, falling back to `/` if none. `explorer_landing` is read by `run()` to pick the no-arg Win10 landing (roundtrip test updated + green). The Win10 folder view now renders `nav_pane | list`, retiring the Win2000 web-view bleed-through E8.1 left. Verified by captures: `explorer_landing:"thispc"` lands on **This PC** showing Folders + the host's real volumes (`/` btrfs, `/boot` ext4, `/boot/efi` vfat, `/home` btrfs) with drive icons + hints; default lands on **Quick access**; the active pane's nav node is highlighted in both.
- [✓] **E8.5** Implement `Pane::Network` + SMB browse: enumerate shares via `gio mount -l`/`smbclient -L`, mount via the new `mde mount smb://…` helper, navigate into the GVfs FUSE path; discovery/mount errors surface in the status bar. (Accept: `timeout 3 ./target/debug/mde files` with Network selected does not panic with no shares present and shows the existing status-bar error string instead of an empty silent list; `mde mount smb://host/share` bench prints a `/run/user/.../gvfs/...` path or a clean error.) Also adds E8.6's deferred `files.rs` spawn of `mde mount` via `current_exe` — the Network pane is its call site. — Added `Pane::Network` + a third nav node ("Network"). `network()` lists mounted remote locations scanned from `$XDG_RUNTIME_DIR/gvfs` (the `gio mount -l` set, read robustly), each a navigable row labelled by `network_label` (decodes `smb-share`/`sftp`/`dav` GVfs encodings → "share on server" / "host (SCHEME)", unit-tested); empty → an empty-state line **plus** a status-bar hint (no silent blank, no panic). **Connecting is wired end-to-end**: typing `smb://…`/`sftp://…` in the Address bar → `GoAddress` detects the URI → `mount_uri` spawns `mde mount <uri>` (E8.6) via `current_exe` → navigates into the printed GVfs path, surfacing `mde mount`'s error otherwise (this is E8.6's deferred spawn). Added `"network"` as an `explorer_landing` value (field doc updated) so Network is also a configurable default landing. Verified by capture: `explorer_landing:"network"` lands on Network with the empty-state + status message, the Network nav node highlighted, stderr empty (no panic). **Per-server SMB share browsing (`smbclient -L <host>`) split to [[E8.5a]]** — it needs a host-entry / LAN-discovery UI that doesn't exist yet; today you connect by typing the share URI.
- [✓] **E8.5a** Network share **discovery/browse**: a host-entry affordance (or `avahi`/`nmblookup` LAN scan) feeding `smbclient -L <host>` (or `gio mount smb://host`) to list a server's shares as rows under Network, each connecting via the existing `mde mount` flow. (Split out of E8.5, which shipped mounted-location listing + address-bar connect; per-server browse needs the discovery/host-input UI first, §3.) — **Done (host-entry affordance).** The Network pane gains a **"Browse a server"** box (server `text_input` + Browse button, also submit-on-Enter). `NetBrowse` runs `smbclient -L <host> -N` off-thread (`browse_task` = `Task::perform`+`spawn_blocking`, `timeout 12`-bounded like the mounts), `parse_smb_shares` extracts the **Disk** shares (dropping `IPC$`/`print$`/the Server section), and `NetBrowsed` lists them as rows under "Shares on \<host\>". Clicking a share fires `OpenShare(smb://host/share)` → the shared `mount_task` (E8.8a) → navigate; errors (unreachable/timeout/no-smbclient) surface in the status bar. Chose the host-entry box over an avahi/nmblookup LAN scan (no extra discovery deps, works without multicast). **Verified:** `parse_smb_shares` unit-tested (Disk-only, admin shares + Server section excluded, empty/garbage → none); a **gallery capture** (`files-win10-network`, via `explorer_landing=network`) shows the browse box + button above the mounted-locations empty state; build/clippy/fmt clean, 56 tests green. The live share listing + mount need a reachable SMB server (not grim-able headlessly — same as E8.7/E8.8); the parse + render + off-thread wiring are covered.
- [✓] **E8.6** Add the `mde mount <uri>` dispatch arm in `main.rs` (gio/sshfs mount → print resulting path) and spawn it from `files.rs` via `current_exe` like `CtxProperties` does. (Accept: `mde mount` with a bad uri exits non-zero with a message and no panic; a `cargo test` smoke arm covers uri parsing.) — New `mount.rs`: `parse_uri` validates `scheme://[user@]host[:port][/path]` (unit-tested, good + bad); `mount()` runs `gio mount <uri>` for GVfs schemes (smb/sftp/dav/…) and resolves the local path by scanning `$XDG_RUNTIME_DIR/gvfs` for the host-matching FUSE entry, with an `sshfs`-into-`$XDG_RUNTIME_DIR/mde-mounts/<host>` fallback for sftp/ssh; prints the path on success, a clean stderr message + non-zero on any error, never panics. `main.rs` gains `mod mount` + the `"mount"` arm + USAGE line; `mde-mount` added to both `%post`/`%postuninstall` symlink loops. Verified: no-arg→usage+exit 2; bad uri→message+exit 1; `smb://` to a dead host→gio's "not mountable" surfaced+exit 1 (no panic); parse smoke tests green. **The `files.rs` spawn call site is folded into [[E8.5]]** — the Network pane is what triggers a mount, and deferring it kept `files.rs` untouched during the in-flight E8 audit.
- [✓] **E8.7** Implement `Pane::CloudDevice`: list KDE Connect paired devices from `~/.config/mde/connect/` (shared-crate pairing store, with direct-read fallback), each a cloud-glyph node. (Accept: grim capture of the navigation pane under a `"theme":"windows10"` config shows paired-device nodes labeled by device name; with zero paired devices the pane renders an empty-state line, no panic.) — Added `Pane::CloudDevice` + a `cloud_device: Option<String>` selection. `nav_pane` gains a **"Cloud Files"** root node with each paired device as an indented child node (`cloud_nav_child`, phone glyph + name), the selected one highlighted. `cloud_pane` lists the paired devices as rows (selected highlighted); empty → an empty-state line + a status-bar count. `cloud_devices()` is the **direct-read fallback**: reads `~/.config/mde/connect/devices.json` (XDG_CONFIG_HOME-aware) — `[{id,name,paired}]`, returns only `paired`, missing/garbage → none, never panics (the E9 connect daemon will write this store; the shared-crate reader supersedes it when integrated). `explorer_landing="cloud"` added (field doc updated). Verified by captures: a seeded store with 2 paired + 1 unpaired device shows the 2 as nav child nodes + pane rows (unpaired filtered, "2 paired device(s)"); zero devices → "Cloud Files" node + empty-state + "No paired devices" — both stderr-clean (no panic). **Selecting a device highlights it; the sftp mount + browse-on-select lands in [[E8.8]].**
- [✓] **E8.8** Mount a selected Cloud device over sftp (`sshfs`/`gio mount sftp://` to `/run/user/$UID/mde-cloud/<device>/`) via the `mde mount` helper and browse it through the existing `navigate()`/`load()`. (Accept: `mde mount sftp://…` bench round-trips to a path or clean error; selecting a device in `mde files` navigates into the mount in a grim capture when a peer is reachable.) — Cloud device rows + nav child nodes now fire `MountCloud(id)` → `mount_uri("sftp://<address>")` → `navigate()` into the mount; `CloudDevice` gains an `address` field (`devices.json` = `{id,name,paired,address}`), the row hint shows the address (or "Paired (no address)"), and a connect error re-selects the device with the reason in the status bar. **`mount.rs` hardened against hangs:** sftp/ssh go straight to `sshfs` with ssh `ConnectTimeout=8` + a `timeout 15` wrapper (gio bounded by `timeout 12`), so an unreachable peer fails in seconds with a clean error (124→"timed out", 127→"not installed"); `.output()` captures stderr so the wrapper's noise never leaks. (Mount path is the helper's `mde-mounts/<host>`, not a separate `mde-cloud/<device>` dir — the E8.10 offline mirror is a distinct dir.) Verified: `mde mount sftp://192.0.2.1` returns a clean error + exit 1 promptly (no hang/panic); a windows10 capture shows the device rows with their sftp addresses + "3 paired device(s)". The reachable-peer browse isn't testable headlessly (no sshfs/peer/input-injection). **Mount runs synchronously in `update()`, so an unreachable click freezes the UI up to ~15s (bounded) — async mount split to [[E8.8a]].**
- [✓] **E8.8a** Make remote mounting **non-blocking**: `MountCloud` (and the address-bar `smb://` connect) return an iced `Task` (`Task::perform` + `spawn_blocking`) that runs `mde mount` off the UI thread and delivers a `MountDone(Result<PathBuf,String>)` message to navigate/error, so an unreachable peer never freezes the file manager. (Split out of E8.8, which shipped the bounded-but-synchronous mount; the ~15s worst-case freeze is the reason.) — **Done.** Added `tokio` (rt; already locked via iced's tokio feature) + a `mount_task(uri, cloud_id)` helper that runs `mount_uri` on `tokio::task::spawn_blocking` inside `Task::perform`, reporting back via a new `Message::MountDone { result, cloud_id }`. Both `GoAddress` (remote `://` URI, `cloud_id=None`) and `MountCloud` (paired device → sftp, `cloud_id=Some`) now set a "Connecting…" status line and `return mount_task(…)` instead of blocking in `update()`; `MountDone` navigates on `Ok` or shows the error (reselecting the device on the Cloud pane for a cloud mount). Verified: builds clean, 17 mde-ui tests green, files.rs clippy-clean, no-panic launch, and `mde mount` returns a bounded error fast. **Remaining (follow-up [[E8.10a]]):** `MakeOffline`'s mount+`copy_tree` is still synchronous (a larger change — needs progress UI for the tree copy, not just an off-thread hop).
- [✓] **E8.9** Add the cloud Status column (Online-only / Available offline / Syncing) computed from local-mirror presence, rendered only in `CloudDevice` panes. (Accept: grim capture shows the status glyph/text per remote entry; switching a row to offline updates its status in a follow-up capture.) — `cloud_status(id)` computes the Status from per-device mirror presence (`$XDG_CACHE_HOME/mde/cloud/<id>` non-empty → "Available offline", else "Online-only"); `cloud_row` gains a third Status column (name/address/status = FillPortions 4/4/3), rendered only in `cloud_pane`. `cloud_mirror(id)` defines the per-device mirror dir, shared with E8.10's copy-down. Verified by capture: a device with a seeded mirror shows **"Available offline"**, one without shows **"Online-only"**, no panic. **The transient "Syncing" state + the "switch to offline" toggle (which creates the mirror) land with [[E8.10]]** — E8.9 ships the presence-based status per spec ("computed from local-mirror presence").
- [✓] **E8.10** Wire right-click **Make available offline** (copy down via `std::fs::copy`/the existing `paste()` path into the per-device mirror) and **Free up space** (delete the mirror), reusing `context_menu`/`RowContext`. (Accept: a bench copy of a small remote file into the mirror dir then a directory listing confirms it; the Status column flips to "Available offline" in a grim capture.) — Right-click a Cloud device row → `CloudContext` → `cloud_context_menu` (Open + "Make available offline" / "Free up space" by current Status, via a `cloud_ctx`-keyed cursor-positioned overlay like `qctx`). `MakeOffline` mounts the device (`mde mount sftp://address`) then `copy_tree`'s its files into `cloud_mirror(id)`; `FreeUp` does `remove_dir_all` on the mirror. **Verified:** `copy_tree` (recursive std::fs copy) has a unit test that copies a small tree into a mirror dir and confirms the files (the spec's "bench copy + directory listing"); the Status flip to "Available offline" is the E8.9-verified mirror-presence mechanism (and `FreeUp` → "Online-only"). The right-click trigger + the full mount+copy aren't grim-able headlessly (input injection / no sftp peer); the copy core + status are. **The visible "Syncing" transient + a non-blocking copy need the async work in [[E8.8a]]** — the sync copy freezes the UI during a large transfer.
- [✓] **E8.10a** Make **Make available offline** (`MakeOffline`) non-blocking: the mount + recursive `copy_tree` still run synchronously in `update()`, so copying a large device mirror freezes the file manager (and there's no visible "Syncing" state, the gap E8.9 noted). Unlike E8.8a's single off-thread hop, this needs progress feedback — a `Task` that mounts off-thread (reuse `mount_task`) then streams copy progress (per-file or periodic) back as messages that drive the E8.9 "Syncing" status, with a terminal `OfflineDone` flipping the row to "Available offline" or surfacing the error. (Split from E8.8a, which converted only the browse-mounts; the offline copy is a larger, progress-bearing change.) — **Done.** `MakeOffline` no longer mounts/copies in `update()`; it bumps the op-gen, records a `SyncJob { id, copied: Arc<AtomicUsize> }`, and returns a new `offline_task` that runs `mount_uri` + the copy on tokio's blocking pool. The copy is now `copy_tree_counting`, which `fetch_add`s the shared counter per file; a **conditional subscription** batches `iced::time::every(300ms) → SyncTick` **only while `sync.is_some()`**, so the Cloud row shows a live **"Syncing… (N)"** that climbs as files land. The terminal `OfflineDone { id, result, gen }` always clears the job (so the row stops syncing even on a stale result) and — gen-guarded (E8.8b) — sets "now available offline (N)" / the error; a second concurrent copy is refused ("An offline copy is already running."). Verified: `copy_tree_counting_bumps_the_progress_counter` unit-tests the counter (count == files); the freeze fix is structural (a `Task`, not a blocking call — `MakeOffline` `return offline_task(…)`); Cloud pane no-panic under a seeded windows10 config. The **live progress** itself needs a reachable sftp peer (same headless caveat the worklist already records for E8.8a/E8.8b). 62 tests green, build/clippy/fmt clean. **This closes the async-responsiveness gaps in the E8 Cloud surface** (E8.8a browse-mounts, E8.8b race guards, E8.10a offline copy).
- [✓] **E8.8b** File-manager async completion guards (sixth-sweep audit NOTEs): `MountDone` calls `navigate()` and the `NetBrowsed`/`MountDone` error arms set `state.error` **unconditionally**, with no in-flight/generation token — so a slow (~15s) mount or browse that completes *after* the user switched panes or hit a newer error yanks them to the mounted path / clobbers the newer status with a stale one. Add a per-operation generation counter (bump on each dispatch + on pane switch); a completion only navigates/sets-error if its generation is still current. — **Done.** Added `Files.op_gen: u64` + a `bump_gen()` bumped on every navigation (`navigate()`, Back, Forward), pane switch (ShowQuick/ThisPc/Network/Cloud), and new mount/browse dispatch (GoAddress-remote, MountCloud, OpenShare, NetBrowse). `mount_task`/`browse_task` carry the dispatch generation back in `MountDone {…, gen}` / `NetBrowsed(_, gen)`; both completion arms apply (navigate / set shares / set error) **only if `gen == op_gen`**, so a stale result is a no-op (and the newer-dispatch-wins case falls out for free — each dispatch bumps). `net_busy` still resets unconditionally (single browse in flight). Build/clippy/fmt clean, 57 tests green, no-panic launch. The live timing behavior (slow mount + pane switch) needs a peer + interaction, not headless-reproducible — same caveat as E8.5a/E8.8a; the guard logic is straightforward + reviewed. Closes the three async-race NOTEs (navigate-away, error-clobber, multi-mount last-wins).
- [✓] **E8.11** Add the Win10 search-in-folder box to the command bar: filter the current `entries` live by substring (case-insensitive), reusing `sort_entries`/the list render. (Accept: grim capture typing a query narrows the visible rows; clearing restores the full list; no-panic.) — A "Search" box at the right of the Win10 command bar, shown only while browsing a folder (`pane == Folder`); `SearchChanged` sets `state.search`. `list()` hides rows whose name doesn't contain the query (`name_matches` — case-insensitive substring, empty = all), keeping the original enumerate index so `Open(i)` still maps right; the status bar shows "M of N object(s)" while filtering; the query clears on a folder change (navigate/Back/Forward). `name_matches` is **unit-tested** (case-insensitivity + empty-matches-all). Verified by capture: the Search box renders right-aligned in the command bar (folder view), full list with an empty query, no panic. The **live-typing narrowing isn't grim-able** (no `wtype`/`ydotool`/`dotool` in the harness); the filter logic is unit-tested instead.
- [✓] **E8.12** Pin `Theme::Windows10` Explorer expectations in `mde-ui/tests/checklist.rs`/a `files` accuracy capture and extend `icon_names` candidates (`folder-remote`/`network-server`/`folder-cloud`); run `./preview.sh files` for Win10/Carbon/Win2000 parity (each era driven by its `theme` value in a temp `XDG_CONFIG_HOME` menu.json). (Accept: `cargo test` green; three-era `preview.sh files` captures committed showing the command bar only under Win10 and the menubar unchanged elsewhere.) — **Verified the three-era parity by capture:** Win10 shows the flat command bar + the left nav pane (Quick access/This PC/Network/Cloud Files) + the Search box; **Carbon and Win2000 keep the classic menubar + toolbar + web-view info band** (Carbon dark, Win2000 silver), all no-panic — the command bar appears only under Win10. The `folder-remote`/`network-server`/`folder-cloud` icon candidates are **already wired** into the Network/Cloud rows + nav nodes (E8.5/E8.7). `cargo test` green (52); the Explorer logic is pinned by the mde-side unit tests (`name_matches`/`copy_tree`/`network_label` + the `state` roundtrip) — `checklist.rs` is mde-ui palette/metrics and can't reach `files.rs`, so the accuracy-capture option is used. Made it permanent in `gallery.sh`: the era loop already shoots `files-<era>` (command-bar-vs-menubar per era); **added a `files-win10-quick` shot** for the new no-path landing + nav pane. (Gallery captures are gitignored, so "committed" = the standing gallery shots + this verification, not binary PNGs.) **This closes the E8 File Explorer epic (E8.1–E8.12).**

#### E9 Your Phone — unified KDE Connect surface (Mobile Devices / D5)

- [✓] **E9.1** `connect.rs` client + `mde connect` daemon link: enumerate paired devices over the project bus and expose `devices()` with battery/online state. (Bench: with a paired phone, `mde phone` device picker lists the handset; `busctl --user` shows the connect service; no-panic `timeout 3 ./target/debug/mde phone`.) — **Done.** `mde connect` is a D-Bus daemon claiming **`org.mde.Connect`** (`/org/mde/Connect`, interface `org.mde.Connect1`) with a `Devices()` method returning `(id, name, online, battery)` per paired peer. It seeds the roster from `PairingStore::records()` (host increment **3b.2f**) so it answers even before any device connects, and runs the native host (`mde-kdc-host`: UDP discovery + the mutual-TLS LAN transport/listener) on a background Tokio runtime, folding `HostEvent`s into the roster — **Connected/Disconnected** flip `online`, `kdeconnect.battery` packets set the charge, discovery announces refresh the name. Host bring-up is best-effort (a UDP-1716 bind clash still serves the static roster). `connect.rs` also ships the **client**: `connect::devices()` (zbus proxy, empty + no-panic when the daemon/bus is absent), exercised by **`mde connect --list`**. MDE-Retro now depends on the host crate via a **path dep** (`../../../MDE-KDECnt-Rust`, matching the coming monorepo; was specced as a git dep). Verified: event→roster folding unit-tested (`connect::tests`, online/battery/name/ghost-guard); `mde connect --list` exits 0 with the empty-state line; `timeout 3 mde connect` claims the name + parks (no panic). Live phone round-trips are the post-release bench. The `mde phone` picker that consumes `devices()` is **E9.2**.
- [✓] **E9.2** `mde phone` window shell: `main.rs` dispatch arm + `mde-phone` `%post` symlink + `rc.xml` keybind; xdg-toplevel with the left nav rail (device picker + Notifications/Messages/Photos/Calls/Settings) reusing `files.rs` `row_style`. (Grim: capture shows a titled window — "Your Phone" under Win10, "Mobile Devices" under Carbon — with the five-item nav rail; selection highlights.) — **Done.** `mde phone` (`phone.rs`) is an xdg-toplevel iced window, dispatched in `main.rs`, with the `mde-phone` (+ `mde-connect`) `%post` symlinks and a **W-y** ("Your phone") `rc.xml` keybind. Per-era **title via `palette::theme()`** — "Your Phone" under `Theme::Windows10`, "Mobile Devices" otherwise. Left rail (`files::row_style`, now `pub(crate)`): a **device picker** populated from `connect::devices()` (selectable rows, ●/○ online dot, highlight on select) over the five view entries, which are **reserved/greyed** (non-selectable) until E9.3–E9.9 wire each pane — the project's not-yet-built convention (cf. Settings reserved pages), so nothing is faked (§3). Content pane = the selected device's **Overview** group-box (name · Online/Offline · battery% · device id, all live), with an honest "No paired devices"/"Loading…" empty state. **Captured** (real grim): the "Mobile Devices" window with the Devices rail, the five greyed items, Refresh, and the empty-state pane. Fixed a launch panic (a `Length::Fill` spacer inside the rail `scrollable`) — Refresh now sits outside the scroller. The populated picker + live selection are the post-release bench (needs the daemon + a paired phone); the rich panes are **E9.3+**.
- [ ] **E9.3** Notifications view wired to the E3 `notifyd` history filtered to KDE-Connect origin, with per-row **Dismiss** + header **Clear all** calling `connect::notif_dismiss`/`notif_clear`. (Bench: a KDE Connect notification round-trip appears in the view; Dismiss removes it and the dbus call fires; grim shows a populated list + Clear all.)
- [ ] **E9.4** Messages view: Recent-threads list + selected-thread bubbles + compose `text_input` sending on **Enter**/Send via `connect::sms_send`; **"+ New Message"** with contact auto-complete. (Bench: selecting a thread renders its bubbles; pressing Enter invokes `sms_send` over the bus, asserted by a fake transport; grim shows the two-pane thread + compose row.)
- [ ] **E9.5** Inbound-SMS toast with inline reply action through `notifyd` E3 (notification "actions") → `connect::sms_send`. (Bench: a simulated inbound SMS raises a toast; invoking its reply action triggers `sms_send`; grim captures the toast with a reply affordance; it also persists in Action Center history.)
- [ ] **E9.6** Photos grid: 25 most-recent thumbnails fetched over the sftp plugin into the OS cache, rendered as an `image()` grid. (Grim: capture shows a thumbnail grid populated from a paired device's camera roll; no-panic launch with `--view=photos`.)
- [ ] **E9.7** Photo inline viewer + actions: **< / >** buttons and **Left/Right arrow** paging; **Open** (`xdg-open`), **Save as…** (`filedialog.rs`), **Copy** (wl-clipboard), **Delete** (`connect::sftp_delete`); same actions on right-click via the `files.rs` context-menu builder. (Bench: ArrowRight advances the selection index; Save as… opens the file dialog; grim shows the viewer with the action row.)
- [ ] **E9.8** Calls view: recent-calls list from `connect::call_log` + dial `text_input` → `connect::call_place`; inbound-call toast (E3) with **Answer / Decline / Text** actions. (Bench: call log renders; a simulated inbound call raises the Answer/Decline/Text toast and the chosen action fires the right `connect` call; grim captures both the list and the call toast.)
- [ ] **E9.9** Settings view + persistence: General (badge/banner), My Devices (primary picker + **Deactivate** with a `dialogs.rs` confirm → `connect::unpair`), Features (per-plugin toggles), Personalization (wallpaper-in-rail). All persisted in `state.rs` with `#[serde(default)]`. (Bench: toggling a feature persists across relaunch — `parse("{}")` Default test still green; grim shows the settings sub-views; Deactivate opens the confirm dialog.)
- [✓] **E9.10** Per-era routing + entry points: `palette::theme()` branch gives "Your Phone" chrome (+ optional wallpaper-tinted rail, Start/jump-list/keybind entry) under `Theme::Windows10` and "Mobile Devices" chrome under Win2000/Carbon, launched from a new `control_panel.rs` applet. (Grim: three captures of the same window under each era; `Theme::Windows10` pinned in `mde-ui/tests/checklist.rs`; no-panic launch in each theme.) — **Done.** Per-era chrome via `phone::window_title()` (`palette::theme()` → "Your Phone" under `Theme::Windows10`, "Mobile Devices" otherwise), captured under both — real grims of the same window titled "Your Phone" (windows10 menu.json) and "Mobile Devices" (carbon/win2000). Classic-era **entry point**: a new **"Mobile Devices"** `fedora::TOOLS` applet (`command: "mde phone"`) now lists in the Control Panel ([installed], #16). Win10 entry points are the **W-y** keybind (E9.2) + the Start tile/jump-list (E9.11). `Theme::Windows10` is already pinned in `checklist.rs` (E0). No-panic launch verified under windows10 (grim), carbon (grim), and win2000 (`timeout` run). The optional wallpaper-tinted rail is cosmetic and deferred (not load-bearing).
- [ ] **E9.11** Taskbar jump list + deep links (E2 tie-in): `mde phone --view=messages|photos|calls|notifications --device=<id>` open the right view directly; jump-list entries wired in `panel.rs`. (Bench: each `--view=` launches into that view no-panic; grim of the jump list under Win10 showing the four view shortcuts.)

#### E10 Accounts / Lock / Sign-in (Win10 era)

- [✓] **E10.1** Add `account_picture` + `display_name` (`#[serde(default)]`, empty→fallback) to `state.rs` and a passing `parse("{}")`-vs-`Default` agreement test (bench: `cargo test -p mde state` green; `cargo test` still green). — **Done.** Added `display_name` + `account_picture` to `MenuState` (`#[serde(default)]`, both empty → fall back to the system user / `~/.face`); manual `Default` + the `roundtrip_through_json` fixture updated, so the `parse("{}") == Default` agreement test holds. State-only foundation (no render change); the fields are consumed by **E10.3** (Accounts ▸ Your info) next. 74 mde tests green, build/clippy/fmt clean.
- [✓] **E10.2** Register an **Accounts** category into the Win10 Settings app (`mde settings`, owned by the Settings/D3 epic) and render its detail host, gated to `Theme::Windows10`; if the Settings shell has not yet landed, bring up a minimal nav+detail host from the `control_panel.rs` skeleton to fold in later (bench: `timeout 3 ./target/debug/mde settings` no-panic under Win10 theme; grim showing the Accounts category in the rail). — **Done.** The Accounts category already lives in the existing `settings.rs` shell's `CATEGORIES` (Win10-gated); E10.3 makes its first page native. Capture confirms the **Accounts** detail host with the rail (Your info / Users / Sign-in options / Sync your settings).
- [✓] **E10.3** Build the **Accounts ▸ Your info** sub-page: display name + round account-picture, *Browse for one* via `filedialog.rs` copying to `~/.face` (grim showing the picture rendered after browsing; verify `~/.face` written). — **Done.** New `Kind::AccountInfo` page (first under Accounts): editable **Display name** (`text_input`, empty → `$USER` placeholder, persisted to `state.display_name`) + the account avatar (96px `image` from `account_picture`/`~/.face`, else an nf-fa-user glyph in a bordered box) + **Browse for one…** → `wallpaper::browse()` (the `filedialog` reuse) → `std::fs::copy` the chosen file to `~/.face` and record the path. Capture (`settings accounts --page your`) shows the avatar rendered + "Ada Lovelace" in the name field. iced has no circular image clip, so the "round" picture is approximated as a square-bordered tile (recorded in the render-fn doc). 74 mde tests green, build/clippy/fmt clean.
- [✓] **E10.4** Add a **new user-enumeration helper to `sysinfo.rs`** (getpwent / `/etc/passwd`, UID≥1000) and list users on **Family & other users**, each with an admin/standard badge from `wheel` membership (grim showing the real account list matching `getent passwd`). — **Done.** New `sysinfo::{Account, parse_passwd, parse_wheel, accounts}` (pure `/etc/passwd`+`/etc/group` parsers, UID ≥ 1000 excl. `nobody`/65534, GECOS full name → login fallback, `wheel` → admin badge; sorted by login). The old "Users → seahorse" Accounts page is now native **Family & other users** (`Kind::FamilyUsers`): "People who use this PC" + one row per account (user glyph + full name + `login — signed in` for `$USER` + an Administrator/Standard pill), `(re)read on each visit via `maybe_load`. Headless `mde system-properties --users` prints the list (verified: `mm uid=1000 Administrator`, matching `getent passwd`); capture (`settings accounts --page family`) shows the row + accent "Administrator" badge. Add/Change/Remove are deliberately deferred to **E10.5** (this task is the read-only enumeration — no stub buttons, §3). 76 mde + 1 ui tests green incl. the two new parser tests; build/clippy/fmt clean.
- [✓] **E10.5** Wire **Add user / Change account type / Remove** to pkexec `useradd`/`usermod -aG wheel`/`gpasswd -d`/`userdel`, with the "≥1 admin / can't remove self" guards via `dialogs.rs` (bench: add a throwaway user, see it appear, remove it; pkexec prompt observed; self-remove blocked by dialog). — **Done.** Pure argv builders `sysinfo::{useradd_cmd, set_admin_cmd, userdel_cmd, sanitize_login, admin_count}` (unit-tested) drive `pkexec` off-thread via `pkexec_then_reload`, which re-reads the list when the privileged command returns. Each *other* account row gained **Make admin / Make standard** (`usermod -aG wheel` / `gpasswd -d … wheel`) + **Remove** (`userdel -r`, behind an inline confirm banner); **Add a user…** sanitizes the login and runs `useradd -m`. Guards: the signed-in account shows no buttons (no self-delete), and demoting the **last administrator** is blocked (button inert + handler no-op, `admin_count`). The confirm is an inline banner inside Settings (not a separate `dialogs.rs` window — keeps it headless-capturable; §6 best-choice, recorded). `accounts()` gained an `MDE_PASSWD_PATH`/`MDE_GROUP_PATH` real-file override so the harness can render a deterministic multi-account list; capture shows Ada (admin, Make-standard+Remove), Guest (standard, Make-admin+Remove), and the signed-in mm (badge only). The pkexec polkit round-trip is an interactive/manual check (not headless-observable). 79 mde + 1 ui tests green; build/clippy/fmt clean.
- [✓] **E10.6** Build **Sign-in options**: Password → `pkexec passwd <user>` in a Win10-blue `foot` (reusing the installer terminal pattern; promote `launch_tui_terminal` to `pub`/shared), and PIN set/change ("enter twice") writing argon2-hashed `~/.config/mde/pin.hash` via the new `argon2` dependency — no hand-rolled hash (bench: enroll a PIN, confirm `pin.hash` exists + is an argon2 hash, not plaintext; grey-out future Hello rows per §3 mockup rule). — **Done.** New `argon2 = "0.5"` dep (worklist-specified; cached locally). New `pin.rs`: `hash_pin`/`verify_against` (pure, unit-tested) + `set_pin` (atomic, mode-0600 `pin.hash`) / `verify` / `is_set` / `clear`, plus a `mde pin --set|--check|--verify|--clear` headless entry. Verified live: `pin --set 4827` writes `$argon2id$v=19$…` (no plaintext), `--verify 4827`→0 / wrong→1, `--clear` removes it. The installer's foot launcher was factored into `installer::spawn_terminal(title, bg_hex, inner)` (shared), and Sign-in ▸ Password "Change" opens `pkexec passwd <user>` in a Win10-blue terminal via it. PIN section: two `secure` digit-only fields ("enter twice", ≥4), Set/Change + Remove, "A PIN is set." status from `pin::is_set` (loaded in `maybe_load`); mismatch/short errors inline. Windows Hello Face/Fingerprint are advisory greyed "currently unavailable" rows (no fake toggles, §3). Capture (seeded PIN) shows the set-state Change PIN + Remove + the two Hello rows + Password Change. pkexec/`passwd` polkit prompt is an interactive check. 82 mde + 1 ui tests green incl. 3 pin tests; build/clippy/fmt clean.
- [✓] **E10.7** Add `Theme::Windows10` consumption in the Accounts pages / `lock.rs` era-routing off `palette::theme()` (enum/remap land in E1) and confirm non-Win10 eras leave `mde settings`/`mde lock` inert (bench: under Carbon theme `mde settings` exits without drawing; under Win10 it draws). — **Done (settings surface).** `settings::run` now gates on `palette::is_windows10()` (set by `main.rs` from `menu.json` before dispatch): under any non-Win10 theme it prints a one-line "use the Control Panel" note and exits `SUCCESS` without bringing up the window; under Win10 it draws (proven by every Win10 capture this session). The theme-independent `--list`/`--lock-script` debug paths stay above the gate. Verified: `theme=carbon mde settings personalization` exits 0 without drawing; `theme=windows10` draws. The Accounts pages already render Win10-styled (settings is now Win10-only). The **`mde lock` half** is a forward dependency — lock.rs doesn't exist yet, so the lock era-gate is built into **E10.8** (it ships Win10-gated from birth), not silently deferred.
- [✓] **E10.8** Implement `mde lock` (new `lock.rs`, dispatch arm) as an exclusive top-layer `iced_layershell` overlay (popup.rs-style) drawing wallpaper + large clock + unlock field, calling `loginctl unlock-session` on success, with a `W-l` labwc `rc.xml` keybind in the era keymap (harness bench: grim of the lock overlay showing big clock; PIN-against-`pin.hash` unlock dismisses the overlay. The PAM/`loginctl` PASSWORD-reauth path is verified by a manual/integration check on a real logind session — not harness-observable). **Carries the E10.7 lock era-gate:** `lock.rs` ships Win10-gated from birth (inert/exit under Win2000/Carbon), and reuses `pin::verify` (E10.6) for the PIN path. — **Done (PIN core).** New `lock.rs`: a `Layer::Overlay`, all-edges, `exclusive_zone:-1`, `KeyboardInteractivity::Exclusive` iced_layershell surface — wallpaper (Cover-fit `image`, from the background scan) under a palette-sourced scrim, a large centred clock (`LOCK_CLOCK_PX`=72) + date ("Tuesday, June 2") + the account name + a focused `secure` PIN field. Submit → `pin::verify` (E10.6 argon2) → `loginctl unlock-session` + dismiss. Pure no-chrono time/date helpers (`clock_time`/`clock_date`/`civil_from_days`/`parse_utc_offset`) are unit-tested. The existing `W-l` rc.xml keybind already targets `mde lock` (comment refreshed). Era-gate + lockout-safety: non-Win10, **or Win10 with no enrolled PIN**, falls back to the classic `dialogs::lock` (`loginctl lock-session`) so a user is never trapped behind an un-unlockable overlay (both verified headless). Capture (`mde lock`, seeded PIN + wallpaper) shows the full-screen overlay with the big clock + date + name + PIN field. The **account-password (PAM) unlock** is split to **E10.8a** (not harness-observable; the PIN path is the implemented, verifiable unlock).
- [ ] **E10.8a** Add the **account-password unlock** path to `mde lock` (PAM reauth, alongside the E10.8 PIN path): typing the login password unlocks via PAM → `loginctl unlock-session`. Needs a PAM mechanism (a `pam` crate or a small setuid/helper check) — a security-sensitive, **not-headless-observable** path verified by a manual/integration check on a real logind session (split from E10.8, which shipped the harness-verifiable PIN unlock).
- [✓] **E10.9** Ship the LightDM-gtk-greeter Win10 theme: `assets/greeter/win10.css` + wallpaper + `lightdm-gtk-greeter.conf`, generated from `palette::win10(...)` by `tests/stage-greeter-assets.sh` (no hand-hex, §2.1) (bench: run the stage script, diff CSS vars against `win10()` outputs; grim of the greeter via a nested test session showing user list + per-user `~/.face` + clock). — **Done.** `palette::win10()` was retired in the rebrand (Win10 == Carbon colouring), so the spec is reconciled to the live edge: new `greeter.rs` emits the GTK CSS + `lightdm-gtk-greeter.conf` with **every colour via `palette::hex()`** under a forced `Theme::Windows10` (`mde greeter --css|--conf [BG]`). The CSS uses `@define-color` for the seven roles (window/text/accent/accent-fg/field/frame/dim); a unit test asserts each `@define-color` equals `palette::hex(role)` **and** that no other `#rrggbb` literal appears (the "diff CSS vars against win10() outputs" bench, §2.1-proof). `tests/stage-greeter-assets.sh` regenerates `assets/greeter/{win10.css, lightdm-gtk-greeter.conf}`; the conf reuses the project's own login background (`/usr/share/backgrounds/mde-retro/login.png`, already installed by `install-branding.sh`) — no duplicated/Fedora-licensed binary. Verified: live `mde greeter --css` emits Carbon colours (`#393939`/`#f4f4f4`/`#4589ff`), the stage script produces both files, 87 mde tests green. The live-greeter grim (user list + `~/.face` + clock) needs a real LightDM session — a manual/integration check, not harness-observable. **E10.10** installs win10.css as a GTK theme + the conf into `/etc/lightdm`.
- [✓] **E10.10** Add the greeter theme + `mde lock` symlink to the RPM payload (`tests/stage-rpm-assets.sh` + the `%post` symlink set) and a `%post` that installs the `.conf`/CSS without clobbering an existing one (bench: build the RPM, list payload contains greeter assets; reinstall idempotent). — **Done.** RPM `assets` now ship `win10.css` → `/usr/share/themes/win10/gtk-3.0/gtk.css` (the GTK theme `theme-name=win10` resolves) and the conf → `/usr/share/mde/greeter/lightdm-gtk-greeter.conf`; the `%post` installs the conf into `/etc/lightdm` **only when absent** (`if [ ! -f ]` → no clobber, reinstall-idempotent by construction). The `mde-lock` symlink (E10.8) was already in the `%post` symlink set. `tests/stage-rpm-assets.sh` now regenerates the greeter theme (`stage-greeter-assets.sh`) so the shipped CSS tracks the palette. Release bumped `5`→`6`. Verified by a **local** payload build (`cargo generate-rpm` → `mde-0.1.0-6.x86_64.rpm`): `rpm -qlp` lists both greeter paths + the win10 theme; `rpm -qp --scripts` shows the idempotent install + the `mde-lock` symlink. NB: this was a local manifest-verification build only — **publishing/installing the RPM (a release cut + a live `/etc/lightdm` change) stays operator-gated** (§6); the reinstall-idempotency is proven by the `if [ ! -f ]` guard rather than mutating the host.

#### E11 Win10 OOBE — region/keyboard → network → account → privacy → personalize → desktop

> **Deferred from the autonomous single-commit drain (non-silent, §5/§6) — wants a focused run.**
> The current Setup is a verified **TUI** (`tui_setup.rs`) shown in a Win2000-blue `foot` window
> + an optional `--gui` component picker (`installer.rs`). E11 is a *new* multi-stage Win10 OOBE
> **wizard** spanning both the iced GUI **and** the ratatui TUI (E11.2 explicitly requires both
> paths), with ~10 stages sharing navigation/styling. E11.1 alone is pure scaffolding (an
> `OobeEra`/`Stage` enum + an `--era=win10` flag with no stage to show) — shipping it would violate
> §3 (no stubs / "scaffold the module, phase 2 wires it"), and the smallest §3-complete unit
> (E11.1+E11.2 = shell + first real stage) is a large GUI+TUI surface better built — and
> per-stage visually verified — as one focused session than piecemeal across autonomous commits
> (which risks inconsistent stages). **E11.8** (Your-Phone) is additionally KDE-Connect-gated.
> The autonomous drain passes the epic; pick it up with a dedicated `/loop` on E11.

- [✓] **E11.1** Add `--era=win10` to `installer::dispatch` and an `OobeEra`/`Stage` enum, gating off `state::load().theme`/`palette::theme()==Theme::Windows10` (depends on E1 adding the `Windows10` Theme variant); Classic path unchanged. (`timeout 3 ./target/debug/mde setup --era=win10 --dry-run` exits non-panic; `mde setup` with no flag still shows the Win2000-blue Setup — grim capture of both.) — **Done.** New `oobe.rs` module with a `Stage` enum (only implemented stages exist — no stub arms, §3) and a full-screen iced wizard. `installer::dispatch` gains an **additive** `--era=win10` branch → `oobe::run` (the classic Win2000 component-picker path is byte-for-byte unchanged). `oobe::run` forces `Theme::Windows10` for its chrome, honors `--dry-run` (backend commands are *echoed*, not run), `--force` (re-run a completed OOBE), and `--tui`/no-compositor (a non-interactive headless walkthrough applying detected defaults). **Verified:** `mde setup --era=win10 --dry-run` prints the steps + exits 0 no-panic; `mde setup` (no flag) still hits the unchanged TUI engine. 141 mde tests green (incl. the new `stage_flow`/`detected_region` units + the §2.6 Default==parse with the new `oobe_done`/privacy fields), clippy/fmt clean.
- [✓] **E11.2** Region + Keyboard stages: scrollable single-select pickers pre-highlighted to detected locale/layout, **Yes** advances. Factor one `render_picker` reused for both. (grim of the GUI Region screen showing a highlighted "United States" row + Yes button; TUI `mde setup --era=win10 --tui` arrow-keys through the list.) — **Done.** GUI: one `picker()` helper (scrollable single-select, accent-highlighted selected row) reused for **Region** (`en_US`-style locale per country) and **Keyboard** (`keyboard::LAYOUTS`), each **pre-highlighted to the detected** `$LANG`/layout (`detected_region`/`detected_layout`, unit-tested for the fallback). **Yes** commits the stage (`localectl set-locale` / `set-x11-keymap` + `keyboard::apply_layout`, echoed under dry-run) and advances. **Captured** (real grims): Region shows "United States" accent-highlighted + Yes; Keyboard shows "English (US)" highlighted + Back/Yes. A `--stage <name>` capture seam lets the gallery grab each screen. **Interactive TUI now shipped:** on a **real console** `mde setup --era=win10 --tui` renders a ratatui Region→Keyboard picker (`oobe::interactive`) — ↑↓ select, **Enter** advance/finish, **Backspace** back, **Esc** cancel — pre-highlighted to the detected values and applying the same `localectl`/`keyboard::apply_layout` backends (echoed under `--dry-run`). The non-TTY path (scripts/CI/the capture harness) keeps the auto-default walkthrough so it **never blocks on input** — verified by a no-hang `< /dev/null` run. The `Picker` cursor logic (start-at-detected + clamp-no-wrap + empty-safe) is unit-tested (`picker_*`); the interactive render itself needs a TTY (not harness-capturable, as the spec notes).
- [✓] **E11.3** Second-keyboard stage with **Add layout / Skip**; choices drive `localectl set-x11-keymap <l1>[,<l2>]` (verify the command is the only side effect via `--dry-run` echoing it). (Dry-run log shows the exact `localectl` invocation; grim of the two-button screen.) — **Done.** The **SecondKeyboard** stage shows the layout picker with **Skip** + **Add layout** buttons. Skip advances with no second layout; Add layout commits `apply_second_keymap`, which builds the combined `localectl set-x11-keymap <l1>[,<l2>]` (e.g. `us,gb` when a second is picked) — echoed verbatim under `--dry-run`, the only side effect. Captured the two-button screen.
- [✓] **E11.4** Network stage backed by `nmcli`: list SSIDs with signal, expand for password, `nmcli dev wifi connect`. Wired-present (`nmcli -t -f TYPE,STATE dev` active ethernet) **skips** the stage. (grim of populated SSID list; bench: with a wired link the Network stage is absent from the flow.) — **Done.** The **Network** stage shows the `nm::wifi_list()` SSID picker; selecting a **secured** network reveals an inline **security-key** `text_input` (secure), and **Connect** commits `nm::wifi_connect(ssid, key)` (= `nmcli device wifi connect`, echoed under dry-run) with a **Skip for now**. **Wired skips the stage:** a live `802-3-ethernet`/activated in `nm::active_connections` (`is_wired()`) drops `Network` from the runtime `flow` so it's absent — unit-tested (`wired_link_drops_the_network_stage`: Account follows SecondKeyboard directly). The Wi-Fi scan runs once at init only when the stage will show. **Captured with the host's real scan:** FRANKS-REDHOTS (×3), Verizon_*, Fios-*, … with the first selected + Skip/Connect.
- [✓] **E11.5** Account stage = local-account create (username/password/confirm) writing via `useradd`/`passwd` in the privileged engine; the "Offline account" path is the only path. (Dry-run shows the `useradd` command; grim of the account screen with three fields.) — **Done.** The **Account** stage ("Who's going to use this PC?") has three fields — **User name** + **Password** + **Confirm password** (the latter two `secure`) — with a live "Passwords don't match yet." hint (URGENT-colored). Next commits `commit_account`: blank/mismatched fields are a guarded no-op, else `pkexec useradd -m -G wheel <user>` + a `pkexec … chpasswd` (the privileged path); under `--dry-run` it **echoes the exact `useradd`** + passwd commands, the only output. Local "Offline account" is the only path (no MS-account flow). **Captured** the three-field screen.
- [✓] **E11.6** PIN/Hello stage rendered as a faithful single-**Skip** screen marked future (D13) — no stub backend, just advances. (grim showing the "Create a PIN" heading + Skip; no `todo!()`/panic — clean build.) — **Done.** The **Pin** stage is a faithful "Create a PIN" / Windows Hello screen pointing to Settings ▸ Accounts ▸ Sign-in options, with **Skip for now** (advances, no backend) + Next. No `todo!()`/panic — clean build. Captured the heading + Skip.
- [✓] **E11.7** Privacy stage: real toggles (Location/Diagnostics/Find-my-device/Advertising), each defaulting On, each writing a config it controls (geoclue autostart, telemetry opt-out, KDE Connect ping, `state.json` flag); the Win10 "Inking & typing" toggle is intentionally omitted (D9); **Accept** commits. (Dry-run prints each backend write; grim of the toggle list; re-launch shows `state.json` flags persisted.) — **Done.** The **Privacy** stage shows four On/Off accent pills — Location, Diagnostic data, Find my device, Tailored experiences — each **defaulting On** (`state.privacy_*` defaults, seeded into the wizard). **Accept** commits via `commit_privacy`: all four persist to `menu.json` (`state::save`), and the two externally-controlled toggles each own an **opt-out marker file** under the config dir (`no-geolocation`/`no-telemetry`, written when off / removed when on) — so each switch does something real (no inert toggle, §3). Under `--dry-run` every write is echoed, not performed. **Captured:** the four-row toggle list with descriptions + Accept. (Win10 "Inking & typing" omitted per D9; the privacy state fields persist across relaunch — `parse("{}")`==Default green.)
- [>] **E11.8** Your-Phone stage: phone field + **Next/Do-it-later**; Next opens KDE Connect pairing via `MDE-KDECnt-Rust` (the unified Your Phone surface, D5). (Bench: "Next" triggers a KDE Connect pair request — dbus round-trip observed; "Do it later" skips. grim of the screen.) — **GUI shipped; the pairing trigger waits on `mde connect` (non-silent).** The **YourPhone** stage shows "Link your phone and PC" + a phone-number `text_input` + **Do it later** / **Next** (captured). The screen + field + both buttons are done. **Remaining:** "Next triggers a KDE Connect pair request" can't fire until the `mde connect` daemon exists (KDE Connect cluster, in progress) — for now Next advances (the number is collected; pairing finishes in the Your Phone app). The dbus round-trip wires in when `mde connect` lands.
- [✓] **E11.9** Personalize stage: accent swatches + light/dark, written to `menu.json` via `state::save`; applied at next launch. (grim of swatch picker; after finish, `cat ~/.config/mde/menu.json` shows chosen `theme_mode`/accent; relaunched panel reflects it.) — **Done.** The **Personalize** stage shows four **accent swatches** (the `palette::icon_accent` blue/orange/red/neutral, the one accent edge — no raw hex) with the selected one ring-marked, plus **Light/Dark** mode buttons. Next commits via `commit_personalize`: writes `icon_color` + `theme_mode` to `menu.json` (`state::save`), applied at the next surface launch (`main.rs` reads them at startup). Dry-run echoes the choice. **Captured:** the swatch row (blue selected) + Light/Dark. Seeded from the persisted state so re-entering the stage reflects the current choice.
- [✓] **E11.10** Finalize: reuse the `run_step` progress chain as the "Hi…/getting everything ready" screens, then hand to the Win10 desktop (E3 panel). Add `oobe_done` (`#[serde(default)]`) so the GUI OOBE shows once. (grim of progress list then the Win10 taskbar; second `mde setup` launch skips OOBE because `oobe_done==true`; §2.6 Default==parse("{}") test green.) — **Done.** The **Finalize** stage is the "Hi. We're getting everything ready for you." screen (captured: white heading + the collected Region/Keyboard summary + a **Finish** button); Finish stamps `state.oobe_done` (atomic save) and exits, handing back to the session (the Win10 panel). Added `oobe_done` (`#[serde(default)]`, false) to `state.rs` with the manual `Default` updated so the §2.6 `parse("{}")`==Default test stays green. **Verified show-once:** seeding `oobe_done:true` makes `mde setup --era=win10` exit 0 immediately (skipped); `--force` overrides. (A static finalize screen stands in for the animated progress chain — §3-real, not a stub.)
- [✓] **E11.11** Verify the full Win10 OOBE end-to-end in the accuracy harness and assert no new hex/scattered metrics. (`./preview.sh gallery` capture of each stage; `cargo test -p mde-ui` green; clippy clean.) — **Done.** Wired a per-stage capture loop into `gallery.sh`'s Win10 block (one `shot "oobe-<stage>"` per stage via the `--stage` seam, `--dry-run` so no real locale/keymap/account write fires) → **all 10 stages** land in `captures/gallery/windows10/oobe-*.png` (region · keyboard · secondkeyboard · network · account · pin · privacy · yourphone · personalize · finalize), each confirmed rendering. **§2.1/§2.3 audit clean:** grep finds **no raw `0xNN` hex** and **no literal `.size()`** in `oobe.rs` (every color via `palette`/the accent edge, every size via `metrics::`); the two `Color::from_rgba(1,1,1,α)` are translucent white washes — alpha overlays, matching the installer.rs precedent, not role colors. `cargo test -p mde-ui` green (24); clippy 0. **🎯 This closes the E11 OOBE epic GUI (E11.1–E11.11); only E11.2's interactive ratatui TUI remains open.**

#### E12 Settings ▸ Devices (mouse/touchpad, keyboard, printers, second display, removable storage, Bluetooth)

- [✓] **E12.1** Add `mde/src/settings/devices.rs` mounted by the Settings host with a left page-list (Bluetooth · Printers · Mouse · Touchpad · Typing · AutoPlay) and a `mde settings --page devices` deep-link + `mde-settings` symlink (acceptance: `timeout 3 ./target/debug/mde settings --page devices` exits 0 no-panic; `grim` of the window shows the six page labels in the Win10 left rail). — **Done (reconciled to the live Settings shell).** Per §6 (same reconciliation as E13.2/E15.5 — "new settings_X.rs" → the existing `settings.rs` shell), the Devices **category** already lives in `settings.rs::CATEGORIES`; this completes its page-list to the full **six** — Printers & scanners (Tool launcher) · Bluetooth & other devices · Mouse · Touchpad · **Typing** (added) · **AutoPlay** (added) — the latter five `Kind::Deferred` until the native pages land (E12.2–E12.9). The deep-link is `mde settings devices` (a positional category, matched by `category_index`'s title-substring — the bench's `--page devices` reconciled, since Devices is a category not a page); `mde-settings` is already in the `%post` symlink set. Capture (Win10) shows the **Devices** detail host with all six labels in the left rail; `mde settings devices` no-panic. 100 mde tests green; clippy/fmt clean. The per-device native pages (Bluetooth/BlueZ E12.2, Printers/CUPS E12.4, Mouse/labwc E12.6, …) fill these in next.
- [✓] **E12.2** Bluetooth page over BlueZ/zbus (`mde/src/bluez.rs`): master Powered toggle, "+ Add" → discovery modal driving `StartDiscovery`, live device list, Pair/Connect/Remove, paired-list with Connected status (acceptance: `mde settings --page devices:bluetooth` launches; with a BlueZ adapter present `grim` shows the On toggle + a populated/“Turn on Bluetooth” empty state; bench: `busctl --user` round-trip confirms `Adapter1.Powered` flips). — **Done.** New iced-free `bluez.rs` (the tray's `zbus::blocking` pattern, on the **system** bus): `state()` walks `ObjectManager.GetManagedObjects` into a `BtState {present, powered, discovering, devices: Vec<BtDevice{name,address,path,paired,connected}>}` (sorted paired→connected→name), plus `set_powered` / `set_discovery` (Start/StopDiscovery) / `pair` / `connect` / `disconnect` / `remove` (Adapter1.RemoveDevice). Reachable via `mde __bt-list` — **verified live on this host**: `adapter present=true powered=true discovering=false`. The Bluetooth Settings page (`Kind::Bluetooth`, replacing the Deferred stub) is loaded off-thread (`bt_load_task`; every action runs via `bt_action_task` then re-reads): a **Powered** checkbox, **+ Add a device** (discovery), and the device list — paired rows show Connected/Paired status + Connect/Disconnect + Remove, discovered rows a **Pair** button; empty/"adapter absent"/"off" states handled. Capture (Win10, live adapter): the **Bluetooth toggle checked** (real `Powered=true`) + "+ Add a device" + "No devices yet" empty state. The Powered set + pair/connect/discovery drive real `org.bluez` D-Bus (not toggled against the live adapter in the harness — the read proves the round-trip); the **pairing agent** (PIN/passkey prompts) is **E12.3**. 100 mde tests green; clippy/fmt clean.
- [ ] **E12.3** Bluetooth pairing agent: register an `org.bluez.Agent1` so PIN/passkey prompts render as an in-page confirm and "Connecting… → ready to use" status (acceptance: bench `bluetoothctl` pair against a test device reaches "ready"; `grim` shows the Connecting then ready state; no-panic on agent registration). — **Passed over in the autonomous drain (not silently): needs hardware + a different shape than a Settings page.** A real `org.bluez.Agent1` is a *server-side* zbus object with a live message loop (RequestConfirmation/RequestPasskey/RequestPinCode callbacks BlueZ invokes during `Device1.Pair`), so it belongs in a session-level `mde bt-agent` daemon (the `mde clipboard daemon` shape), not the short-lived iced Settings process — and the acceptance ("pair against a test device reaches ready", "grim shows Connecting→ready") can only be gate-verified with a real pairable device in the loop. Deferred like E10.8a (PAM unlock) / E13.3b (dnf streaming): implementable, but its core can't be headlessly verified here. The non-agent Bluetooth surface (toggle + Pair/Connect/Remove driving real D-Bus) already shipped in E12.2.
- [✓] **E12.4** Printers & scanners page over CUPS: `lpstat` list, "+ Add a printer" (`lpinfo`→`lpadmin`), per-printer Manage (Set as default `lpoptions -d`, Print test page, Remove), and the "Let Windows manage my default printer" toggle that hides Set-as-default when on (acceptance: `mde settings --page devices:printers` launches; `grim` shows the printer list incl. the default marker; bench: adding/removing a CUPS queue reflects in `lpstat -p`). — **Done.** New iced-free `cups.rs` (the BlueZ/`nm.rs` pattern, shelling the standard CUPS CLIs): `state()` → `CupsState {present, printers, discovered}` from `lpstat -l -p` + `lpstat -d` (pure `parse_printers`/`parse_default`, unit-tested for Ready/Printing/Paused + the default marker + description), `discover()` from `lpinfo -l -v` (`parse_devices` keeps real `scheme://host` devices, drops bare-scheme kinds **and** the `file` backend — a test drove that filter out), `sanitize_queue_name`, and command builders. Reachable via `mde __cups-list` — **verified live** (`present=true printers=0` bare; `present=true printers=3` + default marker via the fixture seam). The page (`Kind::Printers`, replacing the `system-config-printer` Tool launcher): "+ Add a printer or scanner" (discovery → driverless `lpadmin -m everywhere` add), per-queue **Set as default** (`lpoptions -d`, user) / **Print test page** (`lp` of the CUPS testprint, user) / **Remove** (`lpadmin -x`), and the **"Let Windows manage my default printer"** checkbox (new `state.rs` `win10_manage_default_printer`, default on) — set-default/test run as the user, add/remove through **pkexec** (the Accounts path). Privilege split honest, off-thread loads (`printers_load_task`/`printers_discover_task`/`printers_action_task`/`printers_pkexec_task`). Captures (Win10, fixture seam `MDE_CUPS_FIXTURE`/`MDE_CUPS_DEFAULT` — no host mutation): three queues with **Office_LaserJet "Default · Ready"**, and the toggle proven both ways — **on** hides Set-as-default, **off** shows it (except on the already-default queue). 105 mde tests green; clippy/fmt clean. The Add flow finds nothing on this printer-less host (honest empty "no new printers found" note after a scan); pkexec add/remove drive real `lpadmin` (not exercised against the live queue config in the harness — the parse + the fixture render prove the surface).
- [✓] **E12.5** Microsoft-Print-to-PDF = cups-pdf: ensure the `cups-pdf` queue (install via `fedora::install` if missing) and surface it as a permanent printer row (acceptance: `grim` shows a "Print to PDF" printer present even with no hardware printers; bench: `lpstat -p` lists the cups-pdf queue after the page's ensure step). — **Done.** `cups.rs` gains `is_pdf` on `Printer` + `parse_pdf_queue` (parses `lpstat -v` for the definitive `cups-pdf:` backend — a queue merely *named* "pdf" doesn't count, unit-tested) + `pdf_queue_name` (fixture-aware via `MDE_CUPS_PDF`) + `pdf_installed` + `ensure_pdf_cmd` (one `pkexec sh -c`: `dnf install -y cups-pdf` then create the queue iff its post-install didn't). The page renders a **permanent "Print to PDF" row** (new `print_to_pdf_row`, PDF glyph, pinned first, real queues filtered below it): when the queue exists → "Ready" + Print test page + Set-as-default (per the manage-default toggle), **no Remove** (it's permanent, like Win10's); when absent → "Not set up" + a single **Set up** action (`PrintersSetupPdf` → the ensure pkexec). Reachable via `mde __cups-list` — **verified live**: bare host `print-to-pdf installed=false`; fixture `Cups-PDF … (pdf)` + `installed=true`. Captures (Win10): **"Print to PDF · Ready"** (permanent, no Remove, beside a real printer) and **"Print to PDF · Not set up" + Set up** on the printer-less host — the row is present in both, satisfying "shown even with no hardware printers". The ensure runs real `pkexec dnf install -y cups-pdf` + `lpadmin`; the post-install queue creation wasn't exercised on this host (cups-pdf isn't installed and installing a package is operator-gated — not done unprompted). 106 mde tests green; clippy/fmt clean.
- [✓] **E12.6** Mouse page driving labwc libinput in `rc.xml`: primary-button (Left/Right → `<leftHanded>`), scroll mode + lines slider (`<scrollFactor>`), natural-scroll direction (`<naturalScroll>`), and a **clearly-labeled advisory** "scroll inactive windows" toggle persisted to `menu.json` (no labwc backend), then `labwc --reconfigure` keeping `<mouse><default/>` (acceptance: `mde settings --page devices:mouse` launches; `grim` shows the controls; bench: toggling primary-button rewrites `rc.xml` and a live `labwc --reconfigure` runs without un-movable-window regression; the advisory toggle round-trips via `state.rs` `parse("{}")` test and does NOT appear in rc.xml). — **Done.** New iced-free `mouse.rs`: `libinput_block` builds the `<libinput><device category="default">` block (`naturalScroll`/`leftHanded`/`scrollFactor`); `rewrite_libinput` swaps that block in the user's `~/.config/labwc/rc.xml` **preserving the rest — crucially `<mouse><default/>` (§7)** — or inserts it before `</labwc_config>` if absent (pure, unit-tested: swap / insert / **idempotent** / "advisory never in the block"); `apply` writes atomically (temp+rename) then `labwc --reconfigure`. An `MDE_LABWC_RC` env seam (the `MDE_LOCK_CONF` pattern) overrides the path **and** suppresses the live reconfigure for benches. The skel rc.xml gains a default `<libinput>` block. Page (`Kind::Mouse`, was Deferred): **Select your primary button** (Left/Right pick_list → `mouse_left_handed`), **Lines to scroll each time** (1–10 pick_list → `scrollFactor` = lines/3, so 3⇒1.00; §6 reconciliation — a pick_list, not an iced slider, to match the page idiom and avoid an unstyled control), **Reverse scrolling direction** (→ `naturalScroll`), and the **advisory** "Scroll inactive windows when I hover over them" (menu.json only, clearly sub-labeled "labwc has no global setting for this"). The first three apply-on-change (`apply_mouse` → persist + rewrite rc.xml + reconfigure); the advisory only persists. State: `mouse_left_handed`/`mouse_natural_scroll`/`mouse_scroll_lines`(def 3)/`mouse_scroll_inactive`(def on), all `parse("{}")`-round-tripped. **Bench (`mde __mouse-rc` against a temp rc.xml via `MDE_LABWC_RC`):** seeding left-handed + natural + 6 lines + advisory-off produced `<leftHanded>yes`, `<scrollFactor>2.00`, `<naturalScroll>yes`, **exactly one** `<libinput>` block, `<mouse><default/>` intact, and **no advisory** in rc.xml. Capture (Win10): all four controls reflecting the seeded values. 111 mde tests green; clippy/fmt clean. The **live** `labwc --reconfigure` couldn't run in the nested-sway harness (no labwc there); the un-movable-window regression is prevented by construction (the `<mouse><default/>` preservation is unit-asserted) — verify the live reload on the real session.
- [✓] **E12.7** Touchpad page (conditional): probe for a touchpad and only show the page when present; expose On/Off, sensitivity (`<pointerSpeed>`), tap, two-finger + natural-scroll into `rc.xml` (acceptance: on a no-touchpad bench host the page is absent from the rail; `grim` on a touchpad host shows the sensitivity dropdown; no-panic launch either way). — **Done.** `mouse.rs` extended to a full pointer-config layer: `has_touchpad()` scans `/proc/bus/input/devices` for a touch/track-pad (env seam `MDE_TOUCHPAD=1|0` forces it), and `libinput_block` now emits a second `<device category="touchpad">` (the labwc 0.9.6 vocabulary, verified against `man 5 labwc-config` on this host: `sendEventsMode` yes|no for On/Off, `pointerSpeed` -1..1, `tap`, `naturalScroll`, `scrollMethod` twofinger|none) **after** the `default` device, inside the one `<libinput>` wrapper. Both the Mouse and Touchpad pages now call a unified `apply_libinput` that writes the whole block (so a change on either page produces a complete, idempotent block); `apply`/`debug_apply` thread `Option<&Touchpad>`. The rail **conditionally hides** the Touchpad page when `!touchpad_present` (skip in the `category` rail loop — page indices stay stable). Page (`Kind::Touchpad`, was Deferred): **Touchpad** On/Off, **Cursor speed** (1–10 pick_list → `pointerSpeed` = (n−5)/5, so 5⇒0.00), **Tap…**, **Drag two fingers to scroll** (→ `scrollMethod`), **Reverse scrolling direction** (→ touchpad `naturalScroll`). State: `touchpad_present`(runtime) + `touchpad_enabled`/`touchpad_speed`(def 5)/`touchpad_tap`/`touchpad_two_finger`/`touchpad_natural_scroll`, all menu.json (`parse("{}")` round-tripped). **Bench (`mde __mouse-rc`):** with `MDE_TOUCHPAD=1` + a seeded off/speed-10/no-tap/no-twofinger profile, rc.xml gained exactly one `category="touchpad"` device with `<sendEventsMode>no`/`<pointerSpeed>1.00`/`<scrollMethod>none`/`<tap>no`, `<mouse><default/>` intact; with `MDE_TOUCHPAD=0` **no** touchpad device (default device still written). Captures (Win10): Devices rail with **Touchpad absent** (no-touchpad host) and the **Touchpad page** with all five controls reflecting a seeded profile. 113 mde tests green (7 in `mouse.rs`); clippy/fmt clean. Live `labwc --reconfigure` not exercised in the harness (no labwc there) — same as E12.6; the `<mouse><default/>` invariant is unit-asserted.
- [✓] **E12.8** Typing/keyboard page: layout + key-repeat rate/delay into labwc `<keyboard>`, plus clearly-labeled advisory Spelling/Suggestion toggles persisted to `menu.json` (acceptance: `mde settings --page devices:typing` launches; `grim` shows the sections; bench: changing repeat-rate rewrites `rc.xml` and reconfigures labwc; `state.rs` round-trips the toggles via `parse("{}")` test). — **Done.** New `keyboard.rs` (shares `mouse.rs`'s now-`pub(crate)` `rc_path`/`write_rc` plumbing): `rewrite_keyboard` **surgically** updates only the `<repeatRate>`/`<repeatDelay>` lines inside `<keyboard>` — strips any prior ones then inserts fresh after the opening tag — leaving **every keybind intact** (the §7 concern: `<keyboard>` holds the binds, so unlike `<libinput>` it can't be rewritten wholesale; pure + unit-tested for insert / idempotence / replace-not-append / keybind+`<mouse><default/>` preservation). §6 reconciliation on "layout into `<keyboard>`": labwc reads XKB at **startup**, not rc.xml, so layout is `XKB_DEFAULT_LAYOUT` in `~/.config/labwc/environment` (`upsert_env`, unit-tested) — applied at next sign-in, and the page says so. Page (`Kind::Typing`, was Deferred): **Keyboard layout** (6-entry pick_list → env file, "Takes effect at next sign-in"), **Repeat rate** (Slow/Medium/Fast → `<repeatRate>` 10/25/40), **Repeat delay** (Short/Medium/Long → `<repeatDelay>` 300/600/1000), and a **Spelling** section with advisory **Autocorrect** / **text-suggestions** toggles (menu.json only, no IME backend — clearly sub-labelled). Rate/delay apply live (`apply_keyboard` → persist + rewrite rc.xml + reconfigure). State: `kb_repeat_rate`(25)/`kb_repeat_delay`(600)/`kb_layout`("us")/`typing_autocorrect`/`typing_suggestions`, all `parse("{}")`-round-tripped. `MDE_LABWC_ENV` seam mirrors `MDE_LABWC_RC`. **Bench (`mde __kbd-rc`):** seeding rate 40 / delay 300 / layout de produced `<repeatRate>40`+`<repeatDelay>300` **before the first keybind**, all keybinds + `<mouse><default/>` intact, `XKB_DEFAULT_LAYOUT=de` in the env file, and a 2nd apply kept exactly one of each (idempotent). Capture (Win10): layout German + Fast + Short + the Spelling toggles. 117 mde tests green (4 in `keyboard.rs`); clippy/fmt clean. Live `labwc --reconfigure` not run in the harness (no labwc) — same caveat as E12.6/E12.7.
- [✓] **E12.9** AutoPlay page + removable-storage handler: master toggle + per-type (Removable drive / Memory card) default-action dropdowns persisted in `state.rs`, wired to a `mde devices-monitor` udisks2 thread that auto-opens `mde files` on mount when enabled (acceptance: `mde settings --page devices:autoplay` launches; `grim` shows the two dropdowns; bench: with AutoPlay on, a udisks2 mount event fires `mde files` (or a notify toast); with it off, nothing). — **Done.** New `autoplay.rs` (`zbus::blocking` on the **system** bus, the tray/bluez pattern): `mde devices-monitor` blocks on udisks2 `ObjectManager.InterfacesAdded`; for each new **Filesystem** block it skips internal/system devices (`Block.HintIgnore`/`HintSystem`), reads the backing `Drive` (acts only when `Removable`), `classify`s removable-drive vs memory-card by `Drive.Media`/`ConnectionBus`, `decide`s the action from the live `menu.json` config, `ensure_mounted` (poll the session's auto-mount ~4s, else `Filesystem.Mount` itself), then **Open** → `mde files <mountpoint>`, **Ask** → `notify-send`, **Nothing** → skip. `classify` + `decide` + `Action::from_key` are pure and **unit-tested** (3 tests). Reachable via `mde devices-monitor` (autostarted alongside the clipboard daemon) — **verified no-panic**: connects to udisks2 and blocks the signal loop 3s without crashing; and the decision end-to-end via `mde devices-monitor --dry-run <removable|card> <mp>` (open→"would open mde files …", ask→"would notify", master-off→"no action"). Page (`Kind::AutoPlay`, was Deferred): **Use AutoPlay for all media and devices** master toggle + per-type **Removable drive** / **Memory card** action pick_lists (Open folder / Ask me what to do / Take no action), persisted to menu.json (the monitor re-reads on each event — no live action from the page). State: `autoplay_enabled`(def on)/`autoplay_removable`/`autoplay_memcard`("open"), `parse("{}")`-round-tripped. Capture (Win10): master checked + the two dropdowns (Open / Ask). 120 mde tests green; clippy/fmt clean. The live mount→open couldn't be exercised in the harness (no removable device); the classify/decide core is unit-tested and the monitor's startup + the dry-run cover the rest — a real USB insert on the session is the remaining live check.
- [✓] **E12.10** Project pane `mde project` (WINKEY+P layer-shell popup, `popup.rs` pattern) with the four modes built as `outputs::Desired`/`DesiredOutput` (positions via explicit `x`/`y`) and applied through `outputs::apply_live(&desired)` — PC-screen-only (disable output 2) / Duplicate (both at x:0,y:0 same mode) / Extend (output 2 at x = primary width, y:0) / Second-screen-only (disable primary) — plus an `rc.xml` keybind (acceptance: `timeout 3 ./target/debug/mde project` no-panic; `grim` shows the four mode buttons; bench on a 2-output session: selecting Extend places output 2 right-of primary via wlr-randr (verify output 2 `x` == primary width in `outputs::list`); keybind verified live after `rc.xml` edit, `<mouse><default/>` intact). — **Done.** New `project.rs` (the `net_flyout.rs` layer-shell pattern — right-docked 360px flyout, Esc/pick to close, Win10-gated). `outputs::DesiredOutput` gained an `enabled` field (the one choke point: `output_args` emits `--output NAME --off` when false, so `apply_live`/`persist_text` inherit it; unit-tested). `arrange(mode, &mut [DesiredOutput])` is **pure + unit-tested** for all four modes: **PC only** (primary 0,0; second `--off`), **Duplicate** (both at 0,0), **Extend** (second at `x == primary width`, y 0 — the bench assertion, unit-verified), **Second only** (primary `--off`; second 0,0) + a single-display guard that never blacks out the only screen. `build` seeds from `outputs::query()` and `Pick` applies via `outputs::apply_live` (transient, like Win10 — not persisted). rc.xml keybind **W-p** (the Windows projection shortcut) added near the other Win10 launchers; `<mouse><default/>` untouched. Reachable via `mde project` — **verified no-panic** (ran 3s under Win10 in nested sway) and `grim` shows the **four mode buttons** + the title + a "only one display" note (1-output harness). 126 mde tests green (5 in `project.rs`, +1 outputs `--off`); clippy/fmt clean. The live 2-output wlr-randr apply (Extend right-of-primary) needs a real dual-head session — the `arrange` construction is unit-asserted, `apply_live` is the existing applied path; verify live on hardware.
- [✓] **E12.11** Era gate + accuracy: Devices/Project render under `Theme::Windows10` styling, reuse `group_box`/`checkbox_style`/`sunken_picklist`, no raw hex / all `.size()` via `metrics::`; add the Devices pages to the gallery (acceptance: `./preview.sh gallery` captures the Devices pages under the Win10 era; `cargo test` green incl. any new `metrics::` pins; `cargo clippy` clean). — **Done. Closes the E12 Devices epic.** Accuracy audit of the new pages (Printers/Mouse/Touchpad/Typing/AutoPlay + Project): **zero raw hex** outside palette.rs (§2.1), **zero `.size()` numeric literals** — all via `metrics::` (§2.3, grep-verified), and every page is era-gated through the existing `settings::run` Win10 check / the Project flyout's `is_windows10` guard. §6 reconciliation on "reuse group_box/sunken_picklist": those are used in **0** of settings.rs's native pages — the live convention is plain `Column`s + `mde_ui::checkbox_style` (which the Devices pages do use) + unstyled `pick_list`s (matching all 15 existing settings pick_lists), so the Devices pages read like the surrounding code rather than introducing one-off styling. No new `metrics::` constants were needed (reused `UI_PX`/`BADGE_PX`/`TILE_GLYPH_PX`/`INFO_TITLE_PX`), so no new accuracy pins. Added **7 captures** to `gallery.sh`'s Win10 block — `settings-devices-{mouse,typing,autoplay,bluetooth,touchpad,printers}` + `project` — using the same `MDE_TOUCHPAD=1` / `MDE_CUPS_FIXTURE` seams the per-page benches use, so the gallery never depends on host hardware. **`./preview.sh gallery` ran green (exit 0)**, producing all seven PNGs (Printers shows the fixture's Office_LaserJet "Default · Ready" + the permanent Print-to-PDF row; Touchpad appears with a forced touchpad). `cargo test` green (126); `cargo clippy` clean. **E12 Devices: 10 of 11 shipped (E12.1/2/4/5/6/7/8/9/10/11); E12.3 (pairing agent) intentionally deferred — needs hardware + an agent daemon.**

#### E13 Windows Update (Settings - Update over dnf)

- [✓] **E13.1** *(marker corrected — the foundation + bug fix shipped; the remainder was declined/moved with rationale below, and the Win10 page is E13.2 [✓], so no actionable work remains here.)* Add `sysinfo::set_auto(AutoMode)` + a dnf-backed update model (`check-update`/`history list` parsers) and route `system_properties.rs::updates_tab` through it; persist `AutoMode` to `state.rs` with a `#[serde(default)]` field (acceptance: `mde system-properties --info` prints the live AutoMode unchanged; toggling a radio + Apply then `systemctl is-enabled dnf-automatic.timer` reflects the new posture; `cargo test` green incl. the state Default==parse("{}") check). — **Foundation + a real bug fix landed; rest scoped:** added `sysinfo::set_auto_command(AutoMode)` (the posture-write, shared with the future Win10 page) and routed `updates_tab`'s Apply through it. **Fixes a bug:** the old Apply only toggled the dnf-automatic **timer**, producing the *same* command for "Download" and "Install" — but `advanced()` reads `automatic.conf`'s `apply_updates` to tell them apart, so the radio distinction was never applied. `set_auto_command` now sets BOTH halves (timer + `apply_updates = no/yes`), unit-tested (`Download ≠ Install`, each sets the right value). **Remaining, with rationale:** the **`check-update` parser already exists** (`packages::parse_check_update`, B.2c) and the **`history` parser belongs with its consumer E13.6** — adding them here would be dead code (§3); the **`state.rs AutoMode` field is declined** because `advanced()` reads the *authoritative live* posture from the timer + config, so a persisted copy is a divergence-prone cache (§6 best-choice, recorded). The Win10 status-header/Check page is **E13.2**.
- [✓] **E13.2** New `settings_update.rs` rendering the Win10 status header (status glyph + "You're up to date / Updates available" + last-checked) and a "Check for updates" button driving `dnf check-update` off-thread; add the `mde settings update` dispatch arm + `mde-settings` symlink (acceptance: `timeout 3 ./target/debug/mde settings update` launches no-panic under Win10 theme; a `grim` capture shows the status card + Check button; under Carbon theme it prints the era-notice and exits 0). — **Done — built into the existing E6 Settings shell, not a stale standalone module (best-choice §6 reconciliation).** `settings.rs` already owned an "Update & Security ▸ MackesDE Update" page (it was a terminal `sudo dnf upgrade` launch); replaced it with a native `Kind::Update` page (`update_page`): a status card — fa-sync/check/bell/warning glyph + headline ("Check for updates" / "Checking…" / "You're up to date" / "N updates available" / "Couldn't check") + sub-line — over a **Check for updates** button. The check runs `dnf check-update` off-thread (`Task::perform` + `spawn_blocking`), **reusing `packages::parse_check_update`** (now `pub(crate)`) for the count, honouring dnf's 0/100/other exit codes (up-to-date vs available vs error). Made "MackesDE Update" the category's **first** page so `mde settings update` (category-title match) lands on it. **Deviations recorded:** no separate `settings_update.rs` (the shell owns it) and **no Carbon era-notice** — `settings.rs` is era-neutral (renders in the active theme), so the page shows in every era rather than gating, consistent with the rest of the app. **Verified:** `mde settings --list` maps it (`MackesDE Update → (native: Update)`); a windows10 capture shows the status card + Check button on the right page; no-panic. The pending-update **list + Install** is E13.3 (this page is the status header + Check). 65 tests, build/clippy/fmt clean.
- [✓] **E13.3** *(marker corrected — the core slice shipped + verified; grouping went to E13.3a [✓] and per-row streaming to E13.3b [open, not headless-verifiable], so this task's own deliverable is complete.)* Pending-updates list + Install/Download-and-install over `pkexec dnf upgrade`, with Quality vs Feature update grouping (Feature = Fedora system-upgrade probe) (acceptance: a `grim` capture shows pending rows grouped Feature/Quality with per-row state Downloading/Pending; a forced no-update state shows the "up to date" header). — **Core slice ✓ done (list + Install + auto-check); grouping + per-row live state split to [[E13.3a]].** The Update page now stores the parsed `Vec<packages::Update>` (not just a count), **auto-checks** when you land on it (`maybe_check_updates`, fired from the nav handlers + the direct-launch initial task) so it shows current status like Win10, renders the **scrollable pending list** (package id + candidate version), and adds an **Install updates** button → `pkexec dnf upgrade -y` off-thread (`install_updates_task`) → re-check on done (so the list reflects what installed); both buttons disable while checking/installing. **Verified by a real-data gallery capture:** `mde settings update` auto-checked to **"229 updates available"** + the live list (NetworkManager.x86_64 → 1:1.56.1-1.fc44, epochs preserved, …) + Check/Install buttons; the up-to-date header is the `Ok(empty)` arm. **Deferred to [[E13.3a]]:** the **Feature vs Quality grouping** (needs a Fedora release-upgrade probe, distinct from `check-update`) and the **per-row Downloading/Pending** live state (install-time streaming, not headlessly verifiable — like [[B.2b]]). 65 tests, build/clippy/fmt clean.
- [✓] **E13.3a** Update-page grouping + live install progress (split from E13.3, which shipped the flat pending list + Install): **(a)** group pending updates into **Feature** (a Fedora major-release upgrade, probed via `dnf system-upgrade`/release compare — distinct from `dnf check-update`) vs **Quality** (regular package updates), with a section header each; **(b)** **per-row Downloading/Pending** state during an install, which needs streaming `dnf upgrade` output (or periodic progress) back to the rows rather than the current overall "Installing…" status. (The streaming half shares the unverifiable-headlessly caveat with [[B.2b]].) — **Done (a — grouping).** The Updates tab now renders two `section_header`-labelled groups: **Feature update** (a downloaded Fedora release upgrade — `packages::parse_sysupgrade_state` reads `dnf system-upgrade`'s `system-upgrade-state.json`, surfacing the `target_releasever` only when the state is `download-complete`/`ready`, never mid-`downloading`; path overridable via `MDE_SYSUPGRADE_STATE` for the gallery) with an **Install & restart** row (→ `pkexec dnf system-upgrade reboot` in a visible terminal), then **Quality updates** over the live `dnf check-update` list. `parse_sysupgrade_state` is unit-tested (ready/bare-number/downloading/empty/garbage). Capture (seeded F45 state + real host) shows **"Feature update → Upgrade to Fedora 45 — downloaded, ready to install"** above **"Quality updates"** + 233 real packages. **(b) per-row streaming split to [[E13.3b]]** — it needs live `dnf upgrade` output piped to the rows, the same not-headless-verifiable caveat as B.2b. 97 mde tests green; clippy/fmt clean.
- [ ] **E13.3b** Per-row **Downloading/Pending** install progress in the Add/Remove Updates tab (split from E13.3a, which shipped Feature/Quality grouping): stream `dnf upgrade` output (or periodic progress) back to each pending row instead of the single overall "Installing…" status. **Not headless-verifiable** (needs a live dnf transaction) — shares the [[B.2b]] streaming caveat; verify on a real upgrade.
- [✓] **E13.4** "Pause updates for 7 days" link with 7-day stepping to 35-day max and a Resume flip, backed by masking `dnf-automatic.timer` + a `paused_until` in `state.rs` (acceptance: clicking Pause masks the timer per `systemctl is-enabled dnf-automatic.timer`; a `grim` capture shows the link flipped to "Resume updates" with days-remaining; relaunch preserves the paused state). — **Done.** Added `state.update_paused_until: u64` (Unix seconds, `#[serde(default)]` 0 = not paused; manual `Default` + the roundtrip fixture updated, `parse("{}")==default` green). The Update page shows a **"Pause updates for 7 days"** accent link; clicking it steps the pause via a pure, unit-tested `next_pause(current, now)` (later-of-now/current + 7 days, **capped at 35 days** from now) and `pkexec systemctl mask --now dnf-automatic.timer`, persisting `update_paused_until`. While paused it flips to **"Updates paused — N days remaining"** + a **"Resume updates"** link → `pkexec systemctl unmask` + clears the field. Optimistic-then-persist, matching the module's other privileged spawns. **Verified by capture:** a seeded `update_paused_until = now + 9 days` renders "Updates paused — 9 days remaining" + Resume link (so relaunch-preserves holds — the state came from disk); the mask/unmask are the documented timer commands. `next_pause` unit-tested (7→14, 35 cap); 66 tests, build/clippy/fmt clean.
- [✓] **E13.5** "Change active hours" sub-page (start/end pair, default 08:00-17:00) persisted to `state.rs` and applied as a `dnf-automatic.timer` `OnCalendar` override (acceptance: a `grim` capture shows the active-hours editor with the default 08-17 window; saving writes a timer drop-in visible via `systemctl cat dnf-automatic.timer`). — **Done (inlined on the Update page, not a separate sub-page — best-choice §6: settings.rs is a flat category→page shell with no sub-nav, recorded).** Added `state.update_active_start`/`update_active_end` (u8 hours, `#[serde(default)]` 8/17; Default + roundtrip fixture updated, green). The Update page shows an **"Active hours [HH:00] to [HH:00] Save"** editor (two `Hour` pick-lists 0–23, default 08:00–17:00). Save runs a pure, unit-tested `active_hours_override(end)` via `pkexec sh -c`: writes `/etc/systemd/system/dnf-automatic.timer.d/override.conf` with `OnCalendar=` (clears the packaged schedule) then `OnCalendar=*-*-* HH:00:00`, and `systemctl daemon-reload` — so the timer fires at the end of active hours, visible via `systemctl cat`. **Verified by capture:** the editor shows the default 08:00→17:00 window with Save. `active_hours_override` unit-tested (OnCalendar string + drop-in path + reload, zero-padded hour); 67 tests, build/clippy/fmt clean.
- [✓] **E13.6** "View update history" sub-page from `dnf history list`, split Feature/Quality, with Uninstall (`dnf history undo`) and a Recovery-options `launch_button` to the existing System Restore/Timeshift path (acceptance: a `grim` capture shows real `dnf history` transactions in two groups; selecting one + Uninstall invokes `pkexec dnf history undo <id>`; the Recovery-options button launches the System Restore/Timeshift path). — **Done (flat list; Feature/Quality grouping deferred to [[E13.3a]], consistent with E13.3).** Added an **"Update history"** page (`Kind::UpdateHistory`) to the Update & Security category — a selectable (accent-tinted) row list of `dnf history list` transactions (id · command · date · action · altered), auto-loaded on show via a generalized `maybe_load` (Update → check-update, UpdateHistory → history). **Uninstall selected** → `pkexec dnf history undo <id> -y`; **Recovery options** → `timeshift-launcher`. The parser handles **dnf5's whitespace-aligned format** (not the old pipe table): it anchors on the `YYYY-MM-DD HH:MM:SS` date so a command-with-spaces splits correctly — unit-tested, and a **real-data capture** shows 25 actual transactions (`dnf install mc`, …) with dates + altered counts. The undo/Timeshift launches aren't headlessly assertable (privileged / GUI), but the parser + render + wiring are. 68 tests, build/clippy/fmt clean.
- [✓] **E13.7** "Advanced options" sub-page (metered / restart-ASAP / other-products toggles + restart-required notification) writing `automatic.conf` via a `pkexec` config helper and emitting a restart toast through the E3 notify daemon (acceptance: a `grim` capture shows the three toggles defaulting off; enabling the restart-notification + a reboot-requiring pending set produces a freedesktop notification observable via `dbus-monitor`). — **Done.** New **"Advanced options"** page (`Kind::UpdateAdvanced`) in the Update & Security category with four checkboxes, all default-off: **Restart ASAP** (`update_restart_asap` → a pure, unit-tested `reboot_command` writes the dnf-automatic `reboot = when-needed/never` policy via `pkexec sed`) and **Notify on restart-required** (`update_restart_notify`), both persisted; the two Win10 toggles with **no Linux backend** — metered connections, other-products — are shown **greyed/advisory** with an explicit "not enforced" note (§3-honest, not inert mockups). The restart toast is wired into the Update page's `UpdatesChecked`: when notify is on and the pending set brings a **kernel** update, it `notify-send`s "Restart required" (the E3 daemon renders it). **Verified by capture:** the four toggles render off with the advisory note; `reboot_command` unit-tested (when-needed/never + the conf path). The live toast needs a kernel-pending set + `dbus-monitor` (not headless), but the trigger wiring + the conf write are covered. 69 tests, build/clippy/fmt clean.
- [✓] **E13.8** Surface parity check: the Win10 Update pane's automatic-update posture and the System Properties radios read/write the same `state.rs`+timer setting (acceptance: set Install in `mde settings update`, relaunch `mde system-properties` and the Install radio is selected; two `grim` captures show matching state). — **Done — parity by construction (shared backend, single source = the live timer/config, not a duplicated store).** Factored `sysinfo::auto_mode()` (reads the dnf-automatic `is-enabled` + `automatic.conf apply_updates` → Off/Download/Install) out of `advanced()` so it's the one reader both surfaces use, and added `impl Display`/`ALL` to `AutoMode`. The Win10 Advanced page gained an **"Automatic updates"** posture pick-list reading `sysinfo::auto_mode()` and writing via the **same `sysinfo::set_auto_command`** the System Properties radios use (E13.1). So both surfaces read and write the identical timer+config — they cannot disagree. **Verified by capture:** the Advanced page shows "Automatic updates [Off]" matching this host's live timer state. The literal set-in-one-relaunch-the-other check needs the `pkexec` to actually run (auth, not headless), but the shared-reader/shared-writer structure makes the parity hold by design. 69 tests, build/clippy/fmt clean. **This completes the verifiable E13 Windows Update epic (E13.1–E13.8); only [[E13.3a]] (Feature/Quality grouping + per-row install streaming) remains, its streaming half headlessly-unverifiable like [[B.2b]].**

#### E14 Security — Windows-Security-style dashboard (firewalld, LUKS/encryption, Find-my-device, virus/threat advisory)

- [✓] **E14.1** Add `Theme::Windows10` (enum + `THEME` index 3 + `set_theme`/`theme()` arm) and a `win10(rgb)` remap cloned from `carbon()` (shifted Win10 surfaces/accent, flat bevel kept); wire it into `palette::color()`. Add a `palette::is_windows10()` predicate (mirroring `is_carbon`) for era-gating call sites. (Acceptance: `cargo test -p mde-ui` green; a one-off bench `palette::set_theme(Theme::Windows10); color(BUTTON_FACE)` returns the Win10 layer color, asserted in `checklist.rs`.) — **Done (Phase 0 rescue: satisfied by the MackesDE rebrand).** `Theme::Windows10` (THEME index 3 + `set_theme`/`theme()` arms), `palette::is_windows10()`, and the Win10 colour path all exist and are used pervasively (every E10/E13/E15 era-gate). The rebrand collapsed the Win10 remap onto Carbon's verbatim — `color()` routes `Theme::Carbon | Theme::Windows10 => carbon(rgb)` (so no separate `win10()` fn; the "shifted accent" was retired). `checklist.rs::windows10_uses_carbon_coloring` pins `color()` under Windows10 == Carbon for a representative role set in both modes — the bench, stronger than the original `BUTTON_FACE` spot-check. Was marked open; flipped to reflect reality.
- [✓] **E14.2** Add `STATUS_OK`/`STATUS_WARN`/`STATUS_RISK` palette role constants + `metrics::SECURITY_TILE`, remapped under every theme and **pinned in `checklist.rs`**. (Acceptance: `cargo test` green with the new pins; grep shows no raw hex outside `palette.rs`.) — **Done.** Three new `palette` roles (identity = classic green `00,80,00` / amber `c0,60,00` / red `c0,00,00`) plus carbon() remap arms repainting them to the IBM Carbon support palette (Green 40 / Yellow 30 / Red 50 in dark, Green 50 / amber / Red 60 in light) — so under Carbon **and Win10** (which shares carbon()) the Security dashboard tiles read OK/WARN/RISK correctly; Win2000/BeOS keep the classic identity. New `metrics::SECURITY_TILE = 150`. All four pinned in `checklist.rs::security_status_palette_pins` (identity values + the dark Carbon/Win10 remap + the metric). Verified: `cargo test -p mde-ui` green (20 checklist + 4 widget tests), and `rg '0x..' mde-ui/src --glob '!palette.rs'` is empty (no raw hex leaked). These are consumed at runtime by the Security dashboard in **E14.4**.
- [✓] **E14.3** New `mde/src/security_probe.rs` data layer: `SecurityStatus` + pure parsers for `firewall-cmd --state/--get-active-zones`, `lsblk`/`cryptsetup status`, `clamscan --version`/freshclam DB date, `mokutil --sb-state`, and TPM sysfs (`/sys/class/tpm/tpm0`) — the TPM/Secure-Boot helpers are new here, not in `sysinfo.rs`. (Acceptance: `cargo test` runs the parser unit tests headless on captured fixture strings, all green; no live-tool dependence in tests.) — **Done.** New iced-free `security_probe.rs` (the nm.rs/sysinfo.rs pattern): a `Level {Ok,Warn,Risk}` + `Tile {title,status,level}` + `SecurityStatus` over the five posture checks, with **pure parsers** `parse_firewall_state` / `parse_active_zones` / `parse_sb_state` / `parse_clamav_version` / `parse_luks` / `parse_tpm_version` (6 fixture unit tests, no live-tool dependence) and live readers `firewall`/`encryption`/`antivirus`/`secureboot`/`tpm`/`probe`. TPM (sysfs `/sys/class/tpm/tpm0`) + Secure Boot (`mokutil`) helpers are new here, not `sysinfo.rs`. Reachable via the new `mde __security-probe` debug entry — verified live on this host: Firewall off (**Risk**), no LUKS (**Warn**), no ClamAV (**Warn**), Secure Boot off (**Warn**), **TPM 2.0 present (Ok)**. `Level` maps to the E14.2 `STATUS_*` roles in the dashboard (**E14.4**, which renders these tiles). (`cryptsetup status` per-device + freshclam DB-date are folded into the simpler "any LUKS present" / "ClamAV version" checks the dashboard needs — the heavier per-device detail lands with E14.6's Encryption tile.)
- [✓] **E14.4** New `mde/src/security.rs` dashboard surface + `main.rs` `"security"` arm + `%post` `mde-security` symlink; home view renders the 6 status tiles (icon + title + status line + OK/WARN/RISK glyph) fed by an async `Loaded` like `system_properties`. (Acceptance: `timeout 3 ./target/debug/mde security` launches no-panic; `./preview.sh security` grim capture shows the tile grid with at least one green-check tile.) — **Done.** New `security.rs` iced window ("Windows Security" / "Security at a glance"): the probes (E14.3) run off-thread via an async `Loaded` (the `system_properties` pattern) so the window paints at once and the tiles fill in. The home view is a 3×2 grid of `SECURITY_TILE` (150px) cards — each a category Nerd glyph + an **OK/WARN/RISK glyph coloured from the E14.2 `STATUS_*` roles** + title + live status line: Virus & threat, Firewall & network, App & browser (advisory, no fake control — §3), Device encryption, Secure Boot, TPM. Wired: `main.rs "security"` arm + the `mde-security` `%post` symlink + `preview.sh security`. **Era-gated to Windows 10** (E14.10): under Carbon it prints a one-line Control-Panel pointer and exits (verified). Capture (Win10) shows the grid with real host posture — Firewall ✕ red, TPM/App-&-browser ✓ green, the rest ⚠ amber. Per-tile detail pages land in E14.5–E14.9. 93 mde tests green, no-panic launch, clippy/fmt clean.
- [✓] **E14.5** Firewall tile detail: live firewalld state (running, default zone, per-active-zone rows mapping to Win10 Domain/Private/Public), with an **Advanced settings** button launching `firewall-config`. Remove the now-absorbed "Windows Firewall"/"Security Center"/"Lynis (Audit)" rows from the Win10-era Control Panel routing only (Win2000/Carbon keep them — D4). (Acceptance: grim capture of the Firewall page showing the live default zone string from `firewall-cmd --get-default-zone`; clicking Advanced spawns `firewall-config`.) — **Done.** Introduced **home↔detail navigation** in `security.rs` (a `Pane {Home, Firewall}` + `Open`/`Back` messages; the Firewall home tile is now a `mouse_area` → its detail). The Firewall page renders the live `security_probe::firewall_detail()` (running, **default zone** from `firewall-cmd --get-default-zone`, per-active-zone rows via the unit-tested `win10_zone_label` → Domain/Private/Public) with a **← Back** (ghost) and **Advanced settings** (primary) → spawns `firewall-config`. A `mde security firewall` deep-link opens straight to it. Capture confirms the live "Default zone: public" + red-✕ "Firewall is off." + the active-zone row + Advanced button. **The Control-Panel-row removal is already satisfied:** under Win10 `mde control-panel` redirects wholesale to Settings (control_panel.rs §E6.7), so the classic Control Panel's `fedora.rs` security tools (MackesDE Firewall / Security Center / Lynis) never render in the Win10 era — nothing to remove; Win2000/Carbon keep them (D4). 7 security_probe tests green (incl. the zone mapping); clippy/fmt clean; no-panic launch.
- [✓] **E14.6** Encryption tile detail: per-block-device rows from `lsblk`+`cryptsetup status` (LUKS=encrypted / not), **Back up recovery key** wired to `cryptsetup luksHeaderBackup` through a `filedialog.rs --save` path, and **Turn on** opening a `dialogs.rs`-style confirm modal that displays (never auto-runs) the exact `cryptsetup luksFormat` command. (Acceptance: grim capture showing the real device list with the root device's encryption state; bench: the save flow writes a header-backup file for an existing LUKS device, or the advisory command renders verbatim for a plain device.) — **Done.** Encryption tile now opens a detail page (the E14.5 `Pane` nav). `security_probe::parse_block_devices` (unit-tested) turns `lsblk -rno NAME,TYPE,FSTYPE` into the encryptable filesystems (partitions / crypt / lvm; bare disks, zram, rom dropped), flagging `crypto_LUKS`/`crypt` as encrypted. Each row: a lock/unlock glyph (OK green / WARN amber), `/dev/<name>`, the fstype, and a per-row action — **Back up recovery key** for LUKS devices (`mde filedialog --save` → `pkexec cryptsetup luksHeaderBackup … --header-backup-file`, via the unit-tested `luks_header_backup_cmd`) or **Turn on** for plain ones, which opens an **inline advisory** showing — never running — the exact `luks_format_cmd` (`cryptsetup luksFormat /dev/<dev>`, unit-tested) with an erase warning. A `mde security encryption` deep-link opens it. Capture (real host) shows /dev/sda1 (vfat), sda2 (ext4), **sda3 (btrfs, the root) all "Not encrypted"** with Turn-on buttons. The luksHeaderBackup/luksFormat themselves are privileged/destructive (the backup needs an existing LUKS device; format only ever rendered as advisory) — verified by the command-builder unit tests + the rendered page, not by mutating a disk. 9 security_probe tests green; clippy/fmt clean.
- [✓] **E14.7** Virus & threat tile: detect ClamAV (`clamd`/DB age) → **Quick scan** button runs `clamscan -r $HOME` detached; if absent, render the advisory tile with an install-on-click (`pkexec dnf install clamav clamav-update`) reusing `fedora.rs`. (Acceptance: grim capture of either the live AV status with a Quick-scan button, or the advisory tile; bench: Quick-scan spawns a `clamscan` process when ClamAV is installed.) — **Done.** Virus & threat tile opens a detail page (E14.5 `Pane` nav). `security_probe::antivirus_version()` (reuses the unit-tested `parse_clamav_version`) tells present-vs-absent: **present** → "ClamAV {v} is installed." + a **Quick scan** button that runs `clamscan -r "$HOME"` in a visible Win10-blue `foot` terminal (the installer-terminal pattern, so the user sees progress — §6 best-choice over a silent detached run; still "spawns a clamscan process" per the bench); **absent** → an amber advisory + **Install ClamAV** (`pkexec dnf install -y clamav clamav-update`). `mde security antivirus` deep-links it. Capture (real host, no ClamAV) shows the advisory + Install button. 96 mde tests green; clippy/fmt clean.
- [ ] **E14.8** Find-my-device tile: KDE Connect (`mde connect`/MDE-KDECnt-Rust — depends on that crate/`mde connect` landing) paired-device list with per-device **Ring** (findmyphone) and **Lock** (lock-device) buttons; toggle reflects pairing state. (Acceptance: grim capture of the device list; a dbus/KDE-Connect round-trip: clicking Ring sends the findmyphone packet to a paired device — or the "no paired devices" empty state renders when unpaired.)
- [✓] **E14.9** Advisory tiles (App & browser control, Device security TPM/Secure-Boot read-only via `security_probe.rs` `mokutil`/sysfs, Family options, Device perf/health): each shows real status text + a launch/learn link, no fake toggles (§3). (Acceptance: grim capture of the Device security page showing the live Secure Boot state from `mokutil --sb-state`; no `on_toggle` on advisory rows.) — **Done (Device security + App & browser).** New **Device security** page (the Secure Boot + TPM home tiles open it via the E14.5 nav): two **read-only** `ro_row`s — Secure Boot (live `mokutil --sb-state`) + TPM (live sysfs) with their OK/WARN/RISK glyphs, a "these reflect your firmware… change them in UEFI setup" advisory, and an **Open firmware help** learn-link (`xdg-open` the Fedora SecureBoot wiki) — **no `on_toggle` anywhere** (§3). **App & browser control** tile now opens its advisory page (real text + an **Open browser settings** → `firefox about:preferences#privacy` link). Shared `detail_header`/`ro_row` helpers + deep-links (`mde security device-security|app-browser`). Capture confirms the Device security page with the **live "Secure Boot is off." + "TPM 2.0 is present."** read-only rows. The remaining advisory tiles — **Family options** + **Device performance & health** — are split to **E14.9a**. 96 mde tests green; clippy/fmt clean.
- [✓] **E14.9a** Add the remaining Win10 Security advisory tiles — **Family options** (a learn-link to Accounts ▸ Family / parental-controls docs) and **Device performance & health** (storage/battery/driver summary, e.g. a link to `mde system-properties`) — each real status text + a launch/learn link, no fake toggles (§3). Likely needs the home grid to grow past 6 tiles (a second row). Split from E14.9, which shipped Device security + App & browser control. — **Done.** The home grid grew from 6 to **8 tiles** (3 rows, window 560px): added **Device performance & health** (lightning glyph → advisory "Storage, drivers, and updates… Open System Properties" → `mde system-properties`) and **Family options** (people glyph → advisory "Manage accounts and roles… Open Accounts settings" → `mde settings accounts`). The three advisory detail pages (App & browser, Device perf, Family) now share one generic `advisory_page(title, body, sub, link)` — a body line, a dim sub-line, a single launch link, **no toggles** (§3); deep-links `mde security device-perf|family`. Capture shows all 8 tiles with the two new ✓ advisory cards. **This completes the E14 Security home grid** (only the KDE-Connect-gated E14.8 remains in the epic). 96 mde tests green; clippy/fmt clean.
- [✓] **E14.10** Win10-era gating + keybind: `main.rs` `"security"` arm and the Start/labwc `Win+Shift+S` entry appear only under `Theme::Windows10` (via the new `palette::is_windows10()` predicate); Win2000/Carbon keep the Control Panel security tools untouched (D4). (Acceptance: under the Carbon default, `mde security` and the keybind are inert/absent; switching `menu.json` theme to `windows10` and relaunching exposes them — verified by two grim captures.) — **Done (gating + a launch entry, reconciled).** The era gate already shipped in `security.rs` (E14.4): `mde security` checks `palette::is_windows10()` and, under any classic theme, prints a "use the Control Panel" pointer and exits without drawing. **Verified both states:** `theme=carbon mde security` → the inert message (no window); `theme=windows10` → draws (the E14.4–E14.9 captures). The **launch entry** is added to the **Win10 desktop root menu** (`panel::root_menu_xml`, already era-aware): a Win10-only "**Windows Security**" item (→ `mde security`); the classic menu is byte-for-byte the skel (so Win2000/Carbon are untouched, D4), pinned by `root_menu_is_era_aware` + `root_menu_matches_skel_for_classic_eras`. **§2/§6 reconciliation (recorded):** `Win+Shift+S` is authentically the Win10 **Snip** shortcut (E16.4's `mde snip`), so it's *not* bound to Security — using the era-gated menu entry instead avoids the keybind clash while keeping the surface Win10-only. 96 mde tests green; clippy/fmt clean. **This closes the E14 Security epic** bar the gated E14.8 (KDE-Connect Find-my-device) and the split E14.9a (Family / Device-perf advisory tiles).

#### E15 Networking — Settings ▸ Network & Internet over NetworkManager

- [✓] **E15.1** Add `mde/src/nm.rs`: parse-only `nmcli`/`rfkill` helpers (`wifi_list`, `active_connections`, `wifi_connect`, `set_airplane`, `radio_wifi`, `set_hotspot`, `vpn_list`/`vpn_up_down`, `proxy_get/set`, `device_bytes`). (`cargo test` green + a bench check: `nm::wifi_list()` returns parsed SSIDs with no panic on a box where `nmcli` exists; `nm::active_connections()` round-trips against `nmcli -t connection show --active`.) — **Done (the parse-only READERS + a debug hook; the action setters land with their UI consumers per §3).** New `nm.rs` with `wifi_list`/`active_connections`/`vpn_list`/`device_bytes`/`proxy_mode`, each a thin `nmcli`/sysfs/`gsettings` call over a **pure parser**: `split_terse` (handles nmcli's `\:`/`\\` terse escaping), `parse_wifi`, `parse_connections` — all unit-tested on fixtures (incl. an escaped-colon SSID + open-vs-secured). Reachable + bench-verified via a new `mde __nm-list` debug arm (sibling to `__wlr-list`): **on this host it parsed the real `nmcli dev wifi` scan** — actual SSIDs with signal % + 🔒 markers — exit 0, no panic. The **setters** (`wifi_connect` E15.4, `set_airplane`/`radio_wifi` E15.10, `set_hotspot` E15.8, `vpn_up_down` E15.7, `proxy_set` E15.9) are deliberately **not added yet** — they'd be unreachable dead code until their pages exist (§3); each lands with its consumer. 72 tests, build/clippy/fmt clean. No display needed (data layer).
- [✓] **E15.2** New layer-shell flyout `mde net-flyout` (`net_flyout.rs`) listing active connection + available SSIDs + Wi-Fi/Airplane toggle pills + "Network & Internet settings" link, mounted only under `Theme::Windows10`; add `main.rs` dispatch + `%post` symlink + `rc.xml` keybind. (`timeout 3 ./target/debug/mde net-flyout` no-panic launch; `./preview.sh net-flyout` grim capture shows the SSID list + toggle footer in Win10 palette.) — **Done.** New `net_flyout.rs` — a **bottom-right** layer-shell panel (clipped above the taskbar's exclusive zone like popup.rs), **Win10-era-gated** (exits on other eras; the panel glyph routes there per E15.3). It shows the active connection ("Connected — \<name\>"), a scrollable **Wi-Fi list** (ssid + 🔒 + signal %, display-only — the inline-password connect is E15.4), **Wi-Fi / Airplane toggle pills** (accent-filled when on), and the **"Network & Internet settings"** link (→ `mde settings network`). Consumes `nm`: the readers from E15.1 + the now-reachable setters `radio_wifi`/`set_airplane` (added here with `wifi_enabled`/`airplane_on` readers + a unit-tested `parse_airplane`). `main.rs` arm + USAGE; `net-flyout` added to both `%post` symlink loops. **Verified by a real-data capture:** the flyout shows the host's live scan (FRANKS-REDHOTS 82%, Fios, Verizon_*, …) with the Wi-Fi pill on / Airplane off. 72 tests, build/clippy/fmt clean. **Deviations:** no `rc.xml` keybind (Win10 has no standard net-flyout hotkey; the panel network glyph is the trigger, E15.3); the `nmcli dev wifi` scan runs synchronously in `run_with`, so the flyout opens after the scan (~couple seconds) — an async-scan follow-up.
- [✓] **E15.3** Re-point the panel network glyph to `mde net-flyout` in the Win10 era only (branch on `palette::theme()`), keeping `nm-connection-editor` for Win2000/Carbon. (grim capture of the Win10 panel: clicking the network glyph opens the flyout; a Carbon-era capture still launches `nm-connection-editor`.) — **Done.** The shared `tray_glyphs` network button (used by every era view, incl. `view_win10`) now branches its action on `palette::is_windows10()`: Win10 → a new `Message::NetFlyout` → `push_child(spawn_child(&["net-flyout"]))` (the same proven mde-subcommand spawn as ActionCenter/Search), other eras → the existing `Launch("nm-connection-editor")`. **No render change** (the glyph is identical; only its click action differs by era), so no capture needed — verified by build/clippy/fmt clean + 6 panel tests green; the click→flyout itself reuses the already-verified `spawn_child` path (input-injection, not headless). The flyout it opens was real-data-verified in E15.2.
- [✓] **E15.4** Wi-Fi connect flow: inline password field in the flyout submits `nmcli device wifi connect "<ssid>" password "<key>"`; show secured padlock + signal bars per SSID. (bench: pick a known SSID, enter key, observe `nmcli connection show --active` now lists it; grim capture shows the inline password prompt and padlock glyphs.) — **Done.** Added `nm::wifi_connect(ssid, key)` (the deferred E15.1 setter, now consumed) = `nmcli device wifi connect <ssid> [password <key>]`. The flyout's Wi-Fi rows are now clickable (`Message::SelectSsid`): an **open** network connects immediately; a **secured** one reveals an **inline masked password field + Connect button** below it (Enter or the button submits → `wifi_connect` → refresh active connections). Each row shows the **padlock** + a **1–4 signal-bar** indicator (`signal_bars`, ▂▄▆█ by quartile, replacing the bare %). A `--select <ssid>` deep-link pre-selects a network (a "connect to X" entry + the capture hook). **Verified by capture:** `mde net-flyout --select FRANKS-REDHOTS` shows the inline Password field + blue Connect button under the secured network, with padlocks + signal bars across the list. 73 tests, build/clippy/fmt clean. (Minor: a multi-AP SSID shows the field under each of its rows — harmless, connect is by SSID; the bars sit tight against the panel edge — a cosmetic follow-up.)
- [✓] **E15.5** Settings ▸ Network & Internet **Status** page in `settings/network.rs`: connection diagram driven by the live active connection (`nmcli -t -f NAME,TYPE,DEVICE,STATE connection show --active`), active-connection Properties row with Private↔Public (firewalld zone via `nmcli connection.zone`), advanced-settings handoff to `nm-connection-editor`. (`mde settings --page network` no-panic launch; grim capture shows the status diagram reflecting the real active connection + Private/Public toggle; toggling flips `nmcli -g connection.zone` between `home`/`public`.) — **Done (built into the existing Settings shell — the Network & Internet category's "Connections" page became native, not a separate `settings/network.rs`, same §6 reconciliation as E13.2).** The page (`Kind::NetworkStatus`) shows a **connection card** (wifi glyph + "Connected — \<name\>" + "\<type\> on \<device\>", or "Not connected") from `nm::active_connections()` read at settings start, a **"Make this a private (trusted) network"** checkbox driving the active connection's firewalld zone via `nm::set_zone`/`connection_zone` (Private→`home`, Public→`public`), and a **"Change advanced sharing options"** button → `nm-connection-editor`. **Verified by a real-data capture:** `mde settings network` shows "Connected — FRANKS-REDHOTS · 802-11-wireless on wlp3s0" + the private-network checkbox + Advanced button. 73 tests, build/clippy/fmt clean. (The zone-toggle's live nmcli write needs a real connection/auth, not headless — the read + render + wiring are covered.)
- [✓] **E15.6** **Wi-Fi** page: full SSID list with forget/connect + an **auto-connect to known networks** toggle (maps to NM `connection.autoconnect`). (grim capture shows the SSID list with the auto-connect toggle in Win10 dark + light variants; bench: toggle flips `nmcli -g connection.autoconnect <c>` between `yes`/`no`.) — **Done (connect + auto-connect; forget split to [[E15.6a]]).** The Network & Internet category's "Wi-Fi" page is now native (`Kind::Wifi`): an **"Automatically connect to known networks"** checkbox over the **async-scanned SSID list** (`wifi_scan_task` runs `nmcli dev wifi` off-thread, like the update check, so settings doesn't block — the scan fires lazily via `maybe_load` on first show). Each row = ssid + 🔒 + signal % + a **Connect** button that hands the key prompt to the flyout's connect flow (`mde net-flyout --select <ssid>`, reusing E15.4 rather than duplicating the inline-password state). The toggle drives new `nm::set_wifi_autoconnect` / `wifi_autoconnect` over the saved Wi-Fi connections (`parse_saved_wifi`, unit-tested). **Verified by a real-data capture:** `mde settings network --page wi-fi` shows the toggle + the host's live scan (FRANKS-REDHOTS 99%, Fios, Verizon_*, NORAD, …) with Connect buttons. 74 tests, build/clippy/fmt clean. The autoconnect nmcli write needs saved connections (not headless).
- [✓] **E15.6a** Wi-Fi page **Forget** (split from E15.6, which shipped connect + auto-connect): a **Forget** button on rows whose SSID matches a saved connection (`nm::saved_wifi`), running `nmcli connection delete <name>`; show Forget (for saved) vs Connect (for unsaved) per row. Needs the saved-vs-scanned SSID match on the row, distinct from E15.6's flat Connect list. — **Done.** The Wi-Fi page reads `nm::saved_wifi` (alongside the scan, in `maybe_load`) into `wifi_saved`; each scanned row now shows **Forget** (`Message::ForgetSsid` → new `nm::forget_wifi` = `nmcli connection delete`, then a re-scan so the row flips back to Connect) when its SSID matches a saved connection, else **Connect**. **Verified by a real-data capture:** the host's saved/connected network (FRANKS-REDHOTS, all three APs) shows **Forget**, while unsaved networks (Fios, Deimos, Verizon) show **Connect**. 74 tests, build/clippy/fmt clean. **🎯 This closes the entire E15 Networking epic (E15.1–E15.12 + E15.6a): the `nm` data layer, the net-flyout + connect flow + panel routing, and all nine Settings ▸ Network & Internet pages are done.**
- [✓] **E15.7** **Ethernet** + **VPN** pages: Ethernet shows wired connection + Public/Private; VPN lists `nmcli` vpn/wireguard connections with up/down switches and an "Add VPN" button that spawns `nm-connection-editor`. (grim capture of both pages; bench: a `vpn up`/`down` round-trips and the page reflects the new active state.) — **Done.** Added an **Ethernet** page (`Kind::Ethernet`) to the Network & Internet category and made **VPN** native (`Kind::Vpn`). **Ethernet:** the active wired connection (`net_conns` filtered to `802-3-ethernet`/activated) — name + device — over the Private/Public profile toggle (reuses `SetNetPrivate`/`net_zone`, E15.5), or "Not connected via Ethernet." **VPN:** `nm::vpn_list()` (read at settings start) with a per-row **Connect/Disconnect** button driving new `nm::vpn_up_down` (`nmcli connection up/down`, then re-read), and an **"Add VPN connection"** button → `nm-connection-editor`; "No VPN connections configured." when empty. **Verified by captures (both pages, real state):** on this wifi-only host the Ethernet page correctly shows "Not connected via Ethernet" and the VPN page shows the Add button + empty state, with both as native rail entries. 74 tests, build/clippy/fmt clean. The active-state round-trips (a live wired link / a configured VPN up-down) aren't on this host — render + wiring are covered. (Minor: the Ethernet toggle reuses the active connection's zone, so it's precise when the wired link is the active one.)
- [✓] **E15.8** **Mobile hotspot** page: "Share my Internet connection" toggle driving `nmcli device wifi hotspot`, with an **Edit** modal (name/password/band) persisting to `state.rs`; same fn callable from the E3 Action-Center tile. (bench: enable toggle → `nmcli connection show --active` lists `Hotspot`; grim capture shows the Edit modal; disable round-trips clean.) — **Done (inline edit, not a modal — settings.rs is a flat shell; band defaulted).** Added a **Mobile hotspot** page (`Kind::Hotspot`) to the Network & Internet category: a **"Share my Internet connection with other devices"** checkbox whose on-state is the live "Hotspot" connection, driving new **`nm::set_hotspot(on, ssid, key)`** (`nmcli device wifi hotspot [ssid…password…]` / `connection down Hotspot`), over inline **Network name** + masked **Network password** fields persisted to new `state.hotspot_name`/`hotspot_password` (serde defaults `"MackesDE"`/empty; roundtrip green). `set_hotspot` is a shared `nm` fn the E3 Action-Center tile can also call. **Verified by capture:** seeded `hotspot_name:"MackesDE-AP"` + a password render in the fields, toggle off, with Mobile hotspot a native rail entry. 74 tests, build/clippy/fmt clean. (Band not exposed — nmcli's default; the actual AP enable needs a Wi-Fi device with hotspot support, not headless. The `nmcli connection show --active` round-trip is the real-hardware bench.)
- [✓] **E15.9** **Proxy** page: manual address/port/bypass written via `gsettings set org.gnome.system.proxy*`, plus an auto/off mode selector. (bench: set a manual proxy, confirm `gsettings get org.gnome.system.proxy mode` == `manual` and host/port match; grim capture of the populated form.) — **Done.** The "Proxy" page is now native (`Kind::Proxy`): an **Off / Manual / Automatic** `ProxyMode` picker → `nm::set_proxy_mode` (`gsettings set org.gnome.system.proxy mode`), and under **Manual** an **Address + Port + Apply** that writes `org.gnome.system.proxy.http host/port` via new `nm::set_proxy_http` (port digit-filtered). Read live at settings start (`proxy_mode` + `proxy_http`, via shared `gget`/`gset` helpers). **Verified by capture:** the page shows the mode picker (Off, this host's live value); the Manual fields are conditional (`mode == Manual`) — code-verified, not shown headlessly because dconf needs a dbus service (a temp-`XDG_CONFIG_HOME` seed didn't stick). 74 tests, build/clippy/fmt clean. **This completes the Network & Internet category — all six pages (Status, Wi-Fi, Ethernet, VPN, Mobile hotspot, Proxy) are now native, no greyed entries.** Remaining E15: [[E15.6a]] (forget), E15.10 (Airplane page), E15.11 (Data usage), E15.12 (cellular grey + Win10 pins).
- [✓] **E15.10** **Airplane mode** page + master toggle wiring `rfkill block/unblock all` and a per-radio Wi-Fi toggle (`nmcli radio wifi`); state read from `rfkill -no`. (bench: toggle on → `rfkill -no SOFT,TYPE` shows wlan blocked, panel glyph shows wifi-off; toggle off restores; grim capture of the page.) — **Done (lean — reuses the existing E15.1/E15.2 nm helpers, no new ones).** Added an **Airplane mode** page (`Kind::Airplane`) to the Network & Internet category: a master **Airplane mode** checkbox (`nm::set_airplane` = `rfkill block/unblock all`, read via `airplane_on`/`parse_airplane`) over a **Wireless devices ▸ Wi-Fi** toggle (`nm::radio_wifi`, read via `wifi_enabled`) that's **disabled while airplane mode is on** (Win10 behaviour). State is read live on each visit via `maybe_load` (cheap sync, no init bloat). **Verified by capture (real state):** Airplane mode off, Wi-Fi on — matching this host's live connection. 74 tests, build/clippy/fmt clean. (The live block/unblock round-trip mutates the host's radios, so it's the real-hardware bench, not run headlessly.)
- [✓] **E15.11** **Data usage** page: per-device rx/tx totals from `/sys/class/net/*/statistics`, a persisted data-limit field (`#[serde(default)]` in `state.rs`, `Default` agrees with `parse("{}")`), and a threshold-exceeded freedesktop notification through `notifyd` (E3). (grim capture shows usage bars; bench: set a tiny limit, exceed it, observe a dbus notification round-trip via the E3 daemon; `cargo test` enforces the new state field default.) — **Done.** Added a **Data usage** page (`Kind::DataUsage`): per-device **rx+tx** from new `nm::all_device_bytes` (`/sys/class/net/*/statistics`, lo skipped, reuses `device_bytes`), each a **proportional accent bar** (vs the busiest device) + human-readable bytes, with a "Total since boot" line and an editable **Monthly limit (MB)** field persisted to new `state.data_limit_mb` (`#[serde(default)]` 0; Default == `parse("{}")` green). Setting the limit below current usage fires a **`notify-send`** "Data limit reached" (the E3 daemon renders it). Read live on each visit (`maybe_load`). **Verified by a real-data capture:** "Total since boot: 11.3 GB" with `wlp3s0` (wifi) at 11.3 GB / full bar and `enp0s31f6` (wired) at 0 B, plus the seeded 5000 MB limit field. 74 tests, build/clippy/fmt clean. The notification round-trip needs a live notify daemon (the bench). **Remaining E15: E15.12 (cellular grey + Win10 pins) + [[E15.6a]] (Forget) — then the epic closes.**
- [✓] **E15.12** Grey out the cellular/eSIM/data-plan surface to a single disabled "No cellular hardware detected" row (mirroring §3 greying of non-functional toggles); pin `Theme::Windows10` network-page palette sentinels in `mde-ui/tests/checklist.rs`. (grim capture shows the greyed cellular row; `cargo test -p mde-ui` green with the new Win10 assertions.) — **Done.** Added a **Cellular** page (`Kind::Cellular`): a **disabled** "Cellular" checkbox (no `on_toggle`, like the labwc-managed taskbar toggles) + a **"No cellular hardware detected."** advisory — §3-honest greying, not an inert mockup. Added **`windows10_network_palette_pins`** to `checklist.rs` pinning the roles the Win10 net surfaces paint with (the **accent** for flyout pills / Wi-Fi & data-usage bars / selection = `carbon_accent`, plus the `WINDOW`/`MENU`/`GRAY_TEXT` neutrals) under `Theme::Windows10` (== Carbon, guard-held). **Verified by capture:** the greyed Cellular row renders; the Network rail now lists all **9 native pages**. 74 mde + 19 mde-ui tests green, build/clippy/fmt clean. **This closes the E15 page work — the whole Settings ▸ Network & Internet category + the flyout + the `nm` data layer are done; only [[E15.6a]] (the Wi-Fi Forget split) remains in E15.**

### M3 — productivity + storage + polish

#### E16 Clipboard History + Snip & Sketch (Win+V / Win+Shift+S / PrintScreen)

- [✓] **E16.1** Add `mde clipboard` layer-shell history popup (clipboard.rs) under Theme::Windows10, reusing the popup.rs scaffold; rows from the persisted index, click re-copies via wl-copy and shows a "Copied" toast. (grim capture of `mde clipboard` shows a flat Win10 history card with text + image-thumbnail rows; `timeout 3 ./target/debug/mde clipboard` no-panic.) — **Done.** `mde clipboard` (no subcommand) now opens a layer-shell history popup (a `popup` submodule in `clipboard.rs`, the net_flyout/popup.rs scaffold): a flat dark Win10 card anchored **bottom-left above the taskbar** ("Clipboard" heading + a scrollable list), reading `load_index()` (E16.2). Each row is a bordered, clickable `mouse_area` — **text** rows show the snippet (newlines flattened, truncated to 48 chars with "…"); **image** rows render an `image()` thumbnail of the PNG blob. Clicking re-copies via `copy_entry` (text on `wl-copy` stdin / the blob as `image/png`), fires `mde toast "Copied"`, and closes; Esc closes. **Win10-gated** (classic eras print a pointer and exit — verified). The popup also spawns the daemon (idempotent) so it works on first open; the daemon is now in the **labwc autostart** + a **Win+V** rc.xml keybind (inert under non-Win10). Capture (seeded index) shows the card with 4 text rows incl. the truncated long entry; `timeout 3 mde clipboard` no-panic. 98 mde tests green; clippy/fmt clean. **E16 clipboard half done** (E16.3 pin/delete + E16.4 snip remain).
- [✓] **E16.2** Add the `mde clipboard daemon` watcher: wl-paste --watch for text + image/png appending to a 25-entry ring at ~/.local/share/mde/clipboard/ via state.rs atomic save, lockfile-idempotent. (Bench: `wl-copy hello` then inspect the JSON index shows the new entry; a second daemon launch exits without a second watcher.) — **Done.** New `clipboard.rs`: a `ClipEntry {Text, Image}` ring at `~/.local/share/mde/clipboard/index.json` (atomic temp-file+rename write, honours `$XDG_DATA_HOME`), capped at `RING`=25, with a pure unit-tested `ring_push` (most-recent-first, dedups a re-copied item to the front, truncates to 25). `mde clipboard daemon` spawns two `wl-paste --watch` watchers — text (`__add`) and image/png (`__add-image`, writing the PNG bytes to an `img-N.png` blob via a monotonic counter file, no clock/RNG) — and is **lockfile-idempotent**: a second launch sees the live PID in `daemon.lock` and exits. `mde clipboard --list` dumps the history. **Verified in nested sway:** `wl-copy "hello world"` then `wl-copy "second item"` → `--list` shows both most-recent-first; a second `clipboard daemon` prints "already running (pid …)" and exits. 97 mde tests green (incl. the ring test); clippy/fmt clean. The history popup (**E16.1**) reads this index; the session autostart of the daemon rides with it.
- [✓] **E16.3** Pin / delete / "Clear all" on history rows, persisted to the index and rebuilt in-place; empty-state when no entries. (grim capture shows a pinned row surviving "Clear all" while unpinned rows vanish; relaunch shows the same pinned set.) — **Done.** `ClipEntry` became a struct `{kind: ClipKind, pinned: bool}` (was a bare enum), so each entry carries a pin. Each popup row now has a **pin toggle** (a thumbtack glyph, **accent-coloured when pinned**) + a **delete** ✕, and the header gained **Clear all**. The handlers mutate the on-screen list and **persist in place** via `save_index`: TogglePin flips the flag, Delete drops the row, **Clear all retains only pinned**. `ring_push` was rewritten to dedup by *content* (a re-copied pinned item stays pinned) and to **cap so pinned entries are never dropped** (budget = RING − pinned count; order preserved) — unit-tested by `pinned_entries_survive_the_cap` (1 pinned + RING+5 unpinned → the pin survives, rest capped to RING−1). Empty-state line when no entries; `--list` now marks `[pinned]`. Capture (seeded) shows four rows with pin/delete affordances — the pinned row's thumbtack accent-blue, the rest grey — plus Clear all. The interactive Clear-all click + relaunch is the un-headless part; the retain-pinned + cap logic is unit-tested and the persistence is the atomic write. 99 mde tests green; clippy/fmt clean. **E16 clipboard fully done** (E16.1–E16.3); only E16.4 (snip) remains.
- [✓] **E16.4** Add `mde snip` (snip.rs) rect mode under Theme::Windows10: slurp region -> grim -g -> PNG in ~/Pictures/Screenshots/ AND wl-copy --type image/png. (Bench: run `mde snip rect`, drag a region; a Screenshot_*.png appears and `wl-paste --type image/png` returns the bytes.) — **Done.** New `snip.rs` + `mde snip` dispatch: **rect** mode (default) picks a region with `slurp`, **full**/**screen** grabs the whole output; either way it runs `grim [-g <region>] <path>` into `~/Pictures/Screenshots/Screenshot_<epoch>.png`, then **`wl-copy --type image/png`** (so it pastes *and* the clipboard daemon E16.2 records the image) + a `notify-send`. **Win10-gated** (classic eras print a pointer and exit — verified); a cancelled `slurp` exits cleanly. Wired: the **Win+Shift+S** rc.xml keybind (the authentic Snip shortcut reserved back in E14.10) → `mde snip rect`, the `mde-snip` `%post` symlink, and the help line. `shot_name` is unit-tested. **Verified headless** (`mde snip full` in nested sway): a valid 5731-byte RGB PNG lands in `~/Pictures/Screenshots/` and `wl-paste --type image/png` returns the same 5731 bytes — both halves of the bench (the `rect` region picker is the same pipeline behind an interactive `slurp`). 100 mde tests green; clippy/fmt clean. **This completes the E16 Clipboard/Snip epic** (E16.1–E16.4).
- [✓] **E16.5** Add clip (clipboard-only PRTSCN), full (WINKEY+PRTSCN whole-output), and window (ALT+PRTSCN: raise the focused toplevel via wlr.rs focus() then grab via slurp's geometry-free drag — the foreign-toplevel protocol exposes no window rect) snip modes through one shared capture(mode) fn; never capture the pointer. (Bench: `mde snip full` writes a full-screen PNG; `mde snip clip` puts an image on the clipboard with NO new file; `mde snip window` raises the focused window and produces a single-window PNG from a slurp drag.) — **Done.** `snip.rs` refactored to one shared `capture(Mode)` over a `Mode {Rect, Full, Clip, Window}` enum (`mode_from_args`, pure + unit-tested for all five tokens incl. flag-skipping). **rect** → slurp+grim+save; **full**/**screen** → whole output save; **clip** → `grim - | wl-copy --type image/png` piped (process `Stdio`, **no file** written); **window** → `crate::wlr::start()` + find the `focused` toplevel + `Wm::focus(id)` to raise it (foreign-toplevel has no rect), then a geometry-free slurp drag. The pointer is never captured (grim excludes the cursor unless `-c`, never passed). **Bench (nested sway, Win10, temp HOME):** `mde snip full` wrote exactly one `Screenshot_*.png` + printed its path; `mde snip clip` left the Screenshots count **unchanged** (0 new files) yet put **`image/png`** on the clipboard (`wl-paste --list-types`). `window` mode's slurp drag is interactive (can't complete headlessly, same as the shipped `rect`); its raise+slurp path is built from the established `Wm::focus`. 127 mde tests green (2 in `snip.rs`); clippy/fmt clean. The W-Print/A-Print/Print keybinds are **E16.7** (not this task); the era self-gate already lived in `run`.
- [ ] **E16.6** Wire the snip success toast via the E7 notification daemon: title "Snip saved", body=filename, thumbnail icon, Open + Copy-again actions. (dbus round-trip: a notify-send-equivalent call from snip raises a toast whose Open action launches the file in the image viewer.) — **Deferred from the autonomous drain (non-silent, §3/§6): needs the notification-action bridge first, which doesn't exist.** `notifyd.rs` defines an `action_invoked` D-Bus signal but **nothing emits it**, and `toast_view` renders **no action buttons** (`action_center.rs` itself flags "inline notification actions … need a daemon action bridge"). So "a toast whose Open action launches the file" can't be honored without first building that bridge end-to-end: (a) action buttons in the toast, (b) a daemon `InvokeAction(id,key)` method that emits `action_invoked`, (c) a per-sender `ActionInvoked` listener (snip's `Open`→xdg-open / `Copy again`→wl-copy, since the freedesktop model has the *sender* own its actions — not the daemon running arbitrary commands). That's a cross-cutting E3 feature (benefits all notifications), and its end-to-end round-trip **can't be verified on this host** — mde's `notifyd` can't claim `org.freedesktop.Notifications` while the session's daemon (swaync) owns it, so it needs a nested `dbus-run-session` harness. Better as a focused effort than a single-commit drain item; the plain `notify-send "Screenshot saved"` toast from E16.4 still fires meanwhile.
- [✓] **E16.7** Add W-v, W-S-s, Print, W-Print, A-Print keybinds to skel + assets labwc rc.xml, and add clipboard/snip to the mde/Cargo.toml %post symlink loop so mde-clipboard/mde-snip resolve via argv[0]. (Bench: mde-clipboard and mde-snip symlinks dispatch the right subcommand; in a Win10-era labwc session W-S-s launches slurp.) — **Done.** W-v (clipboard) + W-S-s (snip rect) keybinds and the `clipboard`/`snip` entries in the `%post` symlink loop already shipped (E16.2/E16.4); this adds the **PrintScreen family** to the skel `rc.xml`, mapped to the E16.5 modes: **Print → `mde snip clip`** (clipboard, no file), **W-Print → `mde snip full`** (save whole screen), **A-Print → `mde snip window`** (focused window). **Bench:** local `mde-snip`/`mde-clipboard` symlinks dispatch correctly via argv[0] (each printed its own subcommand's Win10-era stderr notice under Carbon). §6 reconciliation on "skel **+ assets** rc.xml": **`assets/labwc/rc.xml` is dead** — it is NOT in the RPM asset list (the shipped one is `skel/.config/labwc/rc.xml` → `/usr/share/mde/skel/...`, Cargo.toml:96), is referenced by no code, and is stale (last touched at E19, already missing the libinput block + W-v/W-S-s/W-p). Adding a keybind to an uninstalled file would be busywork, so the keybinds go to the **live skel only**; the dead `assets/labwc/rc.xml` is flagged for removal (operator-gated, §0.5 destructive). Live labwc keybind firing isn't harness-verifiable (no labwc in nested sway), same caveat as E12.6.
- [✓] **E16.8** Self-gate both subcommands on palette::theme(): outside Theme::Windows10 they print a one-line stderr notice and exit(0) with no surface. (Bench: with Carbon active, `timeout 3 ./target/debug/mde clipboard` exits 0 immediately and renders no window.) — **Reconciled ✓ (already done — Phase-0 catch).** Both subcommands already self-gate: `snip.rs` (`run`: `if !palette::is_windows10() { eprintln!("mde snip: the snipping tool is a Windows 10-era surface."); return ExitCode::SUCCESS }`) and `clipboard.rs` (the popup path: `if !palette::is_windows10() { … return SUCCESS }`). **Verified** this turn via the E16.7 symlink bench under the default Carbon theme: `mde-snip` and `mde-clipboard` each printed their one-line era notice and exited 0 with no surface. No code change — closing the ledger.

#### E17 Storage / Backup / Recovery (Settings ▸ System ▸ Storage; Recovery)

- [✓] **E17.1** Add `Theme::Windows10` to `palette.rs` `enum Theme` (atom value 3), a `win10(rgb)` remap modeled on `carbon()` with NEW Win10 role constants (page/card surfaces, the Win10 system-accent blue as a real UI-accent role, and a `RESTORE_PRIMARY` success-green), a `win10()` arm in `color()`, and a `"windows10" => Theme::Windows10` arm in `main.rs` startup — additive, Carbon stays default; no raw hex leaves `palette.rs`. (acceptance: `cargo test -p mde-ui` green incl. a new checklist assertion pinning the Win10 accent + `RESTORE_PRIMARY` roles; `cargo build` clean.) — **Reconciled ✓ (superseded, Phase-0 catch).** `Theme::Windows10` exists (atom 3) + the `"windows10" => Theme::Windows10` startup arm is live (`main.rs`), so the theme goal is met. But the **separate `win10()` remap + `RESTORE_PRIMARY`/Win10-accent roles + the checklist pin were deliberately NOT built**: the later MackesDE rebrand collapsed Win10 onto Carbon's palette (`color()` routes `Theme::Carbon | Theme::Windows10 => carbon(rgb)` — Win10 = Carbon verbatim, per-era accent retired). Authority §6 (newer lock wins): the rebrand supersedes this task's body. Marking ✓ so the drain stops re-attempting an obsolete spec; the success-green is covered by the E14.2 `STATUS_OK` role instead.
- [✓] **E17.2** Create `settings.rs` with `mde settings [page]` dispatch (main.rs arm + `%post` `mde-settings` symlink + labwc `WIN+I` keybind) — a Win10 modern-Settings xdg-toplevel home/nav shell reusing `control_panel.rs` scaffold; Storage/Backup/Recovery nav entries route to their pages. (acceptance: `timeout 3 ./target/debug/mde settings` no-panic launch; grim capture under `Theme::Windows10` shows the left nav rail + category grid.) — **Reconciled ✓ (done, Phase-0 catch).** `settings.rs` exists and is the live Win10 Settings shell built out across this session: `"settings" => settings::run` dispatch (`main.rs:167`), `mde-settings` in the `%post` symlink set (`Cargo.toml:150`), `W-i` keybind in the skel `rc.xml`, and the home **category grid + per-category left nav rail** (`category()`/`rail_entry()`), with Storage/Backup nav living under the System category (deferred pages until E17.4/E17.6). Verified non-panic + captured under Win10 many times (every Devices/Network/Accounts/etc. page this session renders through this shell). No code change needed — closing the ledger.
- [✓] **E17.3** Add `sysinfo::storage_usage()` — headless parser returning per-mount used/free + per-category bytes (Apps/Documents/Pictures/Videos/Temporary/System) from `df`/`statvfs`/`du` over XDG dirs; unit-tested with fixture strings (no iced dep). (acceptance: `cargo test -p mde` covers the parser; `mde settings storage --list` prints a real breakdown for the live root device.) — **Done.** Added to `sysinfo.rs` (the iced-free data layer): `parse_df` (pure — parses `df -B1 --output=source,size,used,avail,target`, keeps spaced mountpoints; unit-tested) + `parse_rpm_sizes` (sums `rpm -qa --qf %{SIZE}`, ignores junk; unit-tested) + `human_bytes` (binary units; unit-tested). `mounts()` keeps real `/dev/*` filesystems; `category_bytes(root_used)` measures **Apps** (rpm footprint), **Documents/Pictures/Videos** (`du -sb` over `$HOME`), **Temporary** (`~/.cache`), and **System & reserved** = the root `used` remainder (saturating, never negative). `storage_usage()` → `StorageUsage {mounts, categories}`. Reachable via **`mde settings storage --list`** (a headless path checked before the generic `--list` category map). **Verified live on this host:** Drives — `/dev/sda3 14.8 GB / 235.9 GB (6%) on /` + /boot, /home, /boot/efi; Categories — Apps 7.3 GB, Pictures 60.6 KB, Temporary 1.3 GB, System 6.1 GB (7.3+1.3+6.1 ≈ the 14.8 GB used — the remainder reconciles). 130 mde tests green (3 new pure-parser tests); clippy/fmt clean. The Storage **page** (proportional bars + Storage Sense + Apps drill-in/uninstall) is **E17.4**, which consumes this.
- [✓] **E17.4** Build the Storage page: Storage Sense toggle (persisted in `state.rs`, drives a systemd user timer), "This PC" category bars via `storage_usage()` + the Win10 accent role, "View storage usage on other drives", and an Apps & features drill-in listing installed packages via `fedora.rs` detection helpers with a NEW `pkexec dnf remove` uninstall (behind `dialogs.rs` confirm) and a greyed-disabled Move button. (acceptance: grim capture of `mde settings storage` shows proportional usage bars matching `df` totals + the Storage Sense toggle; bench: `mde settings storage --apps` lists installed packages and `--remove <pkg>` prints the exact `pkexec dnf remove` command in dry-run.) — **Done.** `Kind::Storage` page (System ▸ Storage, was Deferred), loaded off-thread (`storage_load_task` → `sysinfo::storage_usage` from E17.3). **"This PC" header** + **per-category bars** (`usage_bar`: a Win10-**accent** fill on a muted track), proportional to the root **used** space so they compose what's in use (§6 read of "matching df totals" — Win10 shows the breakdown of *used* storage; capture shows Apps ~48% / System ~43% / Temp ~9% summing to the 14.9 GB used). **Storage Sense** checkbox persists `storage_sense` and `apply_storage_sense` writes `~/.config/systemd/user/mde-storage-sense.{service,timer}` + `systemctl --user enable/disable --now` — the service cleanup is **inline** (purge `~/.cache/thumbnails` + Trash), so the timer is **self-contained and decoupled from E17.5's `--clean-now`** (`storage_sense_units` unit-tested). **Other drives** list (the non-root mounts). **Apps & features drill-in** (`storage_apps` sub-view): the installed packages **largest-first** (`fedora::installed_packages`/`parse_packages`, unit-tested), each with a **greyed Move** (no backend) + **Uninstall** → an inline confirm (the accounts-page pattern) → `pkexec dnf remove` (`fedora::dnf_remove_cmd`, unit-tested) then re-read. **Bench (live):** `mde settings storage --apps` → packages largest-first (libreoffice-core 288 MB, qt6-qtwebengine, firefox…); `mde settings storage --remove htop` → `pkexec dnf remove -y htop`. **Captures (Win10):** the main page (accent bars + Storage Sense + other drives) and the Apps drill-in (greyed Move + Uninstall + scroll). Added a `mde settings storage` shortcut → System ▸ Storage (a page, not a category) + an `MDE_STORAGE_VIEW=apps` capture seam. 133 mde tests green (4 new pure tests); clippy/fmt clean. Live `systemctl --user enable` of the timer wasn't run on the host (avoids mutating the session's systemd) — the unit content + the wiring are unit/string-verified.
- [✓] **E17.5** Build "Configure Storage Sense / Clean now": sub-page with a Clean now button shelling real cleanup (`pkexec dnf clean all`, thumbnail/Trash purge, `journalctl --vacuum-time`) through a `dialogs.rs` confirm, reporting freed bytes via toast. (acceptance: `mde settings storage --clean-now` runs the backend and prints freed bytes; bench: a no-op dry-run path returns success without root in CI.) — **Done.** `sysinfo::clean_now(dry) -> u64`: measures the thumbnail-cache + Trash size, and when **not** dry purges them (user-level, no root) then reclaims system space with one `pkexec sh -c 'dnf clean all; journalctl --vacuum-time=7d'`, returning the **freed bytes** as the root-device df delta floored at the user-level estimate. **`dry == true` deletes nothing and runs no privileged step** — it returns the would-free estimate (the CI-safe path). Reachable via **`mde settings storage --clean-now [--dry-run]`** — **verified live**: `--clean-now --dry-run` → `would free 1.4 MB`, exit 0, **no root, thumbnails untouched** (1.6M before == after). GUI: a **Clean-now sub-view** (`storage_clean`, reached from the main page's "Configure Storage Sense or run it now" button) — "Free up space now" + the cleanup description + a **Clean now** button → an inline **confirm** ("This empties the Trash.", the accounts-page pattern) → `ConfirmClean` runs `clean_now(false)` off-thread → a **"Freed X" toast** (`notify-send`) + a `STATUS_OK` "Freed X." line + a re-read of the breakdown so the bars update. Capture (Win10, `MDE_STORAGE_VIEW=clean` seam): the sub-view with the description + Clean now button. 133 mde tests green; clippy/fmt clean. The **real** `--clean-now` (destructive purge + `pkexec dnf clean`/journal vacuum) wasn't run on the host — verified by the dry-run + construction; the inline Storage-Sense timer cleanup (E17.4) covers the scheduled path.
- [✓] **E17.6** Build the Backup page over Timeshift: "Add a drive" (candidates from `storage_usage()`), persist `backup_drive` + auto-backup toggle in `state.rs`, configure Timeshift snapshot device via `pkexec timeshift`. (acceptance: grim capture of `mde settings backup` shows the drive picker; bench: `mde settings backup --add-drive <dev>` writes the chosen device into `menu.json` and round-trips through `state::load`.) — **Done.** `Kind::Backup` page (Update & Security ▸ Backup, was Deferred), reusing the E17.3 `storage_usage()` mounts (loaded off-thread) for its **candidate-drive list** — one row per distinct backing device with free/total + mountpoint + a **"Use this drive"** button → `SetBackupDrive` persists `backup_drive` to menu.json **and** points Timeshift at it off-thread (`pkexec timeshift --snapshot-device <dev>`, `sysinfo::timeshift_device_cmd`, unit-tested). When a drive is set the page shows "Backing up to <dev>" (`STATUS_OK`) + an **automatic-backup** checkbox (`auto_backup`) + "Remove drive". State: `backup_drive`/`auto_backup` (menu.json). **Bench (live):** `mde settings backup --add-drive /dev/sdb1` → prints `backup drive set to /dev/sdb1` + the `pkexec timeshift --snapshot-device /dev/sdb1` it would run, and writes `"backup_drive": "/dev/sdb1"` to menu.json; a **second** `--add-drive /dev/nvme0n1p3` reads-then-rewrites it (round-trips through `state::load`). Added a `mde settings backup` shortcut → Update & Security ▸ Backup (generalised the `storage` shortcut). Capture (Win10): the drive picker with /dev/sda3/sda2/sda1 candidates + "Use this drive". 134 mde tests green; clippy/fmt clean. The privileged `timeshift --snapshot-device` wasn't run on the host (the CLI prints it / the GUI pkexecs it) — verified by the command builder + the menu.json round-trip.
- [✓] **E17.7** Build Backup "More options": Back up now (`timeshift --create`), schedule dropdown writing the systemd timer `OnCalendar=`, retention dropdown, and the included-folders add/remove list seeded from XDG dirs via `filedialog.rs`. (acceptance: `mde settings backup --backup-now` creates a Timeshift snapshot (or prints the exact pkexec command in dry-run); grim shows schedule/retention dropdowns with Hourly/Forever defaults selected.) — **Done.** A **More options** sub-view of the Backup page (`backup_more`, reached from the "More options" button when a drive is set). **Back up now** → inline confirm → `pkexec timeshift --create` off-thread → toast. CLI **`mde settings backup --backup-now [--dry-run]`** — **verified**: `--dry-run` → `pkexec timeshift --create`, exit 0 (CI-safe, no root). **Schedule** pick_list (Hourly/Daily/Weekly/Monthly, **default Hourly**) → `apply_backup_schedule` writes + enables a `--user` `mde-backup.timer` with `OnCalendar=<schedule>` (the Storage-Sense pattern; service runs `mde settings backup --backup-now`; `backup_schedule_units` unit-tested). **Retention** pick_list (Keep forever / Last 10 snapshots / Last 30 days, **default Keep forever**), persisted. **Included folders** list **seeded from the XDG user dirs** (`seed_backup_includes` — Documents/Pictures/Videos/Music/Downloads that exist), each removable, with **+ Add a folder** → `pick_folder_task` spawns `mde filedialog` and adds the returned path. State: `backup_schedule`/`backup_retention`/`backup_includes` (menu.json; includes seeded in the UI when empty so menu.json stays clean). Capture (Win10, `MDE_BACKUP_VIEW=more` seam): Back up now + **Hourly** + **Keep forever** dropdowns + the seeded folder list + Add/Remove. 134 mde tests green; clippy/fmt clean. Unattended timer runs need a polkit rule for timeshift (noted in the unit doc) — the manual Back up now works with the password prompt; the privileged create wasn't run on the host (dry-run + construction).
- [✓] **E17.8** Build the File History restore browser (`mde settings backup --restore`): `files.rs`-style snapshot browser with bottom prev/next time-navigation, Details/Large-icon toggle, multi-select, a "Restore to original location" primary button colored from the `RESTORE_PRIMARY` palette role + Options-gear "Restore to…" via `filedialog.rs`, all behind `dialogs.rs` replace-confirm. (acceptance: `timeout 3 ./target/debug/mde settings backup --restore` no-panic launch; grim capture shows the time-navigation buttons + the green (`RESTORE_PRIMARY`) restore button over a real `timeshift --list`.) — **Done (core; multi-select + Options-gear split to E17.8a).** New `restore.rs` iced window (`iced::application`, the `files.rs` pattern), launched by `mde settings backup --restore`. `sysinfo::snapshots()` (`MDE_TIMESHIFT_FIXTURE` seam else `timeshift --list`, newest-first) + pure `parse_snapshots` (unit-tested) feed a snapshot list with a **Details / Large-icon** view toggle, bottom **< Older / Newer >** time-navigation (steps the selection through time), and a green **"Restore to original location"** button — colored from **`STATUS_OK`** (§6/§17.1 reconcile: the planned `RESTORE_PRIMARY` was collapsed onto the existing success-green by the MackesDE rebrand) — behind an **inline replace-confirm** (the `dialogs.rs` confirm reconciled to an in-app overlay, as elsewhere this session) → `pkexec timeshift --restore --snapshot <name> --yes` (`timeshift_restore_cmd`, unit-tested). **§6 reconciliations** (noted in the module doc): Timeshift restores the **whole system** from a point in time, so the browsable items are snapshots (restore points), not per-file entries; `timeshift --list` needs root, so an unprivileged session shows an empty list + an admin hint (`MDE_TIMESHIFT_FIXTURE` populates captures). **Verified:** `timeout 3 mde settings backup --restore` no-panic (Carbon → era-gated early exit; Win10 → the window); capture (Win10, fixture) shows the **time-nav buttons + the green restore button** over a 3-snapshot list. 135 mde tests green; clippy/fmt clean. **Split to E17.8a:** snapshot **multi-select** + the Options-gear **"Restore to…"** (a filedialog-chosen target) — not in the acceptance benches, layered on the working browser.
- [✓] **E17.8a** Backup restore browser — multi-select + Options-gear "Restore to…": add snapshot multi-select to `restore.rs`, and an Options-gear that opens `filedialog.rs` to pick a restore **target** (timeshift `--target-device`/restore-to), behind the same replace-confirm. (Split from E17.8, which shipped the single-snapshot restore-to-original core + the time-nav/view-toggle chrome.) — **Done (reconciled to Timeshift's whole-system model).** The **"Restore to…"** target picker is a pick_list of **"Original location" + each distinct device** (`restore_targets` over `sysinfo::mounts`) beside the green Restore button; the button label + the replace-confirm prompt update to the chosen target, and `ConfirmRestore` runs `timeshift --restore --snapshot <name>` for the original location or **`--target-device <dev>`** for a device (`sysinfo::timeshift_restore_to_cmd`, unit-tested). **§6 reconciliations** (why the spec couldn't be taken literally): Timeshift restores the **whole system** from a point in time, so (a) `--target-device` takes a **device**, not a folder — a `filedialog.rs` folder pick doesn't map, so the Options "Restore to…" is a **device pick_list**; and (b) **multi-select is N/A** — there's nothing to multi-select when restoring one whole-system snapshot (the File-History per-file multi-select doesn't exist here). Capture (Win10, fixture): the **"Original location ▾"** target picker next to the green restore button. 135 mde tests green; clippy/fmt clean. The privileged restore wasn't run on the host (the command builders are unit-tested; restore needs root + a real snapshot).
- [✓] **E17.9** Build the Recovery page: Reset this PC chooser (Keep my files -> `dnf distro-sync`+reinstall keeping /home; Remove everything -> typed-confirm modal launching `mde setup`/installer, Clean data? -> documented blkdiscard warning), Advanced startup Restart now (`pkexec systemctl reboot --firmware-setup`), and Uninstall-updates (`dnf history undo last`) behind confirm. (acceptance: grim capture of `mde settings recovery` shows the two Reset modes + Restart-now button; bench: each destructive action requires a typed-confirm and prints its exact pkexec command in dry-run without executing.) — **Done.** `Kind::Recovery` page (Update & Security ▸ Recovery, was Deferred). A `RecoveryAction` enum drives four actions, each with a **title + command preview + a required confirm word**: **Keep my files** (`pkexec dnf distro-sync -y`), **Remove everything** (launches the installer `mde setup`, with the documented **blkdiscard** data-wipe warning shown in `STATUS_WARN` amber), **Restart to firmware** (`pkexec systemctl reboot --firmware-setup`), **Uninstall the latest updates** (`pkexec dnf history undo last -y`). Command builders in `sysinfo.rs` are pure + **unit-tested**. **Typed-confirm gate** (`recovery_confirm`): the red (`STATUS_RISK`) title + the exact command preview + a `text_input` where the **Confirm button stays disabled until the typed text equals the confirm word** (`RESET`/`REMOVE`/`RESTART`/`UNDO`). **Bench (live):** `mde settings recovery --reset-keep|--restart|--uninstall-updates|--reset-remove --dry-run` each print the **exact command without executing** (`pkexec dnf distro-sync -y`, `pkexec systemctl reboot --firmware-setup`, `pkexec dnf history undo last -y`, `launch the installer …`). `mde settings recovery` shortcut → the page. Captures (Win10): the page (both Reset modes + Restart-now + Uninstall) and the **typed-confirm** (red header, blkdiscard warning, disabled Confirm). 136 mde tests green; clippy/fmt clean. No destructive action was run on the host (dry-run + the typed-confirm + construction).
- [✓] **E17.10** Build the Create-recovery-drive wizard (`mde settings recovery --usb-drive`) reusing `installer.rs`/`tui_setup.rs`: pick a removable device from `storage_usage()`, "back up system files" checkbox, erase-warning confirm, then write the bootable rescue image with the installer progress widget. (acceptance: `timeout 3 ./target/debug/mde settings recovery --usb-drive` no-panic launch; grim shows the device picker + erase warning; dry-run prints the `dd`/`livecd-iso-to-disk` command without writing.) — **Done. Closes the E17 Storage/Backup epic.** A `usb_wizard` sub-view of the Recovery page (`recovery_usb`), opened by `mde settings recovery --usb-drive` (threaded through `gui(start_usb)`) or the page's "Create a recovery drive" link. It reuses the E17.3 `storage_usage()` mounts (loaded off-thread) for the **device picker** (one row per device, selected highlighted), a prominent **erase warning** (`STATUS_WARN` amber: "Everything on it will be erased."), a **"Back up system files to the drive"** checkbox, and a **Create** button → an inline **erase-confirm** showing the exact command (red `STATUS_RISK`) → `pkexec dd if=/usr/share/mde/recovery.iso of=<dev> bs=4M status=progress oflag=sync` (`sysinfo::recovery_drive_cmd`, unit-tested; `dd` used since `livecd-iso-to-disk` isn't installed). **Bench (live):** `mde settings recovery --usb-drive --device /dev/sdb --dry-run` → `pkexec dd if=/usr/share/mde/recovery.iso of=/dev/sdb …` **without writing**; `timeout 3 mde settings recovery --usb-drive` no-panic (Carbon → era-gated; Win10 → the wizard). Capture (Win10): the device picker + erase warning + backup checkbox + Create. 136 mde tests green; clippy/fmt clean. §6 reconciliations: `dd` (not `livecd-iso-to-disk`, absent) for the write; the "installer progress widget" → `dd status=progress` in the privileged step (an inline iced progress would need to stream dd's stderr — deferred); the rescue ISO is the shipped `/usr/share/mde/recovery.iso` (documented path). No write was run on the host (dry-run + the erase-confirm + construction).

#### E18 Edge -> Firefox integration (default-browser surfacing) — Win10 era

- [✓] **E18.1** Add `mde/src/browser.rs` with `default_browser()` (shells `xdg-settings get default-web-browser`, maps desktop-id → name + `firefox` icon key via a pure function, falls back to Firefox) and `launch_url()`/`launch()` reusing the existing fire-and-forget `Command::spawn` pattern. (Bench: `timeout 3 ./target/debug/mde browser-default` prints the resolved name with no panic; a unit test asserts the desktop-id→name/icon mapping — including the `firefox.desktop`→"Firefox" fallback — without touching the system default.) — **Done.** New `browser.rs`: `id_to_browser()` maps a `.desktop` id → `Browser { name, icon }` (Firefox/Chrome/Chromium/Brave/Edge known, empty/unknown → Firefox fallback, unrecognized → stem + generic `web-browser` icon); `default_browser()` reads `xdg-settings get default-web-browser` and maps it; `launch_url()` opens a URL via `xdg-open` (honours the default). Reachable via a `browser-default` subcommand (main.rs arm + USAGE): no arg → print name, `--icon` → print icon key, `<url>` → open it. **`launch()` (bare-browser open) deferred to its first consumer** (E18.5's Firefox pin spawns `firefox` directly) to avoid a §3 unused fn. Verified: `desktop_id_maps_to_name_and_icon` unit test (incl. the firefox fallback); `mde browser-default` prints "Firefox" + `--icon` prints "firefox" (exit 0, no panic) on this host.
- [✓] **E18.2** Add `browser::set_default_cmd()` (builds `xdg-settings set default-web-browser firefox.desktop` + the `xdg-mime default` calls for `http`/`https`/`text/html`) and a thin `set_default()` wrapper that runs them. (Bench: a unit test asserts the constructed command argv strings exactly — NOT that the global default actually flipped, so the test mutates no session state; `set_default()` no-panics.) — **Done.** `set_default_cmds()` returns the four (program, args) pairs — `xdg-settings set default-web-browser firefox.desktop` + `xdg-mime default firefox.desktop` for `x-scheme-handler/http`, `…/https`, `text/html` — and `set_default()` runs them (`.status()`, best-effort). Reachable via `mde browser-default --set-default`. `set_default_builds_the_exact_xdg_commands` pins the argv exactly (mutating no session state, per the bench); build/clippy/fmt clean.
- [✓] **E18.3** New `mde browser-jumplist` layer-shell surface (`browser_jumplist.rs` modeled on `popup.rs`, layer-shell only — no title bar drawn) with Tasks ("New window" `firefox --new-window` / "New private window" `firefox --private-window`) + a footer, dispatched from `main.rs`, symlinked in `%post`. (grim: `mde browser-jumplist` under the Win10 era captures a flat themed popup listing "New window"/"New private window"; `timeout 3` launch no-panics.) — **Done (reuses popup.rs's launcher, no scaffold duplication).** Rather than copy ~250 lines of layer-shell scaffold, exposed `popup::{Item (+ Item::new), sep, launch_with}` as `pub(crate)`; new `browser_jumplist.rs` just builds the item list (New Window / New Private Window → `firefox --new-window`/`--private-window`, a separator, then a **Firefox** footer that launches the browser) and hands it to `launch_with`. So it inherits the WAYLAND guard, the flat themed chrome, and the §7 era anchoring (the popup-anchoring fix → Win10 bottom-left). `main.rs` arm + USAGE; `browser-jumplist` added to both `%post`/`%postuninstall` symlink loops in `mde/Cargo.toml`. The **Recent section** (Firefox history) is [[E18.4]], inserted between Tasks and footer. Verified: `mde browser-jumplist` no-panic; a windows10 capture shows the flat bottom-left popup with New Window / New Private Window / Firefox. build/clippy/fmt clean.
- [✓] **E18.4** Recent section: read top-8 `moz_places` rows from Firefox `places.sqlite` via bundled read-only `rusqlite` (copy-to-temp + `?immutable=1` when locked), each launching its URL via `browser::launch_url`; omit the section cleanly on any read failure. Add the `rusqlite` (read-only) dependency to `mde/Cargo.toml`. (grim: with a populated profile the jump list shows recent site rows; with no profile the popup renders Tasks-only and still no-panics — bench both. `cargo build` stays clean with the new dep.) — **Done.** Added **`rusqlite = { version = "0.32", features = ["bundled"] }`** — **user-confirmed** via AskUserQuestion (it's the one C-compiled crate vs the otherwise pure-Rust text/TLS stack; `bundled` means no runtime `sqlite3` — which this host doesn't even have, so the choice was apt). `browser::recent_sites()` finds the `default` Firefox profile (`MDE_PLACES_DB` seam), **copies `places.sqlite` to a temp** and opens it `file:…?immutable=1` read-only (Firefox holds a write lock), then `SELECT url,title FROM moz_places WHERE last_visit_date IS NOT NULL AND url LIKE 'http%' ORDER BY last_visit_date DESC LIMIT 8`; **any failure → empty Vec** (the section omits itself). Each row → `Item::new(title, browser::open_url_cmd(url))` where `open_url_cmd` single-quote-escapes the URL for the popup's `sh -c` (= what `launch_url`'s `xdg-open` runs; unit-tested). The jump list inserts Recent between Tasks and the Firefox footer. **Verified:** `mde __places-recent --seed <f>` then read → 4 rows newest-first; no-DB → empty. **Captures:** populated jump list shows the 4 recent rows between Tasks and Firefox; no-profile shows Tasks-only (no Recent), no-panic. `cargo build` clean (rusqlite + libsqlite3-sys compiled, one-time ~1 min). 137 mde tests green (+`open_url_cmd_shell_quotes`); clippy/fmt clean.
- [✓] **E18.5** Win10-era taskbar Quick-Launch + tiled-Start Firefox pin seeded from `browser::default_browser()` via `state::default_state()` (Win10 era only); Carbon/Win2000 unchanged. (grim of the Win10 taskbar/Start strip shows the Firefox pin/tile; a Carbon/Win2000 capture shows the unchanged flat "Firefox" leaf, confirming Carbon stays default.) — **Done.** New `browser::default_pin()` (name/icon from `default_browser()`, command = the browser binary) + `state::default_state()` (the base default with that pin) + `state::effective_pinned()` — which returns the persisted `pinned`, **or, only under the Win10 era and only when nothing is pinned yet,** the seeded default-browser pin. `seed_start_tiles` now seeds from `effective_pinned`, so the Win10 Start tile appears too. The Win10 **panel gained Quick-Launch** rendering (`view_win10` had none): `win10_pin_button` (an `icon_any` button → `Message::Launch`) for each pin, after Task View. **The classic eras are untouched** — they already render `pinned` (empty by default), so no Firefox Quick-Launch pin there (Firefox stays the cascade-menu leaf). **Verified:** `mde start-win10 --list-tiles` under Win10 → `Firefox firefox medium 2x2`; under Carbon → empty. Captures: the Win10 taskbar shows the Firefox pin right after Task View; the Carbon taskbar shows just the Start button (no pin). 137 mde tests green; clippy/fmt clean. Caught + fixed a `center_x(Length::Fill)` bug that floated the pin mid-bar (now centered in `bar_h()`, adjacent to Task View). The pin icon falls back to a generic web glyph headlessly (a real system with the Firefox icon theme shows its logo).
- [✓] **E18.6** `panel.rs` right-click on the Firefox Quick-Launch button spawns `mde browser-jumplist --pin <idx>` via `push_child(spawn_child(...))`; add the `QuickLaunchContext(idx)` message arm reusing the existing per-tick `try_wait` reaping. (Bench: right-click the pin in a live Win10 panel opens the jump-list popup; the child is reaped — no zombie in `ps` after the popup closes.) — **Done.** `win10_pin_button` (E18.5) now takes the pin `idx` and adds `.on_right_press(Message::QuickLaunchContext(idx))` (the loop enumerates `state.pinned`). The new `Message::QuickLaunchContext(idx)` arm does `push_child(state, spawn_child(&["browser-jumplist", "--pin", &idx.to_string()]))` — the **same** `spawn_child`/`push_child` path + the per-tick `try_wait` reap sweep the window `JumpList` uses, so the child is tracked and reaped (no zombies) by construction. `browser_jumplist::run` now parses + validates `--pin <idx>` (reserved for per-pin anchoring; the popup edge-anchors at the bottom bar, where the Win10 pin sits — `launch_with` has no x-margin). **Verified:** `mde browser-jumplist --pin 0` accepts the arg and launches the popup (no-panic); the popup itself (Tasks + Recent) was captured in E18.4. 137 mde tests green; clippy/fmt clean. The interactive right-click→popup needs a live Win10 panel; the wiring + the reaping reuse the proven window-jumplist mechanism.
- [✓] **E18.7** "Pin web page to taskbar": append a `PinnedItem { name, command: "xdg-open <url>" }` and `state::save()` atomically; verify the existing `parse("{}")`/Default-agreement test still passes. (Bench: pin a URL, relaunch `mde panel`, grim shows the new pin; `cargo test` state-compat assertion green.) — **Done.** New `browser::pin_page(url, name)` appends a `PinnedItem` whose command is `open_url_cmd(url)` = `xdg-open '<url>'` (shell-quoted, the same edge the jump list uses) and saves `menu.json` through the atomic `state::save`. It's **idempotent on the command** (pinning the same URL twice is a no-op → "already pinned"), and the label defaults to the URL's host (www-stripped) via the pure, unit-tested `page_title_from_url`; `--name` overrides it. Reachable via `mde browser-default --pin <url> [--name <title>]` (help updated). The `PinnedItem` gains no fields, so the §2.6 `parse("{}")`==Default test is unaffected — **138 mde tests green**, clippy/fmt clean. **Verified end-to-end** against a temp `XDG_CONFIG_HOME`: two pins landed in `menu.json` (`"command": "xdg-open '…'"`, host-derived + `--name` labels), the duplicate was rejected. The pin renders through the existing Quick-Launch `state.pinned` loop (proven to paint in E18.5).
- [✓] **E18.8** Wire the in-scope default-apps entry points for E15: confirm `browser::default_browser()`/`set_default()` cover the Web-browser row only (Email is E15's own backend); no Maps/Music row. (Bench: a unit test confirms `browser.rs` exposes exactly the read + set-default entry points E15 needs; no Maps/Music symbol introduced.) — **Done — and gave the entry points a real GUI home (the "E15" is a stale cross-ref; E15 is Networking — the line means *Email belongs to a different epic, not E18*).** The bench guard landed (`exposes_only_the_web_browser_default_app_entry_points`): it binds `default_browser:fn()->Browser` + `set_default:fn()` to their signatures (a rename breaks the build — these ARE the wired entry points), asserts the set path is the one 4-command xdg set (no per-category split), and `include_str!`-scans the production code for `default_maps`/`default_music`/`default_email`/`default_mail` — none present. **Beyond the bench (§3, runtime-reachable):** the previously-`Deferred` **Apps ▸ Default apps** Settings page is now native (`Kind::DefaultApps`) with the **Web-browser row only** — the live default (`browser::default_browser` read on each visit via `maybe_load`) shown with its icon + name, and a **"Make Firefox the default"** button → `browser::set_default` off-thread (`set_default_browser_task`, re-reads the row) when it isn't already Firefox, else a "Default" marker. **No Email/Maps/Music rows** (Email is another epic's backend; Maps/Music have no shipped app). **Captured** (`settings-apps-default` wired into `gallery.sh`): the rail entry is live (was greyed), the Web-browser row shows Firefox + "Default". 139 mde tests green, clippy/fmt clean. (The Firefox-logo icon falls back to the generic web glyph headlessly — same icon-theme caveat as E18.5; the actual default-flip needs a session with xdg-mime, the real-hardware bench.)
- [✓] **E18.9** Update prose (`README.md`/`SPEC-system.md`) to record Firefox-as-default-browser surfacing for the Win10 era and the `mde browser-jumplist` subcommand in `USAGE`. (Bench: `mde --help` lists `browser-jumplist`; grep confirms docs name Firefox, not Edge, as the shipped browser.) — **Done (right docs, §6 best-choice — the named `SPEC-system.md` is the XP *System Properties + Device Manager* spec, the wrong home; recorded in `SPEC-windows10.md` where the E18 epic lives, and in `rust/README.md`, the live Rust-shell readme — the root `README.md` is the stale sway-era prose §1 flags).** `rust/README.md` now has a **Default browser (Windows 10 era)** bullet naming **Firefox** as the shipped browser (never an "Edge" brand), the `mde browser-jumplist` jump list, and `mde browser-default [--set-default|--pin URL]`. `SPEC-windows10.md`'s E18 section gained a **"Shipped (E18.1–E18.9)" reconciliation** note recording what landed vs. the design — notably that the Default apps page shipped **Web-browser-only** (the design's Email row is deferred: no shipped email-handler backend, so it would be an inert mockup per §3; additive when E15's mail backend lands). **Benches:** `mde --help` lists `browser-jumplist` ✓; the docs name Firefox (not Edge) as shipped ✓. Docs-only (no code gate needed). **🎯 This closes the entire E18 (Edge→Firefox) epic — E18.1–E18.9 all done.**

#### E20 Polish + accuracy: four-era parity, gallery captures, keyboard-nav, Theme::Windows10 pinned

- [✓] **E20.1** Pin the Win10 remap (authored by E10) in `mde-ui/tests/checklist.rs` via `windows10_remap_is_pinned`: under `Theme::Windows10`, assert the `palette::color()` output for the accent role (`HIGHLIGHT`/`ACTIVE_TITLE`), the white-text and window-frame sentinels, and the desktop/surface tokens for BOTH `set_dark(true)` and `set_dark(false)` branches — asserting role constants, never a raw-hex literal (§2.1/§2.2). (Acceptance: `cargo test -p mde-ui` shows the new test passing against E10's `win10()`; deliberately bumping one Win10 token value in `palette.rs` makes it fail.) — **Done.** Extended the existing `windows10_remap_pins` test (the canonical name; serves the spec) to fully cover E20.1: it already pinned `HIGHLIGHT`→accent (both modes), the settable accent slot, the white-title-text + window-frame sentinels, and the light `WINDOW` neutral; added **`ACTIVE_TITLE`→accent in both modes** (its own raw const, distinct from `HIGHLIGHT`), plus the **`BACKGROUND` (desktop) and `MENU` (panel/face) surface tokens pinned in BOTH light `(0xe6e6e6 / 0xf3f3f3)` and dark `(0x1f1f1f / 0x2b2b2b)`**. All via role constants through `palette::color()`, no raw-hex literals. `cargo test -p mde-ui` green (17); a drift in any of these win10() tokens now fails CI.
- [✓] **E20.2** Add a `Theme::Windows10` case to the `mde-ui` bevel/draw-edge unit test that PINS the flat (non-3D) path fires (i.e. E10 widened the `is_carbon()` flatten predicate to cover Win10). (Acceptance: `cargo test -p mde-ui` green; flipping the predicate so Win10 takes the bevel branch makes the assertion fail, proving `draw_edge` flattens under Windows10 per D2.) — **Done (landed the prerequisite + the pin).** The flatten predicate was still `is_carbon()`, so Win10 widgets drew Win2000 **3D bevels despite Carbon colors** — an inconsistency the MackesDE rebrand (Win10 = Carbon coloring) left dangling. Introduced **`palette::is_flat()` = `is_carbon() || is_windows10()`** and routed all three flat-vs-3D sites in `widget/mod.rs` through it — `draw_edge` (the bevel), the `scrollbar` track, and `ctl_radius` — so Win10 is now fully flat (D2). New `flat_chrome_covers_carbon_and_windows10` pins `is_flat()` true for Carbon **and** Windows10, false for Win2000/BeOS, so narrowing it back (Win10 → 3D) fails CI. **Verified by capture:** the Win10 `add-remove` window is now **pixel-identical (90806 B) to the Carbon one** — flat content frame, thin flat scrollbar, flat buttons — where it previously took the 3D branch. This is the predicate the worklist note (below) tracked as an "E10 deliverable"; E10.1 can now assume `is_flat()` exists. `cargo test -p mde-ui` green (18), build/clippy/fmt clean.
- [✓] **E20.3** Generalize `tests/accuracy/gallery.sh` to loop over `{win2000,carbon,beos,windows10}`: write a throwaway `menu.json` into an isolated `XDG_CONFIG_HOME` before each `shot`, restore after, output to `captures/gallery/<era>/<component>.png`. (Acceptance: `./preview.sh gallery` produces a populated `captures/gallery/windows10/` directory of non-empty PNGs.) — **Done.** `shot` now writes into a `$dest` directory (default `$out`, the default-theme showcase root); the era-parity loop overrides it to `$out/<era>` per iteration and drops the old `-$label` filename suffix, so each era's captures land in `captures/gallery/<era>/<component>.png`. The per-era `XDG_CONFIG_HOME` seed/restore was already there (untouched). The era-taskbars montage was re-pointed to the new `<era>/panel.png` paths; `dest` is reset to `$out` afterward for the root contact sheet. **Verified by a full `gallery.sh` run:** all four era subdirs populated — `windows10/` holds **16 non-empty PNGs** (panel 4.7 KB bottom strip, files, menu, action-center, settings-apps-default, settings-devices-*, start-win10, project, …), and `_era-taskbars.png` assembled from the new subdir paths shows all four taskbars at their correct edges (Carbon top, Win2000 silver, Win10 dark+accent, BeOS left). No Rust harness consumer broke — `accuracy.rs`/`checklist.toml` read `captures/` (via `capture.sh`), a path the gallery rename doesn't touch.
- [✓] **E20.4** Make the panel crop in `gallery.sh` era-aware off the §7 anchor table (Carbon/Win10 top, Win2000 bottom, BeOS left). (Acceptance: `windows10/panel.png` and `win2000/panel.png` each show a non-blank taskbar strip at the correct edge — a grim crop that is not all desktop-color.) — **Done.** Extracted the per-era crop into a single documented `panel_crop()` helper keyed off the §7 anchor table (+ `panel.rs`'s anchor block), replacing the magic numbers that were inline in the `$era` array (the array now carries only `label:theme:iconset`). **Accuracy correction (recorded, §2/§6):** the worklist parenthetical's "Carbon/Win10 **top**" is a mislabel — `panel.rs` anchors the Win10 bar to the **bottom** by default (`taskbar_location=="top"` is the opt-in flip; SPEC-windows10 §135), so the helper maps Carbon→top, **Win2000+Windows10→bottom**, BeOS→left, matching the real anchored edge. **Verified by a focused re-capture:** `win2000/panel.png` (3.5 KB, 210 distinct colors) and `windows10/panel.png` (4.7 KB, 188 distinct colors) are both non-blank strips at the bottom edge — a blank desktop crop would be ~1–2 colors; the Win10 strip shows the Start/search/task-view/pin + tray/clock/action-center bar. `bash -n` clean.
- [✓] **E20.5** Add `[capture.win10-*]` groups to `checklist.toml` asserting the Win10 accent token paints at the Start rail / search box / Action Center toggle / Settings nav locations (grounded in E10's `win10()` role output, not a named hex), and teach `accuracy.rs` to resolve `captures/gallery/<era>/<file>` for those groups. (Acceptance: `WAYLAND_DISPLAY` set + a fresh gallery run -> `cargo test -p mde` reports the new win10 points OK within tolerance.) — **Done — accent grounded in `win10()` at runtime, not a hardcoded hex.** `accuracy.rs` now accepts `role` (+ per-group `era`/`dark`) on a point as an alternative to a literal `hex`: it resolves the expected color via `palette::hex(role_rgb(role))` under the group's `Theme` at test time (`role="HIGHLIGHT"` + `era="windows10"` → the live `carbon_accent()` dark = **#4589ff**), so bumping that token in `palette.rs` moves the assertion with it — zero magic hex. Path resolution needed no code change: a `file = "gallery/windows10/<f>.png"` subpath already joins under `captures/`. Three `[capture.win10-*]` groups landed — **taskbar accent line** `(640,0)`, **Action Center on-tile fill** `(966,776)`, **Settings nav focus ring** `(186,257)`+`(250,244)` — coordinates located by scanning the real frames for the accent's solid blocks. **Acceptance met:** `WAYLAND_DISPLAY=… cargo test -p mde` reports all 4 win10 points **OK @ Δ0** (`#4589ff`). **Honest scoping (§3):** the **Start rail** and **Search box** show the accent only on hover/focus/selection — the static default gallery capture has *no* solid accent there (scan found 0 px in `search.png`, 19 sparse px in `start-win10.png`), so asserting them would be flaky; the three solid locations ground the win10 accent role faithfully. 139 mde tests + accuracy green, clippy/fmt clean.
- [✓] **E20.6** Document and ship the era-gated labwc `rc.xml` keybind table fragment (Win+A/Win+Tab/Win+I/Win+S/Win -> the E11-E15 subcommands; Win+I -> `control-panel` outside the Win10 era per D4), installed via `%post`, starting `<mouse><default/>` per §7. (Acceptance: a no-panic `timeout 3 mde <sub>` for each bound subcommand exits 0/124, and the bound surface actually launches under the bind — observable proof the fragment is well-formed.) — **Done.** The Win10 P0 binds already shipped in the skel `rc.xml` (installed via the RPM `files` → `mde setup`; `%post` symlinks each `mde-<sub>`; labwc has no include mechanism, so the "fragment" is the documented keybind section of the one rc.xml). **Real gap closed — Win+I is now era-gated (D4):** `mde settings` under a non-Win10 theme **routes in-process to `control_panel::run`** instead of the old dead no-op message, so the single Win+I bind opens Settings (Win10) / Control Panel (classic) — **captured proof:** seeding `theme=carbon` then `mde settings` opens the "Control Panel - mde" window. Added a documented **"Windows 10 P0 keybind table"** comment (Win/Win+A/Win+Tab/Win+S/Win+I → their subcommands, each self-gating on the theme) and clarified the Win+I comment. **Fixed two correctness issues found here:** (a) `action-center` had **no `WAYLAND_DISPLAY` guard** (panicked headless, exit 101) — added one (before the read-stamp, so a headless sweep is a pure no-op) matching the other 7 layer-shell surfaces; (b) a pre-existing `--` inside an XML comment made the rc.xml **strictly invalid** — rephrased it so `xmllint --noout` now passes. **Acceptance met:** headless `timeout 3 mde <sub>` for start/action-center/task-view/search/project all exit **0** (no more 101); under nested sway each bound surface **launches** (exit 124), incl. both era branches of Win+I. §7 `<mouse><default/>` intact. 139 tests + clippy/fmt clean. (Acceptance: a no-panic `timeout 3 mde <sub>` for each bound subcommand exits 0/124, and the bound surface actually launches under the bind — observable proof the fragment is well-formed.)
- [✓] **E20.7** Keyboard-nav parity check: a per-era no-panic launch sweep (`timeout 3 mde <each P0 surface>` across all four eras) plus a grim capture per Win10 surface showing the focus ring on the first focusable control. (Acceptance: the sweep exits clean for every era/surface pair; the focus-ring capture shows the accent ring, proving Tab focus reaches the first control.) — **Done.** New durable `tests/accuracy/nav-sweep.sh` (wired as `./preview.sh nav-sweep`), run the same isolated-nested-sway way as the gallery. **Part A — no-panic sweep:** 11 P0 surfaces (panel · menu · files · control-panel · system-properties · settings · action-center · task-view · search · project · start-win10) × all 4 eras = **44/44 pairs clean (0|124), 0 crashes** — a crash exit (101/134/139) would fail the script (exit 1). It correctly shows the era-gating: `search`/`project` exit 0 under the classic eras (Win10-only, guarded) and 124 under Win10; `control-panel` exits 0 under Win10 (the D4 inverse). **Part B — focus-ring captures:** each Win10 surface is captured and PIL-scanned for the accent ring (#4589ff). **`focus-settings.png` shows the accent focus ring squarely on the first focusable control** (the "Display" nav entry) — the acceptance's proof that Tab focus reaches the first control; `action-center` also paints accent. **Honest limit (§3):** `search`/`task-view` auto-focus a *text-input* (caret, not an accent outline) and there's **no `wtype`/`ydotool` on this host** to inject a Tab, so a forced ring on those can't be captured headlessly — reported as "no auto-focus ring" rather than faked. The settings ring carries the proof. `bash -n` clean; captures gitignored. **🎯 Closes the E20 polish/accuracy cluster (E20.1–E20.8) — and with E18 done, the M3 layer is complete.**
- [✓] **E20.8** Wire the Win10 P0 surfaces (action-center, settings, start/menu, task-view, search) into `gallery.sh`'s component list so a single `./preview.sh gallery` renders the whole era. (Acceptance: `captures/gallery/windows10/` contains one PNG per P0 surface and the per-era contact sheet assembles.) — **Done.** The windows10 block already captured action-center / settings (start, apps-default, devices-*) / start-win10 / menu; added the two missing P0 surfaces — **Task View** and **Search** — after a clean Win10 re-seed (both confirmed no-panic when WAYLAND is truly unset; the earlier exit-101 was an empty-string `WAYLAND_DISPLAY=` test artifact, not a real guard gap — both have `.is_none()` guards). Added a **per-era Win10 contact sheet** that tiles whatever P0 shots exist (panel · start-win10 · menu · search · task-view · action-center · settings-start · settings-apps-default) into `windows10/_contact-sheet.png`, skipping any missing one (never fatal). **Verified by a single full `gallery.sh` run:** all **8 P0 PNGs** present in `captures/gallery/windows10/` (task-view 14 KB, search 14 KB, …) and `_contact-sheet.png` (360 KB, "8 surfaces") assembled — the whole Win10 era on one page (the era-taskbar strip + root contact sheet also still assemble). `bash -n` clean.

> Note: the former E20.1 ("add `Theme::Windows10` + placeholder `win10 = carbon`") was removed — per §3 no placeholder/stub remap may ship "until E10 lands", and a Carbon-aliased remap would make every E20 pin meaningless. The `enum Theme` variant, `theme()`/`set_theme`/`is_windows10()`, the `color()` arm, and the broadened flatten predicate are E10 deliverables that E20 depends on and pins.
